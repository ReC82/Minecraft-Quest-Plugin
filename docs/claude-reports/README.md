# Rapports Claude

Historique chronologique, **immuable**, de chaque session de développement effectuée par Claude
dans ce dépôt — voir `CLAUDE.md`, section « Session Reports », pour la règle qui impose leur
création. Un rapport est créé après **chaque** demande (développement, correction de bug,
diagnostic, refactoring, migration, ou modification de configuration/documentation), même si la
tâche est petite, partielle, bloquée, ou n'a finalement modifié aucun code.

## Pourquoi ce dossier existe

Ces rapports ne remplacent **jamais** la documentation fonctionnelle du projet
(`docs/current_state.md`, `docs/ARCHITECTURE.md`, `docs/RPGQUEST_BIBLE.md`, etc.), qui continue
d'être maintenue à jour normalement. Ils servent un besoin différent : pouvoir être envoyés
**tels quels** à un autre assistant (ou humain) qui n'a suivi aucune des sessions précédentes, et
qui doit pouvoir comprendre, sans contexte supplémentaire :

- ce qui était demandé ;
- ce qui existait avant l'intervention ;
- ce qui a changé, et pourquoi (décisions techniques incluses) ;
- comment cela a été testé ;
- comment le déployer ;
- ce qui reste éventuellement problématique ou non traité.

## Règles

- **Un fichier par demande, jamais réutilisé** — un rapport n'écrase jamais un rapport précédent.
- **Immuable après création** — ne jamais modifier un ancien rapport pour refléter l'état actuel
  du projet. Un rapport documente l'état des choses *au moment où il a été écrit*, pas l'état
  actuel (voir `docs/current_state.md` pour ça).
- **Toujours créé, même en échec** — un statut `PARTIAL`/`BLOCKED`, des tests qui échouent, ou une
  demande purement diagnostique n'exemptent jamais de la création du rapport.
- **Reflète la réalité, pas la demande initiale** — le rapport décrit ce qui a été *réellement*
  fait, qui peut différer de ce qui était prévu au départ.
- **Mesure du temps réel, jamais inventée** — voir `CLAUDE.md`, section « Session Reports — Time
  Tracking » : début/fin/durée réels de la tâche, ou `non mesurable` explicitement si le début
  n'a pas pu être capturé.

## Nom de fichier

```
YYYY-MM-DD_HHMM_<description-courte>.md
```

Date et heure **locales réelles** au moment de la création du rapport (pas celles de la tâche
elle-même si le rapport est rédigé après coup). Exemples :

- `2026-08-21_2006_worldportal-debug-tools.md`
- `2026-08-21_2142_fix-hub-auto-teleport.md`
- `2026-08-22_1015_story-npc-integration.md`

## Gabarit obligatoire

Chaque rapport contient au minimum les sections suivantes, dans cet ordre :

```markdown
# RPGQuest — Rapport Claude

## Informations
* Date :
* Heure :
* Sujet :
* Statut : DONE / PARTIAL / BLOCKED
* Branche Git :
* Commit actuel si disponible :
* Début de la tâche : YYYY-MM-DD HH:MM:SS (heure locale réelle, ou « non mesurable »)
* Fin de la tâche : YYYY-MM-DD HH:MM:SS (heure locale réelle)
* Durée totale : HH:MM:SS (calculée entre les deux, jamais estimée)

## Demande
## Analyse
## Travail effectué
## Fichiers créés
## Fichiers modifiés
## Base de données / migrations
## Configuration / données
## Tests automatiques
## Tests manuels à effectuer
## Résultat attendu
## Reset / retour à l'état initial
## Déploiement VeryGames
### À transférer
### Ne PAS transférer/altérer
### Redémarrage requis
### Migration automatique
## Rollback
## Logs / diagnostic
## Documentation mise à jour
## Limitations / travail restant
## Prochaine étape suggérée
```

Voir n'importe quel rapport existant ci-dessous pour un exemple rempli.

## Index chronologique

| Date | Sujet | Statut | Fichier |
|---|---|---|---|
| 2026-08-21 | Outils de diagnostic WorldPortal (`here`/`debug`, TP-TRACE) | DONE | [2026-08-21_2009_worldportal-debug-tools.md](2026-08-21_2009_worldportal-debug-tools.md) |
| 2026-08-21 | Progression automatique de Storyline (connexion au moteur de quête) | DONE | [2026-08-21_2135_story-automatic-progression.md](2026-08-21_2135_story-automatic-progression.md) |
| 2026-08-23 | Investigation BREAK_BLOCK ne progresse pas dans `wild` (story_test) | BLOCKED | [2026-08-23_1234_break-block-wild-investigation.md](2026-08-23_1234_break-block-wild-investigation.md) |
| 2026-08-23 | Instrumentation `[QUEST-TRACE]` pour diagnostiquer la chaîne BREAK_BLOCK | DONE | [2026-08-23_1256_quest-trace-break-block-instrumentation.md](2026-08-23_1256_quest-trace-break-block-instrumentation.md) |
| 2026-08-23 | Investigation reset progression Story/quête (death/respawn, reconnexion) + correctif `premiers_pas.yml` manquant | PARTIAL | [2026-08-23_1643_quest-story-progress-persistence-investigation.md](2026-08-23_1643_quest-story-progress-persistence-investigation.md) |
| 2026-08-23 | Premier claim joueur 5×5 débloqué par une Story (CLAIM_TIER_1 → PNJ Jo → Acte de propriété → pose sans commande) | DONE | [2026-08-23_1758_premier-claim-5x5-story-deed.md](2026-08-23_1758_premier-claim-5x5-story-deed.md) |
| 2026-08-23 | 3 améliorations Claim : Jo « retourner à son claim », `/claim admin tp`, monde `claims` réellement pacifique | DONE | [2026-08-23_1857_claim-gohome-mobs-pacifiques.md](2026-08-23_1857_claim-gohome-mobs-pacifiques.md) |
| 2026-08-23 | MVP monde `claims` : frontière par particules, zone résidentielle safe, Pierre de retour, blocage Nether, complétion config.yml | DONE | [2026-08-23_2003_claims-mvp-frontiere-pacifique-pierre-retour.md](2026-08-23_2003_claims-mvp-frontiere-pacifique-pierre-retour.md) |
| 2026-09-05 | 4 corrections UX post-validation VeryGames : indicateur de canalisation, Pierre de retour limitée à `claims` + anti-perte, faisceau de retrouvaille du claim à distance | DONE | [2026-09-05_1729_claims-mvp-corrections-ux-pierre-retour-faisceau.md](2026-09-05_1729_claims-mvp-corrections-ux-pierre-retour-faisceau.md) |
| 2026-09-05 | Boucle joueur Hub ↔ Wild : système soulbound générique, item Journal des quêtes, Rune de rappel (cooldown persistant), avertissement compact avant entrée Wild, Waystones génératives, config `travel.*`, migrations V16/V17, `/rpgadmin waystone` | DONE | [2026-09-05_1837_boucle-joueur-hub-wild-soulbound-journal-rune-waystones.md](2026-09-05_1837_boucle-joueur-hub-wild-soulbound-journal-rune-waystones.md) |
| 2026-09-05 | Reset admin « nouveau joueur » : `/rpgadmin player resetnew <joueur> confirm` — remet l'état RPGQuest d'un seul joueur (online/offline) à l'équivalent « jamais joué » (quêtes, Stories, variables/unlocks, progression RPG, découvertes Waystones, cooldowns, claim principal, objets RPGQuest de l'inventaire). Voir la section « Reset complet "nouveau joueur" » du rapport et [docs/ADMIN_PLAYER_RESET.md](../ADMIN_PLAYER_RESET.md). | DONE | [2026-09-05_2002_reset-admin-nouveau-joueur.md](2026-09-05_2002_reset-admin-nouveau-joueur.md) |
| 2026-09-05 | Refonte de `CLAUDE.md` : cadre unique des règles pour toutes les futures sessions Claude Code (bootstrap `.ai/`, GIT/source de vérité, politique de push vers la branche de travail, branches sans `develop` permanente, TESTS/BUILD, DOCUMENTATION, SÉCURITÉ, DÉPLOIEMENT, GitHub Issues, mode de travail). Aucun code fonctionnel modifié. | DONE | [2026-09-05_2032_claude-md-refonte-regles-sessions.md](2026-09-05_2032_claude-md-refonte-regles-sessions.md) |
| 2026-09-05 | Issue #8 — mode preview/dry-run du reset joueur admin : `/rpgadmin player resetnew <joueur> preview` liste, catégorie par catégorie, ce qu'un reset réel effacerait, **sans aucune écriture**. Nouveaux points de lecture seule (`PlayerVariableRepository#findAllForPlayer`, `WaystoneService#discoveryCount`, `StoryService#progressRecords`), `PlayerResetService#previewReset`, refactor interne `countOrRemoveRpgItems`. 4 tests ajoutés. Comportement du reset réel inchangé. | DONE | [2026-09-05_2101_player-reset-preview-issue-8.md](2026-09-05_2101_player-reset-preview-issue-8.md) |
| 2026-09-05 | Issue #11 — Guide « centre d'aide » + journal de quêtes en GUI via le Libraire : `guide.yml` réécrit en menu d'aide structuré (nœud `help_menu` + sujets, orientations textuelles) ; nouveau registre `hub.HubGuideRegistry` (`hub-guides/*.yml`) pour la structure multi-Hub + diagnostic `/rpgadmin guide list\|info`. Le journal (`rpgquest:journal_quetes`) ouvre désormais la GUI `QuestJournalService` au clic droit ; GUI passée de 3 à **2 onglets** (« en cours » / « terminées »), onglet catalogue « Disponibles » supprimé ; `QuestJournalBookService` (résumé chat) supprimé. Tests : `hub/*` (3), `BundledDialoguesValidityTest`, `QuestJournalServiceTest` réécrit. | DONE | [2026-09-05_2233_guide-help-center-quest-journal-issue-11.md](2026-09-05_2233_guide-help-center-quest-journal-issue-11.md) |
| 2026-09-06 | Issue #11 — correction du bug de navigation entre onglets du journal : `COMPLETED → IN_PROGRESS` ne rafraîchissait plus la vue et le bouton « Fermer » devenait inerte après le 1er changement d'onglet. Cause : `showList`/`showDetail` enregistraient la session AVANT `player.openInventory()`, dont le `InventoryCloseEvent` synchrone (menu précédent) l'effaçait aussitôt via `handleClose`. Correctif : `openSessions.put(...)` déplacé APRÈS `openInventory`. Test de régression bidirectionnel `IN_PROGRESS → COMPLETED → IN_PROGRESS` (vrais clics, listener enregistré, sans fermer) + vérif du bouton Fermer. 931 tests verts. Redéployé DEV VeryGames (JAR seul). #11 non fermée. | DONE | [2026-09-06_1014_fix-journal-navigation-onglets-issue-11.md](2026-09-06_1014_fix-journal-navigation-onglets-issue-11.md) |
| 2026-09-06 | Issues #21/#22/#23 — parcours Claims cohérent de bout en bout. Audit : l'unlock du 1er claim = variable `CLAIM_TIER_1 == "true"`, accordée uniquement par `crystal_hunt` (fin de l'histoire principale, au Garde) ; le Guide omettait ce prérequis, d'où « Jo ne propose rien » après `resetnew`. Ajouts : `ClaimWorldAccessGuard` (portail Hub→claims fermé sans unlock, aucune téléportation, bypass `rpgquest.admin.world` seul) composé via `CompositeWorldPortalEntryGuard` ; `ClaimWorldSafetyListener` (Pierre de retour donnée à l'arrivée, joueur non éligible renvoyé au Hub — personne coincé, retour sans commande) ; modificateur de dialogue `negate: true` (`NegatedCondition`) ; `guide.yml`/`jo.yml` réécrits, Jo adapté aux 3 états. Tests : `ClaimWorldAccessGuardTest`, `ClaimWorldSafetyListenerTest`, `CompositeWorldPortalEntryGuardTest`, + `DialogueDefinitionParserTest`/`DialogueSessionEngineTest`. #21/#22/#23 non fermées (validation manuelle). | DONE | [2026-09-06_1116_parcours-claims-coherent-issues-21-22-23.md](2026-09-06_1116_parcours-claims-coherent-issues-21-22-23.md) |
| 2026-09-06 | Audit du parcours principal (nouveau joueur → `crystal_hunt` → `CLAIM_TIER_1`) + ré-audit #21/#22. Causes trouvées via le contenu réel du serveur DEV : (1) aucun PNJ Citizens lié `guard` (`npc_citizens_bindings` : guide, libraire, help, jeff, junior, jo) → `first_steps` ET `crystal_hunt` indémarrables ; (2) `dialogues/guard.yml` déployé périmé, sans branche `crystal_hunt`. #21/#22 : le code refuse bien l'entrée / renvoie au Hub pour un compte non-OP — les symptômes correspondent au bypass `rpgquest.admin.world` (défaut op) déclenché par un test OP. Ajouts : journalisation de décision `[claims-access]` / `[claims-safety]` ; redéploiement DEV du JAR + `dialogues/guard.yml`. Doc : §1b PNJ obligatoires (NPC_DIALOGUES_QUESTS_GUIDE), BIBLE, current_state, VERYGAMES. #21/#22/#23 non fermées ; création du PNJ Garde + validation en jeu restantes. | PARTIAL | [2026-09-06_1219_audit-parcours-principal-garde-crystal-hunt.md](2026-09-06_1219_audit-parcours-principal-garde-crystal-hunt.md) |
| 2026-09-06 | Issue #36 — raccourcis admin/DEV pour tester quêtes & stories sans rejouer le gameplay. Réutilise les services métier (jamais d'écriture directe en base). `QuestProgressEngine.accept(..., ignorePrerequisites)` ; `StoryService.adminAdvance`/`adminComplete` (avance déterministe, garde anti double-récompense conservée via `forceComplete`, ordre respecté, bornée) ; `/rpgadmin quest start\|complete\|reset <joueur> <id>` (+ `force`), `/rpgadmin story advance\|complete <joueur> <storyId>` (message « étape à tester »), `/rpgadmin player variable get\|set <joueur> <clé>`. Permission `rpgquest.admin.world` ; `variable set` exige en plus `rpgquest.admin.debug`. Tab-complétion + journalisation. Tests : `StoryServiceTest` +8, `RpgAdminTestShortcutsCommandTest` (11), `QuestProgressEngineTest` +2. Doc : `ADMIN_TEST_SHORTCUTS.md` (nouveau), BIBLE, current_state. Branche `feat/36-admin-test-shortcuts`, non fusionnée, #36 non fermée. | DONE | [2026-09-06_2028_admin-test-shortcuts-issue-36.md](2026-09-06_2028_admin-test-shortcuts-issue-36.md) |
| 2026-09-07 | Préparation d'une session de validation manuelle DEV : audit de tout le backlog `needs-manual-test` (15 issues) → **seules #21/#22/#23/#36 sont implémentées et déployées** (JAR `bcc3a6ec…`) ; #27 sur une branche divergente non déployée ; #24/#25/#26/#28/#30/#31/#32/#33/#34/#35 **non implémentées**. Checklist unique séquentielle PHASE 0→7 (redémarrage + création du PNJ **Garde** = blocant ; parcours Claims non-OP couvrant #21+#22+#23 ; fiche #36 en 5 min ; commentaires de validation pré-rédigés). Audit du serveur DEV : Garde toujours absent, contenu YAML/portails/items OK, `Rondoudou9000` = compte de test neuf non-OP. + Audit du module `web-api` et **`docs/control-panel/`** (vision, architecture, sécurité, config, bridge RPGQuest, roadmap, 7 ADR) pour l'issue #37 — documentation seule, aucun code. Aucune issue fermée, aucune branche fusionnée, aucun déploiement. | DONE | [2026-09-07_0744_preparation-session-validation-manuelle-backlog.md](2026-09-07_0744_preparation-session-validation-manuelle-backlog.md) |
| 2026-09-07 | Issue #37 — socle exécutable du RPGQuest Control Panel : premier flux vertical navigateur → login → dashboard → bridge → **état réel du plugin**. Nouveau module Gradle **`control-panel/`** autonome (auth owner PBKDF2 + session signée HMAC + CSRF + logout, config externe multi-cibles `Target`, `PermissionService`/`Role`/`Permission`, audit log append-only `control-panel.db`, dashboard sobre responsive, gestion propre de l'indisponibilité du bridge). Côté plugin : package **`web.admin`** — `WebAdminServer` + `HealthSource`/`BukkitHealthSource`, route `GET /admin/v1/health` authentifiée Bearer, **fail-closed**, config **par variables d'environnement uniquement** (ADR-008, aucun changement `config.yml`/`PluginConfig`). Tests : `PanelAppTest` (11), `BridgeClientTest` (4), `PasswordHasherTest` (4), `JsonTest` (3), plugin `WebAdminServerTest` (4) — `./gradlew test build` vert. Audit AWS **lecture seule** pour #44 (nginx + 2 vhosts + ports libres + TLS Certbot ; aucune conf touchée) → `docs/control-panel/AWS.md`. Doc `docs/control-panel/` alignée + ADR-008/009/010. Branche `feat/37-control-panel`, non fusionnée, #37 **non fermée** (dépend de #44 + décision d'hébergement du bridge). | DONE | [2026-09-07_0820_control-panel-socle-issue-37.md](2026-09-07_0820_control-panel-socle-issue-37.md) |
| 2026-09-07 | Issue #44 — déploiement de **PlugAdmin** (socle #37) sur AWS via **https://plugadmin.lodylands.com**, sans perturber `dig.lodygames.com` / `lodylands.com`. Audit AWS avant mutation (nginx 1.24, 2 vhosts, port 8090 libre, IP `3.226.216.90`) ; **DNS déjà résolu** → aucune action DNS. Scripts reproductibles `scripts/plugadmin/` (`install.sh` idempotent, `deploy.sh`, `rollback.sh` + templates). Installé : utilisateur système dédié `plugadmin`, app sous `/opt/plugadmin/app` (`installDist`), secrets `/etc/plugadmin/plugadmin.env` (`0640`, hors Git ; `RPGQUEST_PANEL_SECRET` + `RPGQUEST_BRIDGE_TOKEN_DEV` générés, `OWNER_HASH` fourni par l'owner), `control-panel.db` sous `/var/lib/plugadmin`, service systemd `plugadmin` durci (`enable`, `Restart=on-failure`), vhost nginx **dédié** `sites-available/plugadmin` (`nginx -t` + `reload`, jamais `restart`), certificat Certbot **ECDSA** `plugadmin.lodylands.com` (exp. 2026-12-06) + redirection 80→443. Vérifs live : `/health` local **et** public 200 ; backend non exposé sur `:8090` ; `GET /login` 200 + en-têtes sécurité + cookie CSRF `Secure`/`HttpOnly`/`SameSite` ; `/dashboard` anonyme → 303 ; login invalide → 401 ; CSRF manquant/faux → 403 ; systemd start/stop/restart + reprise après `kill -9` ; dashboard « RPGQuest DEV indisponible » **sans 500** (validé sur instance jetable isolée). **Non-régression autres sites : 200/301 inchangés avant/après**, 3 certificats intacts. Aucun code Java modifié. Aucune issue fermée, aucune branche fusionnée. **PARTIAL** : login owner réel par navigateur externe reste à faire par l'owner (mot de passe non communiqué). | PARTIAL | [2026-09-07_0935_plugadmin-aws-deploy-issue-44.md](2026-09-07_0935_plugadmin-aws-deploy-issue-44.md) |
| 2026-09-07 | Issue #51 — **agent sortant** RPGQuest/VeryGames → PlugAdmin/AWS : inversion du flux du bridge #37 (RPGQuest initie l'HTTPS sortant). Plugin `com.lodygames.rpgquest.web.agent` : `AgentConfigLoader` (fichier local hors Git `plugadmin-agent.properties`, fail-closed), `HeartbeatPayload` (réutilise `HealthSource` #37, zéro duplication), `PlugAdminClient` (`java.net.http`, cert normal, corps borné), `AgentLoop` (heartbeat + file d'actions, backoff, idempotence via `ProcessedActionCache`), `AgentActionExecutor` (whitelist `AgentActionType` → services métier ; MVP = `player.variable.get` seul, jamais `/rpgadmin` texte), `PlugAdminAgent` (PluginService async). Control Panel `panel.agent` : `AgentEndpoints` (`/agent/v1/{heartbeat,actions,actions/{id}/result}`, auth par agent temps constant, 413/503), `AgentStore` (`control-panel.db` : `agent_heartbeat` + `agent_action`, idempotent, expiration), `AgentLiveness` ONLINE/STALE/OFFLINE, dashboard « AGENT DISTANT » prioritaire + page `/agents`. Tests : `web.agent.*` 30, `panel.agent.*` 21, `PanelAppTest` +1 — `./gradlew clean build` vert (plugin 1005). **Déployé sur VeryGames DEV** (JAR `5e9d9a5e…` + fichier agent) via `deploy-verygames.sh` ; **RCON VeryGames validé** (mention « pas de RCON » corrigée) + nouveaux `scripts/verygames-rcon.py` / `verygames-restart.sh` / `plugadmin/send-mail.py` ; redémarrage RCON exécuté. **Validation live** : heartbeat réel reçu, dashboard ONLINE (données), `player.variable.get` → SUCCESS, type inconnu → REJECTED, idempotence OK, panne PlugAdmin n'affecte pas Minecraft + reconnexion OK. Email SMTP de fin de tests envoyé. ADR-011 (remplace ADR-004). Branche `feat/51-plugadmin-outbound-agent`, non fusionnée, #51 non fermée. | DONE | [2026-09-07_1127_plugadmin-outbound-agent-issue-51.md](2026-09-07_1127_plugadmin-outbound-agent-issue-51.md) |
| 2026-09-07 | Issue #40 — **abstraction de la persistance** + moteur de base configurable (socle SQLite → MySQL, hors #41/#42). Audit : tout le SQL est déjà confiné à `com.lodygames.rpgquest.database` (aucun SQL dans les services de gameplay) ; couplage réel = URL JDBC + `PRAGMA` en dur de `DatabaseManager`, `PRAGMA user_version` de `SchemaMigrator`, syntaxe SQLite (`ON CONFLICT`/`INSERT OR IGNORE`/`AUTOINCREMENT`) dans 20 repositories + migrations. Nouveau : `DatabaseSettings`/`DatabaseType` (config `database.type: sqlite\|mysql`, mot de passe via `password-env` uniquement), `DatabaseEngine` (`SqliteDatabaseEngine` câblé / `MySqlDatabaseEngine` reconnu — corps réel = #41) + `DatabaseEngineFactory` (unique `switch`), `SqlDialect` (`SqliteDialect`/`MySqlDialect` : upsert, insert-ignore, identité, `columnExists`), `SchemaHistory` (`PragmaUserVersionHistory` SQLite natif / `MigrationTableHistory` portable), `SchemaMigration`+`SchemaMigrationRunner` (application ordonnée, idempotente, échec nommé, `targetVersion`). `SchemaMigrator` → catalogue `ALL` (SQL des 17 migrations **inchangé**). `DatabaseManager` : ctor `Path` conservé (SQLite, inchangé) + ctor `DatabaseEngine` + `healthCheck()` non bloquant. `PluginConfig.databaseFile`→`DatabaseSettings database` (+ `databaseFile()` raccourci). Tests : +25 `database.*` + 8 `ConfigValidatorTest` ; `SchemaMigratorTest` (21) et tous les `*RepositoryTest` **inchangés et verts**. `./gradlew test build` vert. Doc : `docs/PERSISTENCE.md` (nouveau) + ARCHITECTURE / current_state / BIBLE / INDEX / CONTEXT. Branche `feat/40-persistence-abstraction`, non fusionnée, #40 non fermée (backend MySQL réel = #41). | DONE | [2026-09-07_1315_persistence-abstraction-issue-40.md](2026-09-07_1315_persistence-abstraction-issue-40.md) |
| 2026-09-07 | Outillage admin du Control Panel (P0) : rafraîchissement auto des actions (issue #65) + pages **Joueurs / Quêtes / Stories**. Canal agent (#51) étendu de `player.variable.get` à 15 types whitelistés adossés aux **services métier existants** via la façade `AgentActions`/`BukkitAgentActions` (jamais de commande texte, jamais de SQL, mutations sur le thread principal) : lectures `player.list` / `quest.list` / `quest.player.status` / `story.list` / `story.player.status` / `item.list` / `player.resetnew.preview` ; mutations `player.item.give` / `quest.start\|complete\|reset` / `story.advance\|complete` / `player.variable.set` / `player.resetnew.confirm`. Double liste blanche `AgentActionType` ↔ `AgentActionCatalog`, validation 3 couches, `confirm` obligatoire sur les mutations, audit. `#65` : `/assets/panel.js` (même origine, conforme CSP) + `/agents/actions.json`, polling seulement s'il reste du `PENDING`. Tests : `AgentActionExecutorTest` étendu, `AgentActionCatalogTest`, `BusinessPagesTest`, `AgentActionsRefreshTest` ; `./gradlew :test :build :control-panel:build` vert. **P1 (PNJ / Diagnostics / autocomplete #66) et P2 (édition dialogues) non traités**. Branche `feat/control-panel-admin-tools`, non fusionnée, non déployée. | PARTIAL | [2026-09-07_2159_control-panel-admin-tools.md](2026-09-07_2159_control-panel-admin-tools.md) |
| 2026-09-08 | Bug #65 — durcissement du rafraîchissement auto des actions agent (page Agents). Audit bout-en-bout : le mécanisme de `0eee145` (`/assets/panel.js` + `/agents/actions.json`) est fonctionnellement correct (rendu serveur `data-actions-pending`, JSON valide, script exécuté sous `defer`, CSP/proxy OK) mais jamais validé manuellement et dépendant d'un signal de démarrage unique (le compteur serveur figé). `panel.js` : le polling démarre maintenant sur `data-actions-pending > 0` **ou** sur un statut non terminal (`PENDING`/`DELIVERED`) encore visible dans le `<tbody>` (`tableHasPending`), et fait un **premier relevé immédiat** (plus d'attente de 2 s). Garanties inchangées : arrêt à `pending == 0`, garde-fou 5 min, aucun polling si rien en cours, aucun script inline/dépendance. `AgentActionsRefreshTest` renforcé (assertions HTML `data-actions-pending` 1 puis 0 + pastilles PENDING/SUCCESS) + 1 nouveau test verrouillant le comportement du JS servi. `:control-panel:test` (66) + `:control-panel:build` verts ; comportement JS vérifié en DOM headless. Backend + agent inchangés. Doc : `docs/control-panel/ROADMAP.md`, `docs/current_state.md`. Branche `feat/control-panel-admin-tools`, non fusionnée, non déployée, #65 non fermée (validation manuelle navigateur restante). | DONE | [2026-09-08_0648_fix-65-refresh-actions-agent.md](2026-09-08_0648_fix-65-refresh-actions-agent.md) |
| 2026-09-08 | Page « Joueurs » du Control Panel — état constaté. La demande partait de « Joueurs — à venir » ; sur la branche courante, `/players` est **déjà** dans le menu (`Layout.nav` activé au commit `594d865`), routée (`handleBusinessPage`, `Permission.PLAYERS_READ`, session obligatoire), et affiche le roster réel via l'action **existante** `player.list` (`AgentPages.players()` → `AgentStore.latestActionOfType`) : nom, UUID (tronqué + complet en `title`), monde, position ; messages « aucune liste chargée » / « aucun joueur connecté » / bandeau « aucun agent configuré ». La page contient **aussi** l'outillage P0 (variables / GIVE / reset / détail) que le scope strict interdisait → 3 options soumises à l'utilisateur → **décision : « laisser `/players` tel quel »**. Aucun changement de code ni de test ; rendu des 3 cas (roster 1 joueur, sans liste, sans agent) vérifié par harnais jetable non conservé. `:control-panel:test` 66/0 échec (dont `BusinessPagesTest` 9/9). Rapport + index seulement. Branche `feat/control-panel-admin-tools`, non fusionnée, non déployée. | DONE | [2026-09-08_0726_page-joueurs-etat-constate.md](2026-09-08_0726_page-joueurs-etat-constate.md) |
| 2026-09-08 | Déploiement AWS de la branche `feat/control-panel-admin-tools` @ `dce3bf3` sur `https://plugadmin.lodylands.com` via `scripts/plugadmin/deploy.sh` (script existant, sans argument). Pré-vol : branche/commit OK, arbre Git propre, `:control-panel:test` 66/0 échec + `:control-panel:build` vert, baseline des autres sites relevée. Déroulé : `installDist` → sauvegarde `/opt/plugadmin/app` sous `releases/20260908-073415` → swap → `systemctl restart plugadmin` (`active`, `NRestarts=0`, PID 26613, `event=panel_started port=8090`). Vérifs : `/health` local + public `ONLINE` (stable ×3) ; `/` 303, `/login` 200, `/dashboard` anon 303, `/players` anon 303 → `/login` (route active, plus un placeholder) ; distribution déployée **byte-identique** au build de `dce3bf3` (jar sha256 `90cf7b57…`), `panel.js` avec durcissement #65 ; `nginx -t` OK, `sites-enabled` inchangé, `dig`/`lodylands`/`www.lodylands` toujours 200 ; secrets + unit systemd + TLS non touchés ; **aucune action VeryGames**, aucun JAR RPGQuest. Rollback : `scripts/plugadmin/rollback.sh app`. Validation authentifiée navigateur (login owner → `/dashboard`, `/players`, menu sans « Joueurs — à venir ») = `PENDING MANUAL VALIDATION`. Pas de merge. | DONE | [2026-09-08_0735_deploy-aws-branche-control-panel-admin-tools.md](2026-09-08_0735_deploy-aws-branche-control-panel-admin-tools.md) |
| 2026-09-08 | Déploiement **VeryGames DEV** du JAR RPGQuest de `feat/control-panel-admin-tools` @ `3c3ea5a` (contenu agent = `e1ddb8c`) pour que l'agent reconnaisse les 15 nouveaux types d'action déjà servis par le Control Panel (`player.list`, `quest.list`, … `player.variable.set`). Pré-vol : branche/commit OK, arbre propre, `AgentActionType` contient les 15 wire-names, tests ciblés `web.agent.*` **45/45** (`--rerun-tasks`), `deploy-verygames.sh --check` OK. Déploiement : `scripts/deploy-verygames.sh -y` (JAR seul) → `./gradlew test`+`build` OK, backup auto `rpgquest-20260908T075204Z-predeploy.jar` (sha256 `2d648c0b…`), transfert FTP atomique du JAR **sha256 `cd66574c…`** (1 256 923 o), aucun autre fichier ; puis `scripts/verygames-restart.sh` (`save-all` → `stop` RCON → relance auto → ONLINE < 1 min). Vérifs : `/plugins` RPGQuest **en vert**, heartbeat agent reçu par PlugAdmin (`server_state=ONLINE`, mondes `hub`/`claims`/`wild` `loaded:true`), aucun ERROR `journalctl -u plugadmin`. **Validation canal agent** : `player.list` → **SUCCESS** « 0 joueur(s) », `quest.list` → **SUCCESS** « 10 quête(s) » — plus de `REJECTED` « type non whitelisté ». Aucune donnée touchée, nginx/AWS non touchés, aucune migration DB, aucune action destructive, pas de merge. Rollback : `scripts/rollback-verygames.sh --latest`. Scan ERROR console VeryGames = `PENDING MANUAL VALIDATION`. | DONE | [2026-09-08_0756_deploy-verygames-actions-agent.md](2026-09-08_0756_deploy-verygames-actions-agent.md) |
| 2026-09-08 | Bug d'affichage `/stories` : `story.list` renvoie `SUCCESS` (« 2 story(s) chargée(s) ») mais le catalogue affiche « Aucun catalogue chargé. ». **Cause racine** : `AgentPages.latestDetails` faisait `store.latestActionOfType(type)` (dernière action du type, tous statuts) `.filter(SUCCESS)` → dès qu'une action `story.list` plus récente est non terminale (PENDING pendant que l'agent répond) ou en échec, le dernier `SUCCESS` exploitable est masqué et le catalogue se vide ; chaque clic « Rafraîchir » recrée une PENDING par-dessus. Défaut latent identique sur `/quests` et `/players` (mêmes consommateurs de `latestDetails`). **Correctif** (2 fichiers prod, ~20 lignes, pas de refacto, pas de feature) : `AgentStore.latestSuccessfulActionOfType(agentId, type)` (`… AND status='SUCCESS' ORDER BY created_at DESC LIMIT 1`) + `latestDetails` qui l'utilise (drop du `.filter(SUCCESS)` redondant) ; `latestForPlayer` inchangé. Reproduit puis vérifié corrigé. Tests : `StoriesCatalogTest` (3, nouveau), `AgentStoreTest` +1 → `:control-panel:test` **70/0 échec**, `:control-panel:build` vert. Aucun déploiement, aucun merge. | DONE | [2026-09-08_0815_fix-stories-catalogue-vide.md](2026-09-08_0815_fix-stories-catalogue-vide.md) |
| 2026-09-08 | Déploiement AWS du Control Panel (`feat/control-panel-admin-tools` HEAD `70ae001`, correctif `/stories` `541f549`) via `scripts/plugadmin/deploy.sh` — exit 0 : `installDist` OK, ancienne app `dce3bf3` sauvegardée sous `releases/20260908-082229`, swap, `systemctl restart` → `active`, `NRestarts=0`, `event=panel_started port=8090`. JAR déployé **sha256 `281f26e5…`** == build frais de la branche ; `AgentStore.class` contient `latestSuccessfulActionOfType`. `/health` local + public `ONLINE` (×3). Routes `/stories`/`/quests`/`/players` (anon) → 303 `/login` (actives). **Correctif vérifié contre la base de prod** : sur un instantané read-only de `control-panel.db` avec une `story.list` `PENDING` injectée par-dessus le dernier `SUCCESS`, `latestSuccessfulActionOfType` renvoie le `SUCCESS` (catalogue = 2 stories `main_story` + `story_test`) — « Rafraîchir » ne vide plus le catalogue. `/quests` + `/players` : dernières listes en base = `SUCCESS`, pas de régression (`BusinessPagesTest` 9/9). Autres sites nginx 200, nginx/secrets/unit non touchés. Vue navigateur authentifiée = `PENDING MANUAL VALIDATION`. Rollback : `scripts/plugadmin/rollback.sh app`. **Aucun déploiement VeryGames, aucun JAR RPGQuest, aucun merge.** | DONE | [2026-09-08_0824_deploy-aws-fix-stories.md](2026-09-08_0824_deploy-aws-fix-stories.md) |
| 2026-09-08 | Passe UX/UI du Control Panel — **issue #74** (catalogues Quêtes/Stories lisibles) + passe visuelle commune. `MiniText` (nouveau) : rendu web des textes MiniMessage — sous-ensemble sûr interprété (couleurs nommées/hex → `<span style>`, gras/italique/souligné/barré), tout le reste (gradient/hover/click/lang…) retiré proprement, texte toujours échappé → **jamais de balise brute** ; `prettifyId`/`prettifyTokens` (`AMETHYST_SHARD` → « Amethyst Shard », sans table de traduction). `Ui` (nouveau) : composants réutilisables (pastilles de statut **glyphe + texte** PENDING/DELIVERED/SUCCESS/FAILED/REJECTED/EXPIRED, `.tid` id technique discret, `.badge`, `.empty`, `.table-wrap`). Cartes quête/story restructurées (`.entity-card`) : titre humain d'abord, id en second, objectifs/récompenses normalisés, prérequis et étapes de story référencés par **titre de quête** via le catalogue `quest.list` local (aucun appel agent en plus). Passe visuelle : `.danger-zone` (reset joueur isolé), bannière succès après envoi d'action, nav mobile scrollable, tables responsive, thème sombre conservé. #65 inchangé (panel.js consomme `statusHtml`). Tests : `MiniTextTest` (7), `QuestsCatalogTest` (2), `StoriesCatalogTest`/`AgentActionsRefreshTest` ajustés → `:control-panel:test` **79/0**, `:control-panel:build` vert. Correction `scripts/plugadmin/rollback.sh` (sélection de release par nom, pas mtime) + `deploy.sh` rétention. **Déployé AWS** (`bc044fd`) via `deploy.sh` : service `active`, `/health` ONLINE, agent reconnecté, rendu /quests+/stories vérifié contre la base de prod (0 balise brute) ; autres sites nginx 200 ; rollback → `releases/20260908-085217`. Tickets **#75** (donneur de quête, besoin protocole) et **#76** (noms FR matériaux) créés. Vue navigateur authentifiée = `PENDING MANUAL VALIDATION`. **Aucun déploiement VeryGames, aucun JAR RPGQuest, aucun merge.** | DONE | [2026-09-08_0855_passe-ux-control-panel-74.md](2026-09-08_0855_passe-ux-control-panel-74.md) |
| 2026-09-08 | Lot UX Control Panel — **#77** (récompenses lisibles) + **#76** (noms FR matériaux/mobs) + audit **#75** + copie d'identifiant + cohérence Dashboard/Agents. Audit du protocole : `quest.list` envoie récompenses/objectifs **déjà aplatis en chaînes** ; panel sans dépendance Paper ; donneur de quête absent partout. **#77 MVP panel-only** : `RewardText` parse les formats de `describeRewards` (`+Nx MAT` → « Objet : Épée en diamant ×1 », `variable K=true` → « Débloque : Claim Tier 1 », `customitem give …` → conséquence) ; **valeur brute toujours conservée** en `<code class="tid">` copiable ; inconnu → chaîne telle quelle. **#76 MVP panel-only** : `MinecraftNames` (table FR ~90 entrées + repli `prettifyId`) sur les objectifs. **Copie d'ID** : tout `.tid` `role=button data-copy=<valeur complète>` ; `panel.js` listener délégué clic/clavier + repli `execCommand` + flash ✓. **Dashboard/Agents** : `Ui.liveness` (pastille glyphe+texte), en-têtes nettoyés, table actions avec type humain (`AgentActionCatalog`) + fil copiable ; JSON `typeHtml`/`idFull` consommé par `panel.js`, **polling #65 inchangé** (vérifié DOM headless). Tests : `RewardTextTest` (6), `MinecraftNamesTest` (3), `QuestsCatalogTest`/`AgentActionsRefreshTest`/`PanelAppTest` ajustés → `:control-panel:test` **88/0**, `:control-panel:build` vert. **Déployé AWS** (`6a592c9`, sha256 `01f8d883…`) via `deploy.sh` : service `active`, `/health` ONLINE, agent reconnecté, rendu `/quests` vérifié contre la base de prod (objectifs FR, récompenses lisibles + brut conservé) ; autres sites nginx 200 ; rollback → `releases/20260908-092850`. **#75** commenté (audit, ouvert), **#78** créé (structurer récompenses/objectifs agent-side, requiert déploiement JAR VeryGames), **#76**/**#77** commentés MVP. Vue navigateur authentifiée = `PENDING MANUAL VALIDATION`. **Aucun déploiement VeryGames, aucun JAR RPGQuest, aucun merge.** | DONE | [2026-09-08_0930_lot-ux-control-panel-76-77.md](2026-09-08_0930_lot-ux-control-panel-76-77.md) |
| 2026-09-08 | Protocole `quest.list` **structuré** — issues **#78** (objectifs/récompenses structurés) + **#75** (donneur de quête). Audit : `BukkitAgentActions` aplatissait tout en chaînes (`"Tuer SPIDER (x5)"`, `"commande console : …<tronqué 60>"`), le panel les reparsait par regex. **Plugin** : nouveaux records `ObjectiveSummary {kind,target,amount,raw}` et `RewardSummary {kind,amount,target,value,command,raw}` ; `QuestSummary` gagne `rewardDetails`+`giverId`/`giverName`, `QuestStepSummary` gagne `objectiveDetails` — champs legacy `objectives`/`rewards` **conservés à l'identique** (compat agent déployé). `BukkitAgentActions.objectiveTarget()`/`rewardDetails()` — commande console **non tronquée**. `AgentActionExecutor.questList` sérialise les Map imbriquées + `giverId` (si déclaré). **#75** : `QuestDefinition` gagne un champ optionnel `giver` (nullable) ; `QuestDefinitionParser.parseGiver` (vide si défini = erreur) ; `crystal_hunt`/`premiers_pas`/`woodcutters_request` reçoivent `giver:`. **Panel** : `ObjectiveText` (nouveau — verbe FR par `kind` + nom via `MinecraftNames`/`prettifyId`, sans regex sur phrase), `RewardText.fromSummary` (commande complète), `AgentPages.renderQuestCard` (structure prioritaire, repli legacy, ligne « Donneur »). Tests : `QuestListPayloadTest` (nouveau — JSON réel de `quest.list`), `ObjectiveTextTest` (nouveau), `RewardTextTest` (+5), `QuestsCatalogTest` (+structuré), `AuthenticatedSmokeTest` (nouveau — login owner → `/dashboard` `/players` `/quests` `/stories` rendus) → `:test` **1086/0**, `:control-panel:test` **103/0**, `./gradlew build` vert. **Déployé AWS** (`02642e9`, jar sha256 `6493fc15…`) via `deploy.sh` : `/health` ONLINE ×3, routes protégées → 303, autres sites 200, rollback → `releases/20260908-105940`. **Déployé VeryGames DEV** : JAR sha256 `4fbaa345…` (`deploy-verygames.sh -y` + `verygames-restart.sh`, backup `…110044Z-predeploy.jar`) + `crystal_hunt.yml` via `--also` (backup `extra-20260908T110340Z/`) + RCON `quest admin reload` (10 quêtes, 0 erreur). **Validation `quest.list` réelle** (action injectée dans `control-panel.db`, exécutée par l'agent DEV) → **SUCCESS** : `objectiveDetails` (kill/collect/craft/talk) + `rewardDetails` (XP/commande complète/variable) présents, `crystal_hunt.giverId=="guard"`, `first_steps` sans `giverId`, legacy en parallèle. **#78 fermée**. **#75** : implémentée + rendue + validée live, vue navigateur authentifiée = `PENDING MANUAL VALIDATION` → laissée ouverte. Rollback VeryGames : `rollback-verygames.sh --latest` (+ `--also` pour le YAML). **Aucun merge.** | DONE | [2026-09-08_1100_quest-list-structure-78-75.md](2026-09-08_1100_quest-list-structure-78-75.md) |
| 2026-09-08 | Action agent **`npc.list`** + page Control Panel **`/npcs`** (V1, lecture) — issues **#66** / **#75**. Audit : liaison PNJ Citizens ↔ id RPGQuest = table `npc_citizens_bindings` (`NpcBindingRepository`) ; dialogue ↔ PNJ = **convention** `dialogueId == rpgquest:<npcId>` ; quête ↔ PNJ = `QuestDefinition.giver()` (#75) + `TalkToNpcObjective.npcId()` ; tag d'entité vanilla = PDC hors base (scan monde → **hors V1**) ; `/rpgadmin npc` n'a que `tag\|untag\|info` (aucune liste). **Plugin** : `NpcCatalog` (pur, testable sans Bukkit) croise ces sources → `NpcRow` {id, displayName (speaker du dialogue), citizensNumericId, bound, hasDialogue, dialogue nœuds/choix, dialogueStartsQuests, questsGiven, questsReferenced, sources, warnings} + `canonicalIds` (ids attendus par dialogues+quêtes, **jamais** les tags) + compteurs. Anomalies : `DUPLICATE_BINDING` (err), `QUEST_REF_NO_NPC` (warn, dégradé info si Citizens inactif), `TAGGED_UNUSED` (info, + suggestion d'id canonique proche par Levenshtein ≤ 2 → **`garde`→`guard`**, cas #66), `DIALOGUE_NO_NPC`, `GIVER_NO_DIALOGUE`. Nouvelle action `AgentActionType.NPC_LIST` ↔ `AgentActionCatalog` (`Permission.NPC_READ`), lecture seule ; `BukkitAgentActions` reçoit `dialogueEngine`/`npcIdentityService`/`npcBindingRepository` (wiring bootstrap) ; `AgentActionExecutor.npcList()` sérialise ; **aucun `onMain`, aucune lecture du monde**. **Panel** : entrée « PNJ » (`/npcs`) activée ; `AgentPages.npcs()` + `renderNpcCard()` réutilisent `entity-card`/`MiniText`/`Ui` (id copiables, `Ui.severity()` nouvelle), cartes triées anomalies-d'abord, bloc `<details>` « IDs canoniques connus » (préparation #66). #66 **reste séparée** (câblage `/rpgadmin npc tag` = chantier à part + validation manuelle). Tests : `NpcCatalogTest` (9, pur), `web.agent.NpcListPayloadTest` (3, JSON réel), `AgentActionExecutorTest` +1, panel `NpcsCatalogTest` (2), `AuthenticatedSmokeTest`/`BusinessPagesTest` étendus `/npcs` → `:test` **1099/0**, `:control-panel:test` **105/0**, `./gradlew build` vert. **Déployé AWS** (`9fa57d1`, jar sha256 `2bf90a25…`) via `deploy.sh` : `/health` ONLINE ×3, `/npcs` anon → 303, autres sites 200, rollback → `releases/20260908-114604`. **Déployé VeryGames DEV** : JAR sha256 `12786cb3…` (`deploy-verygames.sh -y` + `verygames-restart.sh`, backup `…114637Z-predeploy.jar`). **Validation `npc.list` réelle** (action injectée dans `control-panel.db`, exécutée par l'agent DEV) → **SUCCESS** ; `details.npcs` peuplé, `canonicalIds` + warnings cohérents. Vue navigateur authentifiée = `PENDING MANUAL VALIDATION`. **Aucun merge.** | DONE | [2026-09-08_1147_npc-list-page-npcs-v1.md](2026-09-08_1147_npc-list-page-npcs-v1.md) |
| 2026-09-08 | **Système PNJ V2 déclarative** — définition logique + écritures de contenu (issues **#66** / **#75**). Audit : `NpcCatalog` V1 dérivait `canonicalIds` d'une union de références, aucune notion de définition ; pas de couche d'écriture YAML non destructive (`YamlConfiguration.save()` perd les commentaires). **Plugin** (`com.lodygames.rpgquest.npc`) : `NpcDefinition` `{id, displayName, description?, dialogueId?, role?, enabled}` (sans Bukkit, id non renommable) + `NpcDefinitionParser`/`Loader`/`NpcLoadReport` + `YamlNpcEngine` (`PluginService`, `npcs/*.yml`, exemple `guard.yml` livré, `reload()` hors thread principal). `NpcDefinitionYaml.render` (déterministe, re-parsable) + `NpcDefinitionStore` (create/update atomique, **refuse l'écrasement**, jamais de chemin fourni). `QuestGiverEditor` (transformation **texte pure** : pose/replace la ligne racine `giver:`, commentaires/format/CRLF préservés, idempotent) + `QuestGiverStore` (localise le fichier de quête par son `id:`). `NpcCatalog` V2 : `logicalDefinitionPresent` vs `citizensBindingPresent`, `state` (`LINKED`/`NOT_LINKED`/`DISABLED`/`CITIZENS_ORPHAN`/`UNDEFINED_REFERENCE`/`BROKEN`), `definedIds` = source canonique ; anomalies `NO_DEFINITION`/`BINDING_NO_DEFINITION`/`DIALOGUE_MISSING` (err), `NOT_LINKED`/`DISABLED`/`GIVER_NO_DIALOGUE` (info). **Protocole** : `npc.list` enrichi ; nouvelles mutations `npc.definition.create`/`npc.definition.update` (`NpcDefinitionStore`) + `quest.giver.set` (`QuestGiverEditor` + `reloadQuestDefinitions` sur le thread principal) — whitelistées (`AgentActionType`↔`AgentActionCatalog`), permission dédiée (`NPC_WRITE`/`QUEST_GIVER_WRITE`), `confirm` obligatoire, auditées, validation 3 couches, **jamais de YAML brut ni de chemin arbitraire**. IO sur le thread poll async (conforme CLAUDE.md). **Panel `/npcs`** : deux blocs (Définition RPGQuest / Binding Citizens), formulaire de création (pré-rempli sur carte non définie), édition limitée (`display_name`/`dialogue`/`role`/`enabled` — jamais l'id), attribution de quête (select + confirm), registre canonique « Définis » vs « Tous ». `AgentPages` reçoit `PermissionService`. **Hors scope assumé** : spawn/binding Citizens web, éditeur de dialogues, câblage `/rpgadmin npc tag` (#66), suppression de définition, enrichissement live. Tests : `NpcDefinitionParser/Loader/Yaml/Store`, `QuestGiverEditor/Store`, `NpcCatalogTest`+`NpcListPayloadTest` réécrits V2, `AgentActionExecutorTest` +2, panel `NpcsCatalogTest` réécrit → `:test` **1127/0**, `:control-panel:test` **107/0**, `./gradlew build` vert. **Déployé AWS** (`1340dda`, jar sha256 `01f14c60…`) via `deploy.sh` : `/health` ONLINE ×3, `/npcs` anon → 303, autres sites 200, rollback → `releases/20260908-123552`. **VeryGames DEV : NON déployé** — panne FTP `si-16041.dg.vg` (`curl (28) timed out`) au moment de la tâche ; le script abandonne avant toute écriture, serveur DEV inchangé. À rejouer (`deploy-verygames.sh -y` + `verygames-restart.sh`) dès que le FTP répond ; validation `npc.list`/`npc.definition.create`/`quest.giver.set` = `PENDING`. Vue navigateur authentifiée = `PENDING MANUAL VALIDATION`. **Aucun ticket fermé, aucun merge.** | DONE | [2026-09-08_1235_npc-v2-declarative.md](2026-09-08_1235_npc-v2-declarative.md) |
| 2026-09-08 | **Lier une définition PNJ à un PNJ Citizens existant** — issue **#81, phase 1** uniquement (spawn Citizens = phase 2, non traité). Audit : `CitizensNpcBridge` isole tous les imports `net.citizensnpcs.*` mais n'énumérait pas le registre ; `NpcBindingRepository` n'offrait qu'un `UPSERT ON CONFLICT(citizens_uuid) DO UPDATE` (rebind silencieux, inadapté) ; `NpcIdentityService` avait le cache `citizensCache` mais aucune écriture ; schéma `npc_citizens_bindings` (migration V12) réutilisé **sans changement**. **Plugin** : `CitizensNpc` (record public top-level `{numericId,uuid,name,spawned}`) ; `CitizensBindPlanner` (pur, sans Bukkit/SQL → `Plan{INSERT|NOOP|REJECT, code, message}` : idempotence `NOOP`, `CITIZENS_TAKEN`, `NPC_ID_TAKEN`) ; `CitizensNpcBridge.roster()`/`byNumericId()` (seuls points touchant `CitizensAPI.getNPCRegistry()`) ; `NpcBindingRepository.insertIfAbsent()` (`INSERT OR IGNORE`, renvoie `boolean`, non destructif — l'`UPSERT` de rebind reste inutilisé) ; `NpcIdentityService.citizensRoster()`/`citizensByNumericId()`/`bindCitizens()` (+ record `BindResult`, met à jour `citizensCache` sur `INSERT` → identification en jeu effective **sans redémarrage**). **Protocole** : `npc.citizens.list` (lecture, `NPC_READ` — `loadAll()` async → `onMain` registre → fusion ; payload `{numericId,uuid,name,linkedNpcId?,availableForBinding,spawned}` + compteurs, **aucune position/monde**) ; `npc.citizens.link` (mutation whitelistée `AgentActionType`↔`AgentActionCatalog`, **permission dédiée `NPC_BIND_WRITE`**, `confirm` obligatoire, audit, validation 3 couches — `npc_id` motif `[a-z0-9._-]{1,64}`, `citizens_id` entier 1..10 000 000, jamais de SQL/UUID libre/commande console). Refus tôt si définition absente/`DISABLED`/Citizens inactif/`citizens_id` introuvable ; sinon `bindCitizens` async. **Orphelins** (`guide/help/jeff/jo/junior/libraire` = Citizens sans définition) : **aucune mutation ajoutée** — `NpcCatalog.build` croise déjà binding+définition par id, créer `npcs/<id>.yml` fait passer en `LINKED` au prochain `npc.list`. **Panel `/npcs`** : bouton « Rafraîchir les PNJ Citizens », résumé (`total`/`available`/`linked`), `citizensLinkForm()` (`select` — Citizens occupés `disabled`, `confirmBox` obligatoire) affiché **seulement** sur carte définition OUI + binding NON + `enabled` ; carte `LINKED` → `#N — Nom` sans bouton + mention « procédure de rebind ajoutée ultérieurement ». Future `npc.citizens.create` (phase 2) documentée, **non codée**. Tests : `CitizensBindPlannerTest` (5), `NpcIdentityServiceTest#bindCitizens*` (4), `NpcBindingRepositoryTest` (+1), `NpcCitizensPayloadTest` (3), `AgentActionExecutorTest` (+2), panel `NpcsCatalogTest` (réécrit, `woodcutter_bob` devient `NOT_LINKED` + flux de liaison) → `:test` **1142/0**, `:control-panel:test` **109/0**, `./gradlew build` vert. **Déployé AWS** (`3d1f50c`, jar sha256 `a52ad4d0…`) via `deploy.sh` : release `20260908-131859`, `/health` ONLINE, autres sites 200. **Déployé VeryGames DEV** : JAR sha256 `342bf808…` (`deploy-verygames.sh -y` + `verygames-restart.sh`, backup `…131925Z-predeploy.jar`) ; RCON `plugins` RPGQuest+Citizens verts, heartbeat `13:24:04Z` ONLINE, aucun ERROR `journalctl`. **Validation `npc.citizens.list` réelle** (action injectée dans `control-panel.db`) → **SUCCESS** : 7 Citizens `#0..#6`, tous **déjà liés** (`available:0`), payload sans position/monde. `npc.list` toujours **SUCCESS**. **`npc.citizens.link` — 4 cas live** (aucune liaison créée) : `woodcutter_bob`→#6 (pris) = FAILED `CITIZENS_TAKEN` ; `guard`→#6 (identique) = SUCCESS `NOOP` ; `does_not_exist_xyz`→#6 = FAILED `UNKNOWN_NPC` ; `woodcutter_bob`→#9999 = FAILED `UNKNOWN_CITIZENS`. **Chemin nominal `LINKED` non exerçable en direct** (aucun Citizens libre sur DEV, consigne de ne pas en créer / ne pas détourner) → couvert par les tests. Aucun PNJ Citizens créé/supprimé, aucune progression joueur touchée, aucune migration. Rollback : `rollback-verygames.sh --latest` / `plugadmin/rollback.sh app`. Vue navigateur authentifiée = `PENDING MANUAL VALIDATION`. **#81 non fermée, aucun merge.** | DONE | [2026-09-08_1325_npc-citizens-link-81.md](2026-09-08_1325_npc-citizens-link-81.md) |
| 2026-09-08 | **Email de fin de tâche #81 phase 1 non reçu** — diagnostic + renvoi + durcissement SMTP générique. Chaîne testée avec transcript `smtplib` complet (`set_debuglevel(2)`) : cette machine → relais `send.one.com` **fonctionne** — `AUTH` 235, `MAIL FROM`/`RCPT TO` 250, `DATA` → **`250 Ok: queued as 0552c5e2-ab8b-11f1-8ba2-fd5ae24138eb`**, 0 destinataire refusé, exit 0. Le 1er envoi (~13:29) avait donc aussi été mis en file. Non-livraison **en aval du relais one.com** (one.com→Gmail ou filtrage Gmail) — **hors de portée** depuis ce serveur (pas de MTA local, pas de `/var/log/mail*`). Cause déterministe la plus probable : les scripts d'envoi ad-hoc (et `smtplib.send_message`) partaient **sans en-tête `Date` ni `Message-ID`** → Gmail supprime/classe indésirable silencieusement. **Renvoyé 2×** vers `lloyd.helpdesk@gmail.com` (destinataire habituel), rapport `2026-09-08_1325_npc-citizens-link-81.md` joint : (a) `scratchpad/resend_81.py` avec en-têtes corrigés → queued `0552c5e2…`, exit 0 ; (b) via la procédure générique corrigée → `Message-ID <1788875057…@lodywood.be>`, 0 refusé, session SMTP 250, exit 0. **`scripts/plugadmin/send-mail.py` durci** (outillage, aucun code applicatif) : ajoute **toujours** `Date` + `Message-ID` ; nouvelle option **`--attach PATH` répétable** (MIME deviné : `.md`→`text/markdown`…) ; exit 3 si destinataire refusé ; sortie de contrôle (destinataire, sujet, message-id, pièces, refusés, code SMTP). Rétro-compatible, aucun appelant automatisé. `py_compile` OK. **Aucun déploiement, aucun merge, aucune suite Gradle** (rien de fonctionnel touché). | DONE | [2026-09-08_1344_smtp-rapport-81-non-recu.md](2026-09-08_1344_smtp-rapport-81-non-recu.md) |
| 2026-09-08 | **Spawn d'un PNJ Citizens depuis une définition** — issue **#81 phase 2**. Audit API Citizens (`createNPC(EntityType,name)` + `NPC.spawn(loc, CREATE)` ; `NPC.destroy()` pour rollback ; `getById`/`getByUniqueId` pour vérifier le registre — aucune commande console). Nouvelle action agent **`npc.citizens.create`** (mutation whitelistée `AgentActionType` ↔ `AgentActionCatalog`, **permission dédiée `NPC_SPAWN_WRITE`**, `confirm` obligatoire, audit, validation 3 couches) : crée physiquement un PNJ Citizens (`EntityType.PLAYER`, nom = `NpcDefinition.displayName`) depuis une définition puis le lie. Paramètres **métier uniquement** : `npc_id` / `world` / `x`/`y`/`z` / `yaw`?/`pitch`? — jamais de nom libre / UUID / commande / YAML / chemin. **`CitizensSpawnPlanner`** (pur, sans Bukkit) : toutes les préconditions logiques avant création — Citizens actif (`CITIZENS_UNAVAILABLE`), définition présente (`UNKNOWN_NPC`) + `enabled` (`NPC_DISABLED`), `npc_id` pas déjà lié (`NPC_ALREADY_LINKED`), monde de la **liste blanche RPGQuest** (`hub`/`claims`/`travel.wildWorld` de la config — `UNKNOWN_WORLD`), position **finie et bornée** (`|x|,|z| ≤ 29 999 984`, `-2048 ≤ y ≤ 2048`, `-90 ≤ pitch ≤ 90` — `INVALID_POSITION`, jamais « corrigée »). Puis thread principal : monde **chargé** + `y` dans `[minHeight, maxHeight)`. **`CitizensSpawnCoordinator`** (pur, `Spawner`+`Binder`) : `create → bind → success`, sinon **rollback du seul PNJ créé** (double clé `numericId`+`uuid`, jamais un préexistant) → `BIND_FAILED_ROLLED_BACK` (`rolled_back`). Persistance via `NpcIdentityService.bindCitizens` (réutilise `CitizensBindPlanner` + `insertIfAbsent` + cache → effet en jeu sans redémarrage). Threading : préconditions pures, `loadAll` async, registre Citizens + monde `onMain`, persistance async. Résultat structuré `{code, npc_id, citizens_id, world, rolled_back, effects}`. **Panel `/npcs`** : bloc « Créer le PNJ Citizens » sur les cartes définies `NOT_LINKED` `enabled` — preview (id, nom, « aucun Citizens lié »), monde en **liste déroulante** (mondes chargés du heartbeat, champ libre en repli), X/Y/Z/yaw/pitch à saisir (**jamais devinés**), `confirm` obligatoire ; jamais sur une carte `LINKED`. `Permission.NPC_SPAWN_WRITE`. Tests : `CitizensSpawnPlannerTest` (14), `CitizensSpawnCoordinatorTest` (4), `AgentActionExecutorTest` +2, `NpcCitizensPayloadTest` +3, `NpcsCatalogTest` +2 → `:test` **1165/0** (29 ignorés), `:control-panel:test` **111/0**, `./gradlew build` vert. **Déployé AWS** (`b9f3beb`, jar sha256 `bff44c5b…`) via `deploy.sh`. **Déployé VeryGames DEV** (jar sha256 `6a7ff0d4…`, `deploy-verygames.sh -y` + `verygames-restart.sh`, backup `…144029Z-predeploy.jar`) — la **1re passe a révélé un NPE** (`citizens_id` null dans les détails d'outcome + `Map.copyOf`), **corrigé** (`citizens_id` → `-1`) puis redéployé. **Validation live** (actions injectées) : `npc.list` / `npc.citizens.list` **SUCCESS** ; `npc.citizens.create` **sans spawn réel** → refus `NPC_ALREADY_LINKED` / `UNKNOWN_WORLD` / `INVALID_POSITION` / `UNKNOWN_NPC` (`citizens_id: -1`) + `REJECTED` sur `x=NaN`. Chemin `CREATED` = tests seulement. Premier spawn réel = `PENDING MANUAL VALIDATION`. #81 non fermée, aucun merge. | DONE | [2026-09-08_1423_npc-citizens-create-81-phase2.md](2026-09-08_1423_npc-citizens-create-81-phase2.md) |
| 2026-09-08 | **Page `/dialogues` V1** — lecture structurée des dialogues à embranchements + bases d'un futur éditeur. Audit du modèle : `DialogueDefinition` **lève** si nœuds vides / `start` invalide / `start` absent → « dialogue sans nœud » et « `start` inexistant » sont des **erreurs de chargement**, pas des warnings sur une définition ; `DialogueChoice.next` = redirection intra-dialogue ; actions `sealed` (10 : `START_QUEST` / `ADVANCE_QUEST` / `TURN_IN_QUEST` / `GIVE_ITEM` / `TAKE_ITEM` / `SET_VARIABLE` / `RUN_SAFE_COMMAND` / `OPEN_DIALOGUE` / `OPEN_MERCHANT` / `CLOSE`), conditions `sealed` (8 + `NegatedCondition`) ; `DialogueLoader` valide en 2ᵉ passe id dupliqué / `OPEN_DIALOGUE` manquant / **cycles `OPEN_DIALOGUE`** ; **ouverture en jeu TOUJOURS par convention `rpgquest:<npcId>`** (identité stable), jamais via `NpcDefinition.dialogue` (diag seulement). **Plugin** : `DialogueCatalog` (pur, sans Bukkit) — par dialogue : `id`, `startNodeId`, `linkedNpcIds`, compteurs, `referencedQuestIds` / `startsQuestIds`, `nodes[]` ordonnés (départ d'abord) avec `reachable` (BFS des `next`), choix avec **actions et conditions typées** `{kind, target, value, raw}` (`NegatedCondition` déplié) ; warnings `NODE_UNREACHABLE` / `QUEST_REF_UNKNOWN` / `DIALOGUE_NO_NPC` / `MULTIPLE_NPCS` / `DEFINITION_DIALOGUE_DIVERGES` / `NEXT_MISSING` ; `loadIssues[]` **à part** (fichiers rejetés) + `declaredButMissing[]`. `DialogueDraft` (modèle éditable minimal) + `DialogueDefinitionYaml.render` (déterministe re-parsable) + `DialogueDefinitionStore.create` (atomique tmp+`ATOMIC_MOVE`, **refus d'écrasement** `EXISTS`, rechargement complet du dossier — fichier supprimé si le nouveau dialogue ne recharge pas). **Protocole** : `dialogue.list` (lecture, permission dédiée `DIALOGUE_READ`) ; `dialogue.definition.create` (mutation, permission dédiée `DIALOGUE_WRITE`, `confirm`, audit — squelette `dialogues/<key>.yml` : 1 nœud `start` + 1 choix `CLOSE` ; jamais de YAML brut ni de chemin ; `reload()` après succès). **Panel** : page `/dialogues` activée (nav + route) — cartes avec graphe lisible (nœud de départ mis en avant, nœuds inaccessibles marqués, MiniMessage rendu via `MiniText`, ids copiables), bannières `loadIssues` + `declaredButMissing`, formulaire de création de squelette ; `dialogueEffectLabel()` rend chaque action/condition en libellé lisible (titre humain de quête via `quest.list` local). **Hors scope V1** (futur éditeur documenté dans le rapport) : `dialogue.node.create/update`, `dialogue.choice.add/update/delete`, édition texte préservant commentaires/structure (couche AST), réordonnancement, builder graphique. Tests : `DialogueCatalogTest` (10), `DialogueDefinitionStoreTest` (3), `AgentActionExecutorTest` +2, `DialoguesCatalogTest` (3), smoke tests étendus → `:test` **1179/0** (29 ignorés), `:control-panel:test` **114/0**, `./gradlew build` vert. **Déployé AWS** (`1fa7f4a`, jar sha256 `3c873e53…`) via `deploy.sh` : `/health` ONLINE, `/dialogues` anon → 303, autres sites 200. **Déployé VeryGames DEV** (jar sha256 `57bda61b…`, `deploy-verygames.sh -y` + `verygames-restart.sh`, backup `…151640Z-predeploy.jar`). **Validation `dialogue.list` réelle** (action injectée) → **SUCCESS** : 7 dialogues (`guide`/`help`/`jeff`/`jo`/`guard`/`junior`/`libraire`), 22 nœuds, actions/conditions typées présentes, `guard`/`junior`/`libraire` liés + `guide`/`help`/`jeff`/`jo` → `DIALOGUE_NO_NPC` (PNJ Citizens sans définition, cohérent #81 p1), **14 `loadIssues`** = 7 fixtures `test_*.yml` préexistantes correctement rejetées (page rendable). `npc.list` toujours SUCCESS. `dialogue.definition.create` **non exécuté en réel** (pas de contenu ajouté à DEV) — couvert par les tests. Vue navigateur authentifiée = `PENDING MANUAL VALIDATION`. Aucun ticket fermé, aucun merge. | DONE | [2026-09-08_1510_dialogues-page-v1.md](2026-09-08_1510_dialogues-page-v1.md) |
| 2026-09-08 | **Nettoyage des fixtures `test_*.yml` égarées dans `plugins/RPGQuest/dialogues/` — VeryGames DEV (issue #83).** La validation `dialogue.list` de la V1 `/dialogues` avait relevé **14 `loadIssues`** (« `start` obligatoire » + « `nodes` obligatoire ») provenant de **7 fichiers `test_*.yml`** posés dans le dossier actif du serveur DEV. **Audit** : ce sont des **fixtures de quête de test manuel** (id `rpgquest:test_<objectif>`, structure `steps`/`objectives`/`rewards`, en-tête « QUÊTE DE TEST MANUEL »), déjà **versionnées** à leur place légitime `docs/manual-tests/quests/test_*.yml` — emplacement **non chargé** par `YamlDialogueEngine`. Téléchargées depuis DEV puis comparées octet à octet aux fixtures du dépôt → **identiques 7/7** : copies exactes déposées dans le **mauvais dossier** (`dialogues/` au lieu de `quests/`). Aucune référence du dépôt (tests, scripts, docs, `story_test.yml`, `StoriesCatalogTest`) ne les attend dans `dialogues/` ; aucun rôle gameplay actif. `RPGQuest/quests/` contient bien 3 fixtures `test_*.yml` mais **légitimement** (scénario `story_test`, dossier lu par le moteur de quêtes) → **laissées en place**. **Décision : aucune modification du dépôt** — les fixtures ont déjà le bon emplacement versionné, pas de nouvelle convention. **DEV** : backup FTPS des 7 fichiers vers `~/.local/share/rpgquest/verygames-backups/issue-83-dialogues-20260908T154228Z/` (tailles + SHA-256 enregistrés), puis **7 × `DELE`** de `RPGQuest/dialogues/test_*.yml` (garde-fou « nom `test_*.yml` »), **aucun autre fichier touché** (7 dialogues gameplay, `quests/`, `data.db`, `config.yml`, mondes, `Citizens/` intacts). Pas de reload à chaud du dossier `dialogues/` (`/rpgquest reload` = `config.yml` seul) → **redémarrage propre** `verygames-restart.sh` (0 joueur, aucune progression touchée). **Aucun redéploiement JAR** (aucun code modifié). **Validation réelle** (actions injectées dans `control-panel.db`, exécutées par l'agent DEV) : `dialogue.list` → **SUCCESS** « 7 dialogue(s) (4 avec avertissement, **0 fichier(s) rejeté(s)**) », `loadIssues` **0** (était 14), 7 dialogues gameplay présents (`guide`/`help`/`jeff`/`jo`/`guard`/`junior`/`libraire`), `nodeTotal=22` inchangé, 4 × `DIALOGUE_NO_NPC` **préexistants** (PNJ Citizens sans `NpcDefinition`, cohérent #81 p1), **aucun nouveau warning** ; `npc.list` → **SUCCESS** (8 PNJ) inchangé ; heartbeat agent `ONLINE` ; **0 `ERROR`** dans `journalctl -u plugadmin`. Loader **inchangé** — aucun filtre `test_*`, aucun `loadIssue` masqué. Vue navigateur `/dialogues` authentifiée = `PENDING MANUAL VALIDATION`. **#83 non fermée, aucun merge.** | DONE | [2026-09-08_1547_nettoyage-fixtures-test-dialogues-dev-83.md](2026-09-08_1547_nettoyage-fixtures-test-dialogues-dev-83.md) |
| 2026-09-08 | **Éditeur guidé `/dialogues` — phase 1 de #82 : édition simple et sûre + refonte UX.** Audit : `DialogueDraft` est volontairement pauvre (pas de conditions ni d'actions riches) → un aller-retour par ce modèle **détruirait** les `START_QUEST` / `QUEST_STATE` / `RUN_SAFE_COMMAND` d'un dialogue gameplay. **Décision** : écriture « charger → muter le modèle métier → re-sérialiser le dialogue **complet** → re-parser → sauver ». **Plugin** : `DialogueDefinitionWriter` (nouveau, pur — sérialiseur fidèle et déterministe des 10 actions + 8 conditions + négation, re-parsable à l'identique) ; `DialogueDefinitionEditor` (nouveau, pur IO+parsing) — localise le fichier par `id`, **refuse un fichier déjà invalide** (`SOURCE_INVALID`), applique la mutation, **garde-fou round-trip** (`reparsed.equals(muté)` sinon `ROUNDTRIP`, rien écrit), **écriture atomique**, **rechargement** puis **restauration du contenu d'origine** si échec (`RELOAD_FAILED`). 5 mutations : `dialogue.node.update` (locuteur/texte, choix conservés), `dialogue.node.create` (nœud simple orphelin), `dialogue.choice.add` / `.update` / `.delete` — **choix simple** seulement (ni condition ni action hors `CLOSE` ; `next` vers un nœud existant **xor** fermeture ; jamais le dernier choix ; `UNSAFE_CHOICE` sur un choix riche). `AgentActionType`↔`AgentActionCatalog` miroir, permission dédiée `DIALOGUE_WRITE`, `confirm` obligatoire, audit, 3 couches de validation (panel/agent/service — `dialogue_id` normalisé, `node_id` borné, `text` ≤ 512 mono-ligne, `choice_index` 0..199, `next` xor `close`). IO + re-parse **sur le thread de poll async**, jamais le thread principal ; `YamlDialogueEngine.reload()` après succès. Renommage/suppression de nœud, réordonnancement, édition des actions/conditions riches = **hors périmètre phase 1** (documenté). Fichier édité au **format canonique** (commentaires/mise en forme d'origine non conservés — choix assumé). **Control Panel** : `/dialogues` refondue en 4 blocs (en-tête identité+**pastille d'état**, résumé départ/PNJ/quêtes, **diagnostics triés erreur→attention→info** message humain puis code discret, graphe de **cartes nœud** départ accentué / inaccessible marqué / actions-conditions en éléments secondaires). Édition **semi-inline** (`<details>` par nœud, **aucun JS**, conforme CSP) : « Modifier ce nœud », « Ajouter un choix » (cible = `<select>` des nœuds du dialogue), « Modifier / supprimer » sous chaque choix simple, « Ajouter un nœud » sous la carte ; un choix riche affiche « édition prévue dans une phase ultérieure ». Bandeau format canonique. Tests : `DialogueDefinitionEditorTest` (14, nouveau), `AgentActionExecutorTest` +5, `AgentActionCatalogTest` +5, `DialoguesCatalogTest` +3 → `:test` **1197/0** (29 ignorés), `:control-panel:test` **121/0**, `./gradlew build` vert. Docs : `AGENT.md` (5 lignes + section « éditeur guidé »), `RPGQUEST_BIBLE.md` §5, `current_state.md`, `control-panel/ROADMAP.md`, `SERVER_CHANGELOG.md` (+ « Exécution réelle » à compléter). **Déploiement AWS + VeryGames DEV : à réaliser** (le code agent change) ; vue navigateur authentifiée + cycle d'édition sur dialogue de test = `PENDING MANUAL VALIDATION`. **#82 non fermée, aucun merge.** | DONE | [2026-09-08_1642_dialogues-editeur-guide-phase1-82.md](2026-09-08_1642_dialogues-editeur-guide-phase1-82.md) |
| 2026-09-08 | **Vérification : l'agent VeryGames DEV reconnaît déjà les 15 nouvelles actions du Control Panel** (`player.list`, `quest.list`, `quest.player.status`, `story.list`, `story.player.status`, `item.list`, `player.resetnew.preview/confirm`, `player.item.give`, `quest.start/complete/reset`, `story.advance/complete`, `player.variable.set`). Demande = déployer le JAR RPGQuest de la branche pour lever des `REJECTED` « type non whitelisté » observés sur `player.list` / `quest.list`. **Constat** : cette observation date d'**avant** le déploiement de l'éditeur guidé `/dialogues` #82 (2026-09-08 ~16:52 UTC, même branche, commit `171849b`), qui a déjà posé le JAR contenant toute l'énumération `AgentActionType`. Vérifs : branche `feat/control-panel-admin-tools` @ `70cbeda` (arbre propre, delta vs `171849b` = `docs/` seul → JAR inchangé) ; les 29 wire-names (dont les 15 demandés) présents dans `AgentActionType` **source HEAD** ET dans `AgentActionType.class` extrait du **JAR distant** ; **SHA-256 identiques** entre `build/libs/rpgquest-0.1.0-SNAPSHOT.jar` local et le JAR VeryGames (`12f66391…`, 1 427 111 o) ; DEV **ONLINE** depuis ~95 min (heartbeat `ONLINE`, `/plugins` RPGQuest+Citizens+WorldEdit+Multiverse verts, `rpgquest version` OK, `journalctl -u plugadmin` **0 ERROR** depuis 17:00). Tests : `./gradlew :test --tests 'com.lodygames.rpgquest.web.agent.*'` **70/0**, `./gradlew build` **SUCCESSFUL**. **Validation distante** (lectures pures via le canal agent) : `player.list` → **SUCCESS** (« 1 joueur »), `quest.list` → **SUCCESS** (« 10 quêtes »), `story.list` → **SUCCESS**, `item.list` → **SUCCESS** — **plus aucun `REJECTED` de type inconnu**. **Aucun déploiement, aucun redémarrage** (JAR déjà en ligne ; un redéploiement identique + restart serait une churn inutile et consommerait un auto-reboot VeryGames). Aucun code, aucune donnée, aucun `SERVER_CHANGELOG` (rien de serveur n'a changé). Scan `ERROR` console VeryGames = `PENDING MANUAL VALIDATION`. Rollback du plugin si besoin : backup `~/.local/share/rpgquest/verygames-backups/rpgquest-20260908T165252Z-predeploy.jar` (SHA-256 `57bda61b…`). Aucun merge. | DONE | [2026-09-08_1846_verygames-dev-agent-actions-verification.md](2026-09-08_1846_verygames-dev-agent-actions-verification.md) |
| 2026-09-08 | **bug(wild) #87 — reconnexion depuis le Wild renvoyée au Hub.** Un joueur non-OP déconnecté dans `wild` (~`-1765,80,2607`) se reconnectait dans `world_hub` **exactement au spawn du village** (`spawn.yml` ~`738,67,-679`) → logout/login = échappatoire gratuit du Wild. **Audit exhaustif** des ~15 `PlayerJoinEvent` / `PlayerSpawnLocationEvent` / `PlayerRespawnEvent` et de tous les `teleport*`/`setSpawnLocation`/`setRespawnLocation` de `src/main` : le **seul** chemin écrivant cette position sur une connexion est `SpawnService.handleFirstJoin` (via `PlayerSpawnLocationEvent`), gardé par `!Player#hasPlayedBefore()`. **Cause racine** : `hasPlayedBefore()` renvoie `false` pour un joueur pourtant déjà venu dès que la métadonnée Bukkit `bukkit.firstPlayed` de son `playerdata` manque ou n'est pas encore peuplée (migration/transfert de serveur — cas VeryGames, UUID hors-ligne↔en-ligne, restauration de sauvegarde, aléa de timing du login) → RPGQuest jette sa position `wild` réelle et le force au village (`world_hub`). **Écarté** : Multiverse-Core DEV (`enforce-access: false`, `first-spawn-override: false`, `enable-join-destination: false`, `wild` pas même un monde MV). **Correction minimale, sans nouvelle persistance** : `spawn.JoinSpawnPolicy` (fonction pure testable) — on ne redirige vers le village **que si** un nouveau joueur (`!hasPlayedBefore()`) est placé par Paper dans le **monde principal** (`getServer().getWorlds().get(0)`) **ou** le **monde Hub** (`config.yml` → `hub.world`) ; si Paper restaure déjà le joueur dans tout autre monde chargé (`wild`, `claims`…) sa position est **conservée**, quel que soit `hasPlayedBefore()`. Repli « monde disparu » préservé (Paper renvoie au monde principal → redirection village) ; **aucun `if (!world.equals("world_hub"))`** codé en dur (allowlist config/API). `SpawnService.handleRespawn` (mort → village), Rune/Pierre de rappel, Claims, `dialogue.*` : **non touchés**. `SpawnService` reçoit un `Supplier<String> hubWorldName` (bootstrap `() -> config.hub().world()`). Nouveau log ciblé, une ligne/connexion, sans coordonnées : `join_restore player=<uuid> world=<w> action=KEEP_LAST_LOCATION|REDIRECT_TO_VILLAGE_SPAWN` (remplace le `[TP-TRACE] reason=first_join_spawn` ad hoc). Point d'intégration futur **#88 anti-combat-logging** documenté dans `JoinSpawnPolicy` (non implémenté). Tests : `JoinSpawnPolicyTest` (nouveau, cas 1-6 + bords), `SpawnServiceTest` étendu (régression #87 via le vrai listener + MockBukkit ; onboarding Hub conservé) ; `:test` / `:control-panel:test` / `build` — à renseigner. **Déploiement DEV** : JAR + **un seul** restart (protection anti-boucle VeryGames) — validation en jeu par l'owner (client réel) = `PENDING MANUAL VALIDATION`. **#87 non fermée, aucun merge.** | DONE | [2026-09-08_1905_bug-wild-reconnexion-hub-87.md](2026-09-08_1905_bug-wild-reconnexion-hub-87.md) |
