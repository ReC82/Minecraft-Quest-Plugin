# Rapports Claude

Historique chronologique, **immuable**, de chaque session de développement effectuée par Claude
dans ce dépôt — voir `CLAUDE.md`, section « Session Reports », pour la règle qui impose leur
création. Un rapport est créé après **chaque** demande (développement, correction de bug,
diagnostic, refactoring, migration, ou modification de configuration/documentation), même si la
tâche est petite, partielle, bloquée, ou n'a finalement modifié aucun code.

## Pourquoi ce dossier existe

Ces rapports ne remplacent **jamais** la documentation fonctionnelle du projet
(`docs/current_state.md`, `docs/ARCHITECTURE.md`, `docs/RPGQUEST_BIBLE.md`, etc.), qui continue
d'être maintenue à jour normalement. Ils servent un besoin différent : pouvoir être envoyés
**tels quels** à un autre assistant (ou humain) qui n'a suivi aucune des sessions précédentes, et
qui doit pouvoir comprendre, sans contexte supplémentaire :

- ce qui était demandé ;
- ce qui existait avant l'intervention ;
- ce qui a changé, et pourquoi (décisions techniques incluses) ;
- comment cela a été testé ;
- comment le déployer ;
- ce qui reste éventuellement problématique ou non traité.

## Règles

- **Un fichier par demande, jamais réutilisé** — un rapport n'écrase jamais un rapport précédent.
- **Immuable après création** — ne jamais modifier un ancien rapport pour refléter l'état actuel
  du projet. Un rapport documente l'état des choses *au moment où il a été écrit*, pas l'état
  actuel (voir `docs/current_state.md` pour ça).
- **Toujours créé, même en échec** — un statut `PARTIAL`/`BLOCKED`, des tests qui échouent, ou une
  demande purement diagnostique n'exemptent jamais de la création du rapport.
- **Reflète la réalité, pas la demande initiale** — le rapport décrit ce qui a été *réellement*
  fait, qui peut différer de ce qui était prévu au départ.
- **Mesure du temps réel, jamais inventée** — voir `CLAUDE.md`, section « Session Reports — Time
  Tracking » : début/fin/durée réels de la tâche, ou `non mesurable` explicitement si le début
  n'a pas pu être capturé.

## Nom de fichier

```
YYYY-MM-DD_HHMM_<description-courte>.md
```

Date et heure **locales réelles** au moment de la création du rapport (pas celles de la tâche
elle-même si le rapport est rédigé après coup). Exemples :

- `2026-08-21_2006_worldportal-debug-tools.md`
- `2026-08-21_2142_fix-hub-auto-teleport.md`
- `2026-08-22_1015_story-npc-integration.md`

## Gabarit obligatoire

Chaque rapport contient au minimum les sections suivantes, dans cet ordre :

```markdown
# RPGQuest — Rapport Claude

## Informations
* Date :
* Heure :
* Sujet :
* Statut : DONE / PARTIAL / BLOCKED
* Branche Git :
* Commit actuel si disponible :
* Début de la tâche : YYYY-MM-DD HH:MM:SS (heure locale réelle, ou « non mesurable »)
* Fin de la tâche : YYYY-MM-DD HH:MM:SS (heure locale réelle)
* Durée totale : HH:MM:SS (calculée entre les deux, jamais estimée)

## Demande
## Analyse
## Travail effectué
## Fichiers créés
## Fichiers modifiés
## Base de données / migrations
## Configuration / données
## Tests automatiques
## Tests manuels à effectuer
## Résultat attendu
## Reset / retour à l'état initial
## Déploiement VeryGames
### À transférer
### Ne PAS transférer/altérer
### Redémarrage requis
### Migration automatique
## Rollback
## Logs / diagnostic
## Documentation mise à jour
## Limitations / travail restant
## Prochaine étape suggérée
```

Voir n'importe quel rapport existant ci-dessous pour un exemple rempli.

## Index chronologique

| Date | Sujet | Statut | Fichier |
|---|---|---|---|
| 2026-08-21 | Outils de diagnostic WorldPortal (`here`/`debug`, TP-TRACE) | DONE | [2026-08-21_2009_worldportal-debug-tools.md](2026-08-21_2009_worldportal-debug-tools.md) |
| 2026-08-21 | Progression automatique de Storyline (connexion au moteur de quête) | DONE | [2026-08-21_2135_story-automatic-progression.md](2026-08-21_2135_story-automatic-progression.md) |
| 2026-08-23 | Investigation BREAK_BLOCK ne progresse pas dans `wild` (story_test) | BLOCKED | [2026-08-23_1234_break-block-wild-investigation.md](2026-08-23_1234_break-block-wild-investigation.md) |
| 2026-08-23 | Instrumentation `[QUEST-TRACE]` pour diagnostiquer la chaîne BREAK_BLOCK | DONE | [2026-08-23_1256_quest-trace-break-block-instrumentation.md](2026-08-23_1256_quest-trace-break-block-instrumentation.md) |
| 2026-08-23 | Investigation reset progression Story/quête (death/respawn, reconnexion) + correctif `premiers_pas.yml` manquant | PARTIAL | [2026-08-23_1643_quest-story-progress-persistence-investigation.md](2026-08-23_1643_quest-story-progress-persistence-investigation.md) |
| 2026-08-23 | Premier claim joueur 5×5 débloqué par une Story (CLAIM_TIER_1 → PNJ Jo → Acte de propriété → pose sans commande) | DONE | [2026-08-23_1758_premier-claim-5x5-story-deed.md](2026-08-23_1758_premier-claim-5x5-story-deed.md) |
| 2026-08-23 | 3 améliorations Claim : Jo « retourner à son claim », `/claim admin tp`, monde `claims` réellement pacifique | DONE | [2026-08-23_1857_claim-gohome-mobs-pacifiques.md](2026-08-23_1857_claim-gohome-mobs-pacifiques.md) |
| 2026-08-23 | MVP monde `claims` : frontière par particules, zone résidentielle safe, Pierre de retour, blocage Nether, complétion config.yml | DONE | [2026-08-23_2003_claims-mvp-frontiere-pacifique-pierre-retour.md](2026-08-23_2003_claims-mvp-frontiere-pacifique-pierre-retour.md) |
| 2026-09-05 | 4 corrections UX post-validation VeryGames : indicateur de canalisation, Pierre de retour limitée à `claims` + anti-perte, faisceau de retrouvaille du claim à distance | DONE | [2026-09-05_1729_claims-mvp-corrections-ux-pierre-retour-faisceau.md](2026-09-05_1729_claims-mvp-corrections-ux-pierre-retour-faisceau.md) |
| 2026-09-05 | Boucle joueur Hub ↔ Wild : système soulbound générique, item Journal des quêtes, Rune de rappel (cooldown persistant), avertissement compact avant entrée Wild, Waystones génératives, config `travel.*`, migrations V16/V17, `/rpgadmin waystone` | DONE | [2026-09-05_1837_boucle-joueur-hub-wild-soulbound-journal-rune-waystones.md](2026-09-05_1837_boucle-joueur-hub-wild-soulbound-journal-rune-waystones.md) |
| 2026-09-05 | Reset admin « nouveau joueur » : `/rpgadmin player resetnew <joueur> confirm` — remet l'état RPGQuest d'un seul joueur (online/offline) à l'équivalent « jamais joué » (quêtes, Stories, variables/unlocks, progression RPG, découvertes Waystones, cooldowns, claim principal, objets RPGQuest de l'inventaire). Voir la section « Reset complet "nouveau joueur" » du rapport et [docs/ADMIN_PLAYER_RESET.md](../ADMIN_PLAYER_RESET.md). | DONE | [2026-09-05_2002_reset-admin-nouveau-joueur.md](2026-09-05_2002_reset-admin-nouveau-joueur.md) |
| 2026-09-05 | Refonte de `CLAUDE.md` : cadre unique des règles pour toutes les futures sessions Claude Code (bootstrap `.ai/`, GIT/source de vérité, politique de push vers la branche de travail, branches sans `develop` permanente, TESTS/BUILD, DOCUMENTATION, SÉCURITÉ, DÉPLOIEMENT, GitHub Issues, mode de travail). Aucun code fonctionnel modifié. | DONE | [2026-09-05_2032_claude-md-refonte-regles-sessions.md](2026-09-05_2032_claude-md-refonte-regles-sessions.md) |
| 2026-09-05 | Issue #8 — mode preview/dry-run du reset joueur admin : `/rpgadmin player resetnew <joueur> preview` liste, catégorie par catégorie, ce qu'un reset réel effacerait, **sans aucune écriture**. Nouveaux points de lecture seule (`PlayerVariableRepository#findAllForPlayer`, `WaystoneService#discoveryCount`, `StoryService#progressRecords`), `PlayerResetService#previewReset`, refactor interne `countOrRemoveRpgItems`. 4 tests ajoutés. Comportement du reset réel inchangé. | DONE | [2026-09-05_2101_player-reset-preview-issue-8.md](2026-09-05_2101_player-reset-preview-issue-8.md) |
| 2026-09-05 | Issue #11 — Guide « centre d'aide » + journal de quêtes en GUI via le Libraire : `guide.yml` réécrit en menu d'aide structuré (nœud `help_menu` + sujets, orientations textuelles) ; nouveau registre `hub.HubGuideRegistry` (`hub-guides/*.yml`) pour la structure multi-Hub + diagnostic `/rpgadmin guide list\|info`. Le journal (`rpgquest:journal_quetes`) ouvre désormais la GUI `QuestJournalService` au clic droit ; GUI passée de 3 à **2 onglets** (« en cours » / « terminées »), onglet catalogue « Disponibles » supprimé ; `QuestJournalBookService` (résumé chat) supprimé. Tests : `hub/*` (3), `BundledDialoguesValidityTest`, `QuestJournalServiceTest` réécrit. | DONE | [2026-09-05_2233_guide-help-center-quest-journal-issue-11.md](2026-09-05_2233_guide-help-center-quest-journal-issue-11.md) |
| 2026-09-06 | Issue #11 — correction du bug de navigation entre onglets du journal : `COMPLETED → IN_PROGRESS` ne rafraîchissait plus la vue et le bouton « Fermer » devenait inerte après le 1er changement d'onglet. Cause : `showList`/`showDetail` enregistraient la session AVANT `player.openInventory()`, dont le `InventoryCloseEvent` synchrone (menu précédent) l'effaçait aussitôt via `handleClose`. Correctif : `openSessions.put(...)` déplacé APRÈS `openInventory`. Test de régression bidirectionnel `IN_PROGRESS → COMPLETED → IN_PROGRESS` (vrais clics, listener enregistré, sans fermer) + vérif du bouton Fermer. 931 tests verts. Redéployé DEV VeryGames (JAR seul). #11 non fermée. | DONE | [2026-09-06_1014_fix-journal-navigation-onglets-issue-11.md](2026-09-06_1014_fix-journal-navigation-onglets-issue-11.md) |
| 2026-09-06 | Issues #21/#22/#23 — parcours Claims cohérent de bout en bout. Audit : l'unlock du 1er claim = variable `CLAIM_TIER_1 == "true"`, accordée uniquement par `crystal_hunt` (fin de l'histoire principale, au Garde) ; le Guide omettait ce prérequis, d'où « Jo ne propose rien » après `resetnew`. Ajouts : `ClaimWorldAccessGuard` (portail Hub→claims fermé sans unlock, aucune téléportation, bypass `rpgquest.admin.world` seul) composé via `CompositeWorldPortalEntryGuard` ; `ClaimWorldSafetyListener` (Pierre de retour donnée à l'arrivée, joueur non éligible renvoyé au Hub — personne coincé, retour sans commande) ; modificateur de dialogue `negate: true` (`NegatedCondition`) ; `guide.yml`/`jo.yml` réécrits, Jo adapté aux 3 états. Tests : `ClaimWorldAccessGuardTest`, `ClaimWorldSafetyListenerTest`, `CompositeWorldPortalEntryGuardTest`, + `DialogueDefinitionParserTest`/`DialogueSessionEngineTest`. #21/#22/#23 non fermées (validation manuelle). | DONE | [2026-09-06_1116_parcours-claims-coherent-issues-21-22-23.md](2026-09-06_1116_parcours-claims-coherent-issues-21-22-23.md) |
| 2026-09-06 | Audit du parcours principal (nouveau joueur → `crystal_hunt` → `CLAIM_TIER_1`) + ré-audit #21/#22. Causes trouvées via le contenu réel du serveur DEV : (1) aucun PNJ Citizens lié `guard` (`npc_citizens_bindings` : guide, libraire, help, jeff, junior, jo) → `first_steps` ET `crystal_hunt` indémarrables ; (2) `dialogues/guard.yml` déployé périmé, sans branche `crystal_hunt`. #21/#22 : le code refuse bien l'entrée / renvoie au Hub pour un compte non-OP — les symptômes correspondent au bypass `rpgquest.admin.world` (défaut op) déclenché par un test OP. Ajouts : journalisation de décision `[claims-access]` / `[claims-safety]` ; redéploiement DEV du JAR + `dialogues/guard.yml`. Doc : §1b PNJ obligatoires (NPC_DIALOGUES_QUESTS_GUIDE), BIBLE, current_state, VERYGAMES. #21/#22/#23 non fermées ; création du PNJ Garde + validation en jeu restantes. | PARTIAL | [2026-09-06_1219_audit-parcours-principal-garde-crystal-hunt.md](2026-09-06_1219_audit-parcours-principal-garde-crystal-hunt.md) |
| 2026-09-06 | Issue #36 — raccourcis admin/DEV pour tester quêtes & stories sans rejouer le gameplay. Réutilise les services métier (jamais d'écriture directe en base). `QuestProgressEngine.accept(..., ignorePrerequisites)` ; `StoryService.adminAdvance`/`adminComplete` (avance déterministe, garde anti double-récompense conservée via `forceComplete`, ordre respecté, bornée) ; `/rpgadmin quest start\|complete\|reset <joueur> <id>` (+ `force`), `/rpgadmin story advance\|complete <joueur> <storyId>` (message « étape à tester »), `/rpgadmin player variable get\|set <joueur> <clé>`. Permission `rpgquest.admin.world` ; `variable set` exige en plus `rpgquest.admin.debug`. Tab-complétion + journalisation. Tests : `StoryServiceTest` +8, `RpgAdminTestShortcutsCommandTest` (11), `QuestProgressEngineTest` +2. Doc : `ADMIN_TEST_SHORTCUTS.md` (nouveau), BIBLE, current_state. Branche `feat/36-admin-test-shortcuts`, non fusionnée, #36 non fermée. | DONE | [2026-09-06_2028_admin-test-shortcuts-issue-36.md](2026-09-06_2028_admin-test-shortcuts-issue-36.md) |
| 2026-09-07 | Préparation d'une session de validation manuelle DEV : audit de tout le backlog `needs-manual-test` (15 issues) → **seules #21/#22/#23/#36 sont implémentées et déployées** (JAR `bcc3a6ec…`) ; #27 sur une branche divergente non déployée ; #24/#25/#26/#28/#30/#31/#32/#33/#34/#35 **non implémentées**. Checklist unique séquentielle PHASE 0→7 (redémarrage + création du PNJ **Garde** = blocant ; parcours Claims non-OP couvrant #21+#22+#23 ; fiche #36 en 5 min ; commentaires de validation pré-rédigés). Audit du serveur DEV : Garde toujours absent, contenu YAML/portails/items OK, `Rondoudou9000` = compte de test neuf non-OP. + Audit du module `web-api` et **`docs/control-panel/`** (vision, architecture, sécurité, config, bridge RPGQuest, roadmap, 7 ADR) pour l'issue #37 — documentation seule, aucun code. Aucune issue fermée, aucune branche fusionnée, aucun déploiement. | DONE | [2026-09-07_0744_preparation-session-validation-manuelle-backlog.md](2026-09-07_0744_preparation-session-validation-manuelle-backlog.md) |
| 2026-09-07 | Issue #37 — socle exécutable du RPGQuest Control Panel : premier flux vertical navigateur → login → dashboard → bridge → **état réel du plugin**. Nouveau module Gradle **`control-panel/`** autonome (auth owner PBKDF2 + session signée HMAC + CSRF + logout, config externe multi-cibles `Target`, `PermissionService`/`Role`/`Permission`, audit log append-only `control-panel.db`, dashboard sobre responsive, gestion propre de l'indisponibilité du bridge). Côté plugin : package **`web.admin`** — `WebAdminServer` + `HealthSource`/`BukkitHealthSource`, route `GET /admin/v1/health` authentifiée Bearer, **fail-closed**, config **par variables d'environnement uniquement** (ADR-008, aucun changement `config.yml`/`PluginConfig`). Tests : `PanelAppTest` (11), `BridgeClientTest` (4), `PasswordHasherTest` (4), `JsonTest` (3), plugin `WebAdminServerTest` (4) — `./gradlew test build` vert. Audit AWS **lecture seule** pour #44 (nginx + 2 vhosts + ports libres + TLS Certbot ; aucune conf touchée) → `docs/control-panel/AWS.md`. Doc `docs/control-panel/` alignée + ADR-008/009/010. Branche `feat/37-control-panel`, non fusionnée, #37 **non fermée** (dépend de #44 + décision d'hébergement du bridge). | DONE | [2026-09-07_0820_control-panel-socle-issue-37.md](2026-09-07_0820_control-panel-socle-issue-37.md) |
| 2026-09-07 | Issue #44 — déploiement de **PlugAdmin** (socle #37) sur AWS via **https://plugadmin.lodylands.com**, sans perturber `dig.lodygames.com` / `lodylands.com`. Audit AWS avant mutation (nginx 1.24, 2 vhosts, port 8090 libre, IP `3.226.216.90`) ; **DNS déjà résolu** → aucune action DNS. Scripts reproductibles `scripts/plugadmin/` (`install.sh` idempotent, `deploy.sh`, `rollback.sh` + templates). Installé : utilisateur système dédié `plugadmin`, app sous `/opt/plugadmin/app` (`installDist`), secrets `/etc/plugadmin/plugadmin.env` (`0640`, hors Git ; `RPGQUEST_PANEL_SECRET` + `RPGQUEST_BRIDGE_TOKEN_DEV` générés, `OWNER_HASH` fourni par l'owner), `control-panel.db` sous `/var/lib/plugadmin`, service systemd `plugadmin` durci (`enable`, `Restart=on-failure`), vhost nginx **dédié** `sites-available/plugadmin` (`nginx -t` + `reload`, jamais `restart`), certificat Certbot **ECDSA** `plugadmin.lodylands.com` (exp. 2026-12-06) + redirection 80→443. Vérifs live : `/health` local **et** public 200 ; backend non exposé sur `:8090` ; `GET /login` 200 + en-têtes sécurité + cookie CSRF `Secure`/`HttpOnly`/`SameSite` ; `/dashboard` anonyme → 303 ; login invalide → 401 ; CSRF manquant/faux → 403 ; systemd start/stop/restart + reprise après `kill -9` ; dashboard « RPGQuest DEV indisponible » **sans 500** (validé sur instance jetable isolée). **Non-régression autres sites : 200/301 inchangés avant/après**, 3 certificats intacts. Aucun code Java modifié. Aucune issue fermée, aucune branche fusionnée. **PARTIAL** : login owner réel par navigateur externe reste à faire par l'owner (mot de passe non communiqué). | PARTIAL | [2026-09-07_0935_plugadmin-aws-deploy-issue-44.md](2026-09-07_0935_plugadmin-aws-deploy-issue-44.md) |
| 2026-09-07 | Issue #51 — **agent sortant** RPGQuest/VeryGames → PlugAdmin/AWS : inversion du flux du bridge #37 (RPGQuest initie l'HTTPS sortant). Plugin `com.lodygames.rpgquest.web.agent` : `AgentConfigLoader` (fichier local hors Git `plugadmin-agent.properties`, fail-closed), `HeartbeatPayload` (réutilise `HealthSource` #37, zéro duplication), `PlugAdminClient` (`java.net.http`, cert normal, corps borné), `AgentLoop` (heartbeat + file d'actions, backoff, idempotence via `ProcessedActionCache`), `AgentActionExecutor` (whitelist `AgentActionType` → services métier ; MVP = `player.variable.get` seul, jamais `/rpgadmin` texte), `PlugAdminAgent` (PluginService async). Control Panel `panel.agent` : `AgentEndpoints` (`/agent/v1/{heartbeat,actions,actions/{id}/result}`, auth par agent temps constant, 413/503), `AgentStore` (`control-panel.db` : `agent_heartbeat` + `agent_action`, idempotent, expiration), `AgentLiveness` ONLINE/STALE/OFFLINE, dashboard « AGENT DISTANT » prioritaire + page `/agents`. Tests : `web.agent.*` 30, `panel.agent.*` 21, `PanelAppTest` +1 — `./gradlew clean build` vert (plugin 1005). **Déployé sur VeryGames DEV** (JAR `5e9d9a5e…` + fichier agent) via `deploy-verygames.sh` ; **RCON VeryGames validé** (mention « pas de RCON » corrigée) + nouveaux `scripts/verygames-rcon.py` / `verygames-restart.sh` / `plugadmin/send-mail.py` ; redémarrage RCON exécuté. **Validation live** : heartbeat réel reçu, dashboard ONLINE (données), `player.variable.get` → SUCCESS, type inconnu → REJECTED, idempotence OK, panne PlugAdmin n'affecte pas Minecraft + reconnexion OK. Email SMTP de fin de tests envoyé. ADR-011 (remplace ADR-004). Branche `feat/51-plugadmin-outbound-agent`, non fusionnée, #51 non fermée. | DONE | [2026-09-07_1127_plugadmin-outbound-agent-issue-51.md](2026-09-07_1127_plugadmin-outbound-agent-issue-51.md) |
| 2026-09-07 | Issue #40 — **abstraction de la persistance** + moteur de base configurable (socle SQLite → MySQL, hors #41/#42). Audit : tout le SQL est déjà confiné à `com.lodygames.rpgquest.database` (aucun SQL dans les services de gameplay) ; couplage réel = URL JDBC + `PRAGMA` en dur de `DatabaseManager`, `PRAGMA user_version` de `SchemaMigrator`, syntaxe SQLite (`ON CONFLICT`/`INSERT OR IGNORE`/`AUTOINCREMENT`) dans 20 repositories + migrations. Nouveau : `DatabaseSettings`/`DatabaseType` (config `database.type: sqlite\|mysql`, mot de passe via `password-env` uniquement), `DatabaseEngine` (`SqliteDatabaseEngine` câblé / `MySqlDatabaseEngine` reconnu — corps réel = #41) + `DatabaseEngineFactory` (unique `switch`), `SqlDialect` (`SqliteDialect`/`MySqlDialect` : upsert, insert-ignore, identité, `columnExists`), `SchemaHistory` (`PragmaUserVersionHistory` SQLite natif / `MigrationTableHistory` portable), `SchemaMigration`+`SchemaMigrationRunner` (application ordonnée, idempotente, échec nommé, `targetVersion`). `SchemaMigrator` → catalogue `ALL` (SQL des 17 migrations **inchangé**). `DatabaseManager` : ctor `Path` conservé (SQLite, inchangé) + ctor `DatabaseEngine` + `healthCheck()` non bloquant. `PluginConfig.databaseFile`→`DatabaseSettings database` (+ `databaseFile()` raccourci). Tests : +25 `database.*` + 8 `ConfigValidatorTest` ; `SchemaMigratorTest` (21) et tous les `*RepositoryTest` **inchangés et verts**. `./gradlew test build` vert. Doc : `docs/PERSISTENCE.md` (nouveau) + ARCHITECTURE / current_state / BIBLE / INDEX / CONTEXT. Branche `feat/40-persistence-abstraction`, non fusionnée, #40 non fermée (backend MySQL réel = #41). | DONE | [2026-09-07_1315_persistence-abstraction-issue-40.md](2026-09-07_1315_persistence-abstraction-issue-40.md) |
| 2026-09-07 | Outillage admin du Control Panel (P0) : rafraîchissement auto des actions (issue #65) + pages **Joueurs / Quêtes / Stories**. Canal agent (#51) étendu de `player.variable.get` à 15 types whitelistés adossés aux **services métier existants** via la façade `AgentActions`/`BukkitAgentActions` (jamais de commande texte, jamais de SQL, mutations sur le thread principal) : lectures `player.list` / `quest.list` / `quest.player.status` / `story.list` / `story.player.status` / `item.list` / `player.resetnew.preview` ; mutations `player.item.give` / `quest.start\|complete\|reset` / `story.advance\|complete` / `player.variable.set` / `player.resetnew.confirm`. Double liste blanche `AgentActionType` ↔ `AgentActionCatalog`, validation 3 couches, `confirm` obligatoire sur les mutations, audit. `#65` : `/assets/panel.js` (même origine, conforme CSP) + `/agents/actions.json`, polling seulement s'il reste du `PENDING`. Tests : `AgentActionExecutorTest` étendu, `AgentActionCatalogTest`, `BusinessPagesTest`, `AgentActionsRefreshTest` ; `./gradlew :test :build :control-panel:build` vert. **P1 (PNJ / Diagnostics / autocomplete #66) et P2 (édition dialogues) non traités**. Branche `feat/control-panel-admin-tools`, non fusionnée, non déployée. | PARTIAL | [2026-09-07_2159_control-panel-admin-tools.md](2026-09-07_2159_control-panel-admin-tools.md) |
| 2026-09-08 | Bug #65 — durcissement du rafraîchissement auto des actions agent (page Agents). Audit bout-en-bout : le mécanisme de `0eee145` (`/assets/panel.js` + `/agents/actions.json`) est fonctionnellement correct (rendu serveur `data-actions-pending`, JSON valide, script exécuté sous `defer`, CSP/proxy OK) mais jamais validé manuellement et dépendant d'un signal de démarrage unique (le compteur serveur figé). `panel.js` : le polling démarre maintenant sur `data-actions-pending > 0` **ou** sur un statut non terminal (`PENDING`/`DELIVERED`) encore visible dans le `<tbody>` (`tableHasPending`), et fait un **premier relevé immédiat** (plus d'attente de 2 s). Garanties inchangées : arrêt à `pending == 0`, garde-fou 5 min, aucun polling si rien en cours, aucun script inline/dépendance. `AgentActionsRefreshTest` renforcé (assertions HTML `data-actions-pending` 1 puis 0 + pastilles PENDING/SUCCESS) + 1 nouveau test verrouillant le comportement du JS servi. `:control-panel:test` (66) + `:control-panel:build` verts ; comportement JS vérifié en DOM headless. Backend + agent inchangés. Doc : `docs/control-panel/ROADMAP.md`, `docs/current_state.md`. Branche `feat/control-panel-admin-tools`, non fusionnée, non déployée, #65 non fermée (validation manuelle navigateur restante). | DONE | [2026-09-08_0648_fix-65-refresh-actions-agent.md](2026-09-08_0648_fix-65-refresh-actions-agent.md) |
| 2026-09-08 | Page « Joueurs » du Control Panel — état constaté. La demande partait de « Joueurs — à venir » ; sur la branche courante, `/players` est **déjà** dans le menu (`Layout.nav` activé au commit `594d865`), routée (`handleBusinessPage`, `Permission.PLAYERS_READ`, session obligatoire), et affiche le roster réel via l'action **existante** `player.list` (`AgentPages.players()` → `AgentStore.latestActionOfType`) : nom, UUID (tronqué + complet en `title`), monde, position ; messages « aucune liste chargée » / « aucun joueur connecté » / bandeau « aucun agent configuré ». La page contient **aussi** l'outillage P0 (variables / GIVE / reset / détail) que le scope strict interdisait → 3 options soumises à l'utilisateur → **décision : « laisser `/players` tel quel »**. Aucun changement de code ni de test ; rendu des 3 cas (roster 1 joueur, sans liste, sans agent) vérifié par harnais jetable non conservé. `:control-panel:test` 66/0 échec (dont `BusinessPagesTest` 9/9). Rapport + index seulement. Branche `feat/control-panel-admin-tools`, non fusionnée, non déployée. | DONE | [2026-09-08_0726_page-joueurs-etat-constate.md](2026-09-08_0726_page-joueurs-etat-constate.md) |
| 2026-09-08 | Déploiement AWS de la branche `feat/control-panel-admin-tools` @ `dce3bf3` sur `https://plugadmin.lodylands.com` via `scripts/plugadmin/deploy.sh` (script existant, sans argument). Pré-vol : branche/commit OK, arbre Git propre, `:control-panel:test` 66/0 échec + `:control-panel:build` vert, baseline des autres sites relevée. Déroulé : `installDist` → sauvegarde `/opt/plugadmin/app` sous `releases/20260908-073415` → swap → `systemctl restart plugadmin` (`active`, `NRestarts=0`, PID 26613, `event=panel_started port=8090`). Vérifs : `/health` local + public `ONLINE` (stable ×3) ; `/` 303, `/login` 200, `/dashboard` anon 303, `/players` anon 303 → `/login` (route active, plus un placeholder) ; distribution déployée **byte-identique** au build de `dce3bf3` (jar sha256 `90cf7b57…`), `panel.js` avec durcissement #65 ; `nginx -t` OK, `sites-enabled` inchangé, `dig`/`lodylands`/`www.lodylands` toujours 200 ; secrets + unit systemd + TLS non touchés ; **aucune action VeryGames**, aucun JAR RPGQuest. Rollback : `scripts/plugadmin/rollback.sh app`. Validation authentifiée navigateur (login owner → `/dashboard`, `/players`, menu sans « Joueurs — à venir ») = `PENDING MANUAL VALIDATION`. Pas de merge. | DONE | [2026-09-08_0735_deploy-aws-branche-control-panel-admin-tools.md](2026-09-08_0735_deploy-aws-branche-control-panel-admin-tools.md) |
| 2026-09-08 | Déploiement **VeryGames DEV** du JAR RPGQuest de `feat/control-panel-admin-tools` @ `3c3ea5a` (contenu agent = `e1ddb8c`) pour que l'agent reconnaisse les 15 nouveaux types d'action déjà servis par le Control Panel (`player.list`, `quest.list`, … `player.variable.set`). Pré-vol : branche/commit OK, arbre propre, `AgentActionType` contient les 15 wire-names, tests ciblés `web.agent.*` **45/45** (`--rerun-tasks`), `deploy-verygames.sh --check` OK. Déploiement : `scripts/deploy-verygames.sh -y` (JAR seul) → `./gradlew test`+`build` OK, backup auto `rpgquest-20260908T075204Z-predeploy.jar` (sha256 `2d648c0b…`), transfert FTP atomique du JAR **sha256 `cd66574c…`** (1 256 923 o), aucun autre fichier ; puis `scripts/verygames-restart.sh` (`save-all` → `stop` RCON → relance auto → ONLINE < 1 min). Vérifs : `/plugins` RPGQuest **en vert**, heartbeat agent reçu par PlugAdmin (`server_state=ONLINE`, mondes `hub`/`claims`/`wild` `loaded:true`), aucun ERROR `journalctl -u plugadmin`. **Validation canal agent** : `player.list` → **SUCCESS** « 0 joueur(s) », `quest.list` → **SUCCESS** « 10 quête(s) » — plus de `REJECTED` « type non whitelisté ». Aucune donnée touchée, nginx/AWS non touchés, aucune migration DB, aucune action destructive, pas de merge. Rollback : `scripts/rollback-verygames.sh --latest`. Scan ERROR console VeryGames = `PENDING MANUAL VALIDATION`. | DONE | [2026-09-08_0756_deploy-verygames-actions-agent.md](2026-09-08_0756_deploy-verygames-actions-agent.md) |
| 2026-09-08 | Bug d'affichage `/stories` : `story.list` renvoie `SUCCESS` (« 2 story(s) chargée(s) ») mais le catalogue affiche « Aucun catalogue chargé. ». **Cause racine** : `AgentPages.latestDetails` faisait `store.latestActionOfType(type)` (dernière action du type, tous statuts) `.filter(SUCCESS)` → dès qu'une action `story.list` plus récente est non terminale (PENDING pendant que l'agent répond) ou en échec, le dernier `SUCCESS` exploitable est masqué et le catalogue se vide ; chaque clic « Rafraîchir » recrée une PENDING par-dessus. Défaut latent identique sur `/quests` et `/players` (mêmes consommateurs de `latestDetails`). **Correctif** (2 fichiers prod, ~20 lignes, pas de refacto, pas de feature) : `AgentStore.latestSuccessfulActionOfType(agentId, type)` (`… AND status='SUCCESS' ORDER BY created_at DESC LIMIT 1`) + `latestDetails` qui l'utilise (drop du `.filter(SUCCESS)` redondant) ; `latestForPlayer` inchangé. Reproduit puis vérifié corrigé. Tests : `StoriesCatalogTest` (3, nouveau), `AgentStoreTest` +1 → `:control-panel:test` **70/0 échec**, `:control-panel:build` vert. Aucun déploiement, aucun merge. | DONE | [2026-09-08_0815_fix-stories-catalogue-vide.md](2026-09-08_0815_fix-stories-catalogue-vide.md) |
| 2026-09-08 | Déploiement AWS du Control Panel (`feat/control-panel-admin-tools` HEAD `70ae001`, correctif `/stories` `541f549`) via `scripts/plugadmin/deploy.sh` — exit 0 : `installDist` OK, ancienne app `dce3bf3` sauvegardée sous `releases/20260908-082229`, swap, `systemctl restart` → `active`, `NRestarts=0`, `event=panel_started port=8090`. JAR déployé **sha256 `281f26e5…`** == build frais de la branche ; `AgentStore.class` contient `latestSuccessfulActionOfType`. `/health` local + public `ONLINE` (×3). Routes `/stories`/`/quests`/`/players` (anon) → 303 `/login` (actives). **Correctif vérifié contre la base de prod** : sur un instantané read-only de `control-panel.db` avec une `story.list` `PENDING` injectée par-dessus le dernier `SUCCESS`, `latestSuccessfulActionOfType` renvoie le `SUCCESS` (catalogue = 2 stories `main_story` + `story_test`) — « Rafraîchir » ne vide plus le catalogue. `/quests` + `/players` : dernières listes en base = `SUCCESS`, pas de régression (`BusinessPagesTest` 9/9). Autres sites nginx 200, nginx/secrets/unit non touchés. Vue navigateur authentifiée = `PENDING MANUAL VALIDATION`. Rollback : `scripts/plugadmin/rollback.sh app`. **Aucun déploiement VeryGames, aucun JAR RPGQuest, aucun merge.** | DONE | [2026-09-08_0824_deploy-aws-fix-stories.md](2026-09-08_0824_deploy-aws-fix-stories.md) |
| 2026-09-08 | Passe UX/UI du Control Panel — **issue #74** (catalogues Quêtes/Stories lisibles) + passe visuelle commune. `MiniText` (nouveau) : rendu web des textes MiniMessage — sous-ensemble sûr interprété (couleurs nommées/hex → `<span style>`, gras/italique/souligné/barré), tout le reste (gradient/hover/click/lang…) retiré proprement, texte toujours échappé → **jamais de balise brute** ; `prettifyId`/`prettifyTokens` (`AMETHYST_SHARD` → « Amethyst Shard », sans table de traduction). `Ui` (nouveau) : composants réutilisables (pastilles de statut **glyphe + texte** PENDING/DELIVERED/SUCCESS/FAILED/REJECTED/EXPIRED, `.tid` id technique discret, `.badge`, `.empty`, `.table-wrap`). Cartes quête/story restructurées (`.entity-card`) : titre humain d'abord, id en second, objectifs/récompenses normalisés, prérequis et étapes de story référencés par **titre de quête** via le catalogue `quest.list` local (aucun appel agent en plus). Passe visuelle : `.danger-zone` (reset joueur isolé), bannière succès après envoi d'action, nav mobile scrollable, tables responsive, thème sombre conservé. #65 inchangé (panel.js consomme `statusHtml`). Tests : `MiniTextTest` (7), `QuestsCatalogTest` (2), `StoriesCatalogTest`/`AgentActionsRefreshTest` ajustés → `:control-panel:test` **79/0**, `:control-panel:build` vert. Correction `scripts/plugadmin/rollback.sh` (sélection de release par nom, pas mtime) + `deploy.sh` rétention. **Déployé AWS** (`bc044fd`) via `deploy.sh` : service `active`, `/health` ONLINE, agent reconnecté, rendu /quests+/stories vérifié contre la base de prod (0 balise brute) ; autres sites nginx 200 ; rollback → `releases/20260908-085217`. Tickets **#75** (donneur de quête, besoin protocole) et **#76** (noms FR matériaux) créés. Vue navigateur authentifiée = `PENDING MANUAL VALIDATION`. **Aucun déploiement VeryGames, aucun JAR RPGQuest, aucun merge.** | DONE | [2026-09-08_0855_passe-ux-control-panel-74.md](2026-09-08_0855_passe-ux-control-panel-74.md) |
| 2026-09-08 | Lot UX Control Panel — **#77** (récompenses lisibles) + **#76** (noms FR matériaux/mobs) + audit **#75** + copie d'identifiant + cohérence Dashboard/Agents. Audit du protocole : `quest.list` envoie récompenses/objectifs **déjà aplatis en chaînes** ; panel sans dépendance Paper ; donneur de quête absent partout. **#77 MVP panel-only** : `RewardText` parse les formats de `describeRewards` (`+Nx MAT` → « Objet : Épée en diamant ×1 », `variable K=true` → « Débloque : Claim Tier 1 », `customitem give …` → conséquence) ; **valeur brute toujours conservée** en `<code class="tid">` copiable ; inconnu → chaîne telle quelle. **#76 MVP panel-only** : `MinecraftNames` (table FR ~90 entrées + repli `prettifyId`) sur les objectifs. **Copie d'ID** : tout `.tid` `role=button data-copy=<valeur complète>` ; `panel.js` listener délégué clic/clavier + repli `execCommand` + flash ✓. **Dashboard/Agents** : `Ui.liveness` (pastille glyphe+texte), en-têtes nettoyés, table actions avec type humain (`AgentActionCatalog`) + fil copiable ; JSON `typeHtml`/`idFull` consommé par `panel.js`, **polling #65 inchangé** (vérifié DOM headless). Tests : `RewardTextTest` (6), `MinecraftNamesTest` (3), `QuestsCatalogTest`/`AgentActionsRefreshTest`/`PanelAppTest` ajustés → `:control-panel:test` **88/0**, `:control-panel:build` vert. **Déployé AWS** (`6a592c9`, sha256 `01f8d883…`) via `deploy.sh` : service `active`, `/health` ONLINE, agent reconnecté, rendu `/quests` vérifié contre la base de prod (objectifs FR, récompenses lisibles + brut conservé) ; autres sites nginx 200 ; rollback → `releases/20260908-092850`. **#75** commenté (audit, ouvert), **#78** créé (structurer récompenses/objectifs agent-side, requiert déploiement JAR VeryGames), **#76**/**#77** commentés MVP. Vue navigateur authentifiée = `PENDING MANUAL VALIDATION`. **Aucun déploiement VeryGames, aucun JAR RPGQuest, aucun merge.** | DONE | [2026-09-08_0930_lot-ux-control-panel-76-77.md](2026-09-08_0930_lot-ux-control-panel-76-77.md) |
| 2026-09-08 | Protocole `quest.list` **structuré** — issues **#78** (objectifs/récompenses structurés) + **#75** (donneur de quête). Audit : `BukkitAgentActions` aplatissait tout en chaînes (`"Tuer SPIDER (x5)"`, `"commande console : …<tronqué 60>"`), le panel les reparsait par regex. **Plugin** : nouveaux records `ObjectiveSummary {kind,target,amount,raw}` et `RewardSummary {kind,amount,target,value,command,raw}` ; `QuestSummary` gagne `rewardDetails`+`giverId`/`giverName`, `QuestStepSummary` gagne `objectiveDetails` — champs legacy `objectives`/`rewards` **conservés à l'identique** (compat agent déployé). `BukkitAgentActions.objectiveTarget()`/`rewardDetails()` — commande console **non tronquée**. `AgentActionExecutor.questList` sérialise les Map imbriquées + `giverId` (si déclaré). **#75** : `QuestDefinition` gagne un champ optionnel `giver` (nullable) ; `QuestDefinitionParser.parseGiver` (vide si défini = erreur) ; `crystal_hunt`/`premiers_pas`/`woodcutters_request` reçoivent `giver:`. **Panel** : `ObjectiveText` (nouveau — verbe FR par `kind` + nom via `MinecraftNames`/`prettifyId`, sans regex sur phrase), `RewardText.fromSummary` (commande complète), `AgentPages.renderQuestCard` (structure prioritaire, repli legacy, ligne « Donneur »). Tests : `QuestListPayloadTest` (nouveau — JSON réel de `quest.list`), `ObjectiveTextTest` (nouveau), `RewardTextTest` (+5), `QuestsCatalogTest` (+structuré), `AuthenticatedSmokeTest` (nouveau — login owner → `/dashboard` `/players` `/quests` `/stories` rendus) → `:test` **1086/0**, `:control-panel:test` **103/0**, `./gradlew build` vert. **Déployé AWS** (`02642e9`, jar sha256 `6493fc15…`) via `deploy.sh` : `/health` ONLINE ×3, routes protégées → 303, autres sites 200, rollback → `releases/20260908-105940`. **Déployé VeryGames DEV** : JAR sha256 `4fbaa345…` (`deploy-verygames.sh -y` + `verygames-restart.sh`, backup `…110044Z-predeploy.jar`) + `crystal_hunt.yml` via `--also` (backup `extra-20260908T110340Z/`) + RCON `quest admin reload` (10 quêtes, 0 erreur). **Validation `quest.list` réelle** (action injectée dans `control-panel.db`, exécutée par l'agent DEV) → **SUCCESS** : `objectiveDetails` (kill/collect/craft/talk) + `rewardDetails` (XP/commande complète/variable) présents, `crystal_hunt.giverId=="guard"`, `first_steps` sans `giverId`, legacy en parallèle. **#78 fermée**. **#75** : implémentée + rendue + validée live, vue navigateur authentifiée = `PENDING MANUAL VALIDATION` → laissée ouverte. Rollback VeryGames : `rollback-verygames.sh --latest` (+ `--also` pour le YAML). **Aucun merge.** | DONE | [2026-09-08_1100_quest-list-structure-78-75.md](2026-09-08_1100_quest-list-structure-78-75.md) |
| 2026-09-08 | Action agent **`npc.list`** + page Control Panel **`/npcs`** (V1, lecture) — issues **#66** / **#75**. Audit : liaison PNJ Citizens ↔ id RPGQuest = table `npc_citizens_bindings` (`NpcBindingRepository`) ; dialogue ↔ PNJ = **convention** `dialogueId == rpgquest:<npcId>` ; quête ↔ PNJ = `QuestDefinition.giver()` (#75) + `TalkToNpcObjective.npcId()` ; tag d'entité vanilla = PDC hors base (scan monde → **hors V1**) ; `/rpgadmin npc` n'a que `tag\|untag\|info` (aucune liste). **Plugin** : `NpcCatalog` (pur, testable sans Bukkit) croise ces sources → `NpcRow` {id, displayName (speaker du dialogue), citizensNumericId, bound, hasDialogue, dialogue nœuds/choix, dialogueStartsQuests, questsGiven, questsReferenced, sources, warnings} + `canonicalIds` (ids attendus par dialogues+quêtes, **jamais** les tags) + compteurs. Anomalies : `DUPLICATE_BINDING` (err), `QUEST_REF_NO_NPC` (warn, dégradé info si Citizens inactif), `TAGGED_UNUSED` (info, + suggestion d'id canonique proche par Levenshtein ≤ 2 → **`garde`→`guard`**, cas #66), `DIALOGUE_NO_NPC`, `GIVER_NO_DIALOGUE`. Nouvelle action `AgentActionType.NPC_LIST` ↔ `AgentActionCatalog` (`Permission.NPC_READ`), lecture seule ; `BukkitAgentActions` reçoit `dialogueEngine`/`npcIdentityService`/`npcBindingRepository` (wiring bootstrap) ; `AgentActionExecutor.npcList()` sérialise ; **aucun `onMain`, aucune lecture du monde**. **Panel** : entrée « PNJ » (`/npcs`) activée ; `AgentPages.npcs()` + `renderNpcCard()` réutilisent `entity-card`/`MiniText`/`Ui` (id copiables, `Ui.severity()` nouvelle), cartes triées anomalies-d'abord, bloc `<details>` « IDs canoniques connus » (préparation #66). #66 **reste séparée** (câblage `/rpgadmin npc tag` = chantier à part + validation manuelle). Tests : `NpcCatalogTest` (9, pur), `web.agent.NpcListPayloadTest` (3, JSON réel), `AgentActionExecutorTest` +1, panel `NpcsCatalogTest` (2), `AuthenticatedSmokeTest`/`BusinessPagesTest` étendus `/npcs` → `:test` **1099/0**, `:control-panel:test` **105/0**, `./gradlew build` vert. **Déployé AWS** (`9fa57d1`, jar sha256 `2bf90a25…`) via `deploy.sh` : `/health` ONLINE ×3, `/npcs` anon → 303, autres sites 200, rollback → `releases/20260908-114604`. **Déployé VeryGames DEV** : JAR sha256 `12786cb3…` (`deploy-verygames.sh -y` + `verygames-restart.sh`, backup `…114637Z-predeploy.jar`). **Validation `npc.list` réelle** (action injectée dans `control-panel.db`, exécutée par l'agent DEV) → **SUCCESS** ; `details.npcs` peuplé, `canonicalIds` + warnings cohérents. Vue navigateur authentifiée = `PENDING MANUAL VALIDATION`. **Aucun merge.** | DONE | [2026-09-08_1147_npc-list-page-npcs-v1.md](2026-09-08_1147_npc-list-page-npcs-v1.md) |
| 2026-09-08 | **Système PNJ V2 déclarative** — définition logique + écritures de contenu (issues **#66** / **#75**). Audit : `NpcCatalog` V1 dérivait `canonicalIds` d'une union de références, aucune notion de définition ; pas de couche d'écriture YAML non destructive (`YamlConfiguration.save()` perd les commentaires). **Plugin** (`com.lodygames.rpgquest.npc`) : `NpcDefinition` `{id, displayName, description?, dialogueId?, role?, enabled}` (sans Bukkit, id non renommable) + `NpcDefinitionParser`/`Loader`/`NpcLoadReport` + `YamlNpcEngine` (`PluginService`, `npcs/*.yml`, exemple `guard.yml` livré, `reload()` hors thread principal). `NpcDefinitionYaml.render` (déterministe, re-parsable) + `NpcDefinitionStore` (create/update atomique, **refuse l'écrasement**, jamais de chemin fourni). `QuestGiverEditor` (transformation **texte pure** : pose/replace la ligne racine `giver:`, commentaires/format/CRLF préservés, idempotent) + `QuestGiverStore` (localise le fichier de quête par son `id:`). `NpcCatalog` V2 : `logicalDefinitionPresent` vs `citizensBindingPresent`, `state` (`LINKED`/`NOT_LINKED`/`DISABLED`/`CITIZENS_ORPHAN`/`UNDEFINED_REFERENCE`/`BROKEN`), `definedIds` = source canonique ; anomalies `NO_DEFINITION`/`BINDING_NO_DEFINITION`/`DIALOGUE_MISSING` (err), `NOT_LINKED`/`DISABLED`/`GIVER_NO_DIALOGUE` (info). **Protocole** : `npc.list` enrichi ; nouvelles mutations `npc.definition.create`/`npc.definition.update` (`NpcDefinitionStore`) + `quest.giver.set` (`QuestGiverEditor` + `reloadQuestDefinitions` sur le thread principal) — whitelistées (`AgentActionType`↔`AgentActionCatalog`), permission dédiée (`NPC_WRITE`/`QUEST_GIVER_WRITE`), `confirm` obligatoire, auditées, validation 3 couches, **jamais de YAML brut ni de chemin arbitraire**. IO sur le thread poll async (conforme CLAUDE.md). **Panel `/npcs`** : deux blocs (Définition RPGQuest / Binding Citizens), formulaire de création (pré-rempli sur carte non définie), édition limitée (`display_name`/`dialogue`/`role`/`enabled` — jamais l'id), attribution de quête (select + confirm), registre canonique « Définis » vs « Tous ». `AgentPages` reçoit `PermissionService`. **Hors scope assumé** : spawn/binding Citizens web, éditeur de dialogues, câblage `/rpgadmin npc tag` (#66), suppression de définition, enrichissement live. Tests : `NpcDefinitionParser/Loader/Yaml/Store`, `QuestGiverEditor/Store`, `NpcCatalogTest`+`NpcListPayloadTest` réécrits V2, `AgentActionExecutorTest` +2, panel `NpcsCatalogTest` réécrit → `:test` **1127/0**, `:control-panel:test` **107/0**, `./gradlew build` vert. **Déployé AWS** (`1340dda`, jar sha256 `01f14c60…`) via `deploy.sh` : `/health` ONLINE ×3, `/npcs` anon → 303, autres sites 200, rollback → `releases/20260908-123552`. **VeryGames DEV : NON déployé** — panne FTP `si-16041.dg.vg` (`curl (28) timed out`) au moment de la tâche ; le script abandonne avant toute écriture, serveur DEV inchangé. À rejouer (`deploy-verygames.sh -y` + `verygames-restart.sh`) dès que le FTP répond ; validation `npc.list`/`npc.definition.create`/`quest.giver.set` = `PENDING`. Vue navigateur authentifiée = `PENDING MANUAL VALIDATION`. **Aucun ticket fermé, aucun merge.** | DONE | [2026-09-08_1235_npc-v2-declarative.md](2026-09-08_1235_npc-v2-declarative.md) |
| 2026-09-08 | **Lier une définition PNJ à un PNJ Citizens existant** — issue **#81, phase 1** uniquement (spawn Citizens = phase 2, non traité). Audit : `CitizensNpcBridge` isole tous les imports `net.citizensnpcs.*` mais n'énumérait pas le registre ; `NpcBindingRepository` n'offrait qu'un `UPSERT ON CONFLICT(citizens_uuid) DO UPDATE` (rebind silencieux, inadapté) ; `NpcIdentityService` avait le cache `citizensCache` mais aucune écriture ; schéma `npc_citizens_bindings` (migration V12) réutilisé **sans changement**. **Plugin** : `CitizensNpc` (record public top-level `{numericId,uuid,name,spawned}`) ; `CitizensBindPlanner` (pur, sans Bukkit/SQL → `Plan{INSERT|NOOP|REJECT, code, message}` : idempotence `NOOP`, `CITIZENS_TAKEN`, `NPC_ID_TAKEN`) ; `CitizensNpcBridge.roster()`/`byNumericId()` (seuls points touchant `CitizensAPI.getNPCRegistry()`) ; `NpcBindingRepository.insertIfAbsent()` (`INSERT OR IGNORE`, renvoie `boolean`, non destructif — l'`UPSERT` de rebind reste inutilisé) ; `NpcIdentityService.citizensRoster()`/`citizensByNumericId()`/`bindCitizens()` (+ record `BindResult`, met à jour `citizensCache` sur `INSERT` → identification en jeu effective **sans redémarrage**). **Protocole** : `npc.citizens.list` (lecture, `NPC_READ` — `loadAll()` async → `onMain` registre → fusion ; payload `{numericId,uuid,name,linkedNpcId?,availableForBinding,spawned}` + compteurs, **aucune position/monde**) ; `npc.citizens.link` (mutation whitelistée `AgentActionType`↔`AgentActionCatalog`, **permission dédiée `NPC_BIND_WRITE`**, `confirm` obligatoire, audit, validation 3 couches — `npc_id` motif `[a-z0-9._-]{1,64}`, `citizens_id` entier 1..10 000 000, jamais de SQL/UUID libre/commande console). Refus tôt si définition absente/`DISABLED`/Citizens inactif/`citizens_id` introuvable ; sinon `bindCitizens` async. **Orphelins** (`guide/help/jeff/jo/junior/libraire` = Citizens sans définition) : **aucune mutation ajoutée** — `NpcCatalog.build` croise déjà binding+définition par id, créer `npcs/<id>.yml` fait passer en `LINKED` au prochain `npc.list`. **Panel `/npcs`** : bouton « Rafraîchir les PNJ Citizens », résumé (`total`/`available`/`linked`), `citizensLinkForm()` (`select` — Citizens occupés `disabled`, `confirmBox` obligatoire) affiché **seulement** sur carte définition OUI + binding NON + `enabled` ; carte `LINKED` → `#N — Nom` sans bouton + mention « procédure de rebind ajoutée ultérieurement ». Future `npc.citizens.create` (phase 2) documentée, **non codée**. Tests : `CitizensBindPlannerTest` (5), `NpcIdentityServiceTest#bindCitizens*` (4), `NpcBindingRepositoryTest` (+1), `NpcCitizensPayloadTest` (3), `AgentActionExecutorTest` (+2), panel `NpcsCatalogTest` (réécrit, `woodcutter_bob` devient `NOT_LINKED` + flux de liaison) → `:test` **1142/0**, `:control-panel:test` **109/0**, `./gradlew build` vert. **Déployé AWS** (`3d1f50c`, jar sha256 `a52ad4d0…`) via `deploy.sh` : release `20260908-131859`, `/health` ONLINE, autres sites 200. **Déployé VeryGames DEV** : JAR sha256 `342bf808…` (`deploy-verygames.sh -y` + `verygames-restart.sh`, backup `…131925Z-predeploy.jar`) ; RCON `plugins` RPGQuest+Citizens verts, heartbeat `13:24:04Z` ONLINE, aucun ERROR `journalctl`. **Validation `npc.citizens.list` réelle** (action injectée dans `control-panel.db`) → **SUCCESS** : 7 Citizens `#0..#6`, tous **déjà liés** (`available:0`), payload sans position/monde. `npc.list` toujours **SUCCESS**. **`npc.citizens.link` — 4 cas live** (aucune liaison créée) : `woodcutter_bob`→#6 (pris) = FAILED `CITIZENS_TAKEN` ; `guard`→#6 (identique) = SUCCESS `NOOP` ; `does_not_exist_xyz`→#6 = FAILED `UNKNOWN_NPC` ; `woodcutter_bob`→#9999 = FAILED `UNKNOWN_CITIZENS`. **Chemin nominal `LINKED` non exerçable en direct** (aucun Citizens libre sur DEV, consigne de ne pas en créer / ne pas détourner) → couvert par les tests. Aucun PNJ Citizens créé/supprimé, aucune progression joueur touchée, aucune migration. Rollback : `rollback-verygames.sh --latest` / `plugadmin/rollback.sh app`. Vue navigateur authentifiée = `PENDING MANUAL VALIDATION`. **#81 non fermée, aucun merge.** | DONE | [2026-09-08_1325_npc-citizens-link-81.md](2026-09-08_1325_npc-citizens-link-81.md) |
| 2026-09-08 | **Email de fin de tâche #81 phase 1 non reçu** — diagnostic + renvoi + durcissement SMTP générique. Chaîne testée avec transcript `smtplib` complet (`set_debuglevel(2)`) : cette machine → relais `send.one.com` **fonctionne** — `AUTH` 235, `MAIL FROM`/`RCPT TO` 250, `DATA` → **`250 Ok: queued as 0552c5e2-ab8b-11f1-8ba2-fd5ae24138eb`**, 0 destinataire refusé, exit 0. Le 1er envoi (~13:29) avait donc aussi été mis en file. Non-livraison **en aval du relais one.com** (one.com→Gmail ou filtrage Gmail) — **hors de portée** depuis ce serveur (pas de MTA local, pas de `/var/log/mail*`). Cause déterministe la plus probable : les scripts d'envoi ad-hoc (et `smtplib.send_message`) partaient **sans en-tête `Date` ni `Message-ID`** → Gmail supprime/classe indésirable silencieusement. **Renvoyé 2×** vers `lloyd.helpdesk@gmail.com` (destinataire habituel), rapport `2026-09-08_1325_npc-citizens-link-81.md` joint : (a) `scratchpad/resend_81.py` avec en-têtes corrigés → queued `0552c5e2…`, exit 0 ; (b) via la procédure générique corrigée → `Message-ID <1788875057…@lodywood.be>`, 0 refusé, session SMTP 250, exit 0. **`scripts/plugadmin/send-mail.py` durci** (outillage, aucun code applicatif) : ajoute **toujours** `Date` + `Message-ID` ; nouvelle option **`--attach PATH` répétable** (MIME deviné : `.md`→`text/markdown`…) ; exit 3 si destinataire refusé ; sortie de contrôle (destinataire, sujet, message-id, pièces, refusés, code SMTP). Rétro-compatible, aucun appelant automatisé. `py_compile` OK. **Aucun déploiement, aucun merge, aucune suite Gradle** (rien de fonctionnel touché). | DONE | [2026-09-08_1344_smtp-rapport-81-non-recu.md](2026-09-08_1344_smtp-rapport-81-non-recu.md) |
| 2026-09-08 | **Spawn d'un PNJ Citizens depuis une définition** — issue **#81 phase 2**. Audit API Citizens (`createNPC(EntityType,name)` + `NPC.spawn(loc, CREATE)` ; `NPC.destroy()` pour rollback ; `getById`/`getByUniqueId` pour vérifier le registre — aucune commande console). Nouvelle action agent **`npc.citizens.create`** (mutation whitelistée `AgentActionType` ↔ `AgentActionCatalog`, **permission dédiée `NPC_SPAWN_WRITE`**, `confirm` obligatoire, audit, validation 3 couches) : crée physiquement un PNJ Citizens (`EntityType.PLAYER`, nom = `NpcDefinition.displayName`) depuis une définition puis le lie. Paramètres **métier uniquement** : `npc_id` / `world` / `x`/`y`/`z` / `yaw`?/`pitch`? — jamais de nom libre / UUID / commande / YAML / chemin. **`CitizensSpawnPlanner`** (pur, sans Bukkit) : toutes les préconditions logiques avant création — Citizens actif (`CITIZENS_UNAVAILABLE`), définition présente (`UNKNOWN_NPC`) + `enabled` (`NPC_DISABLED`), `npc_id` pas déjà lié (`NPC_ALREADY_LINKED`), monde de la **liste blanche RPGQuest** (`hub`/`claims`/`travel.wildWorld` de la config — `UNKNOWN_WORLD`), position **finie et bornée** (`|x|,|z| ≤ 29 999 984`, `-2048 ≤ y ≤ 2048`, `-90 ≤ pitch ≤ 90` — `INVALID_POSITION`, jamais « corrigée »). Puis thread principal : monde **chargé** + `y` dans `[minHeight, maxHeight)`. **`CitizensSpawnCoordinator`** (pur, `Spawner`+`Binder`) : `create → bind → success`, sinon **rollback du seul PNJ créé** (double clé `numericId`+`uuid`, jamais un préexistant) → `BIND_FAILED_ROLLED_BACK` (`rolled_back`). Persistance via `NpcIdentityService.bindCitizens` (réutilise `CitizensBindPlanner` + `insertIfAbsent` + cache → effet en jeu sans redémarrage). Threading : préconditions pures, `loadAll` async, registre Citizens + monde `onMain`, persistance async. Résultat structuré `{code, npc_id, citizens_id, world, rolled_back, effects}`. **Panel `/npcs`** : bloc « Créer le PNJ Citizens » sur les cartes définies `NOT_LINKED` `enabled` — preview (id, nom, « aucun Citizens lié »), monde en **liste déroulante** (mondes chargés du heartbeat, champ libre en repli), X/Y/Z/yaw/pitch à saisir (**jamais devinés**), `confirm` obligatoire ; jamais sur une carte `LINKED`. `Permission.NPC_SPAWN_WRITE`. Tests : `CitizensSpawnPlannerTest` (14), `CitizensSpawnCoordinatorTest` (4), `AgentActionExecutorTest` +2, `NpcCitizensPayloadTest` +3, `NpcsCatalogTest` +2 → `:test` **1165/0** (29 ignorés), `:control-panel:test` **111/0**, `./gradlew build` vert. **Déployé AWS** (`b9f3beb`, jar sha256 `bff44c5b…`) via `deploy.sh`. **Déployé VeryGames DEV** (jar sha256 `6a7ff0d4…`, `deploy-verygames.sh -y` + `verygames-restart.sh`, backup `…144029Z-predeploy.jar`) — la **1re passe a révélé un NPE** (`citizens_id` null dans les détails d'outcome + `Map.copyOf`), **corrigé** (`citizens_id` → `-1`) puis redéployé. **Validation live** (actions injectées) : `npc.list` / `npc.citizens.list` **SUCCESS** ; `npc.citizens.create` **sans spawn réel** → refus `NPC_ALREADY_LINKED` / `UNKNOWN_WORLD` / `INVALID_POSITION` / `UNKNOWN_NPC` (`citizens_id: -1`) + `REJECTED` sur `x=NaN`. Chemin `CREATED` = tests seulement. Premier spawn réel = `PENDING MANUAL VALIDATION`. #81 non fermée, aucun merge. | DONE | [2026-09-08_1423_npc-citizens-create-81-phase2.md](2026-09-08_1423_npc-citizens-create-81-phase2.md) |
| 2026-09-08 | **Page `/dialogues` V1** — lecture structurée des dialogues à embranchements + bases d'un futur éditeur. Audit du modèle : `DialogueDefinition` **lève** si nœuds vides / `start` invalide / `start` absent → « dialogue sans nœud » et « `start` inexistant » sont des **erreurs de chargement**, pas des warnings sur une définition ; `DialogueChoice.next` = redirection intra-dialogue ; actions `sealed` (10 : `START_QUEST` / `ADVANCE_QUEST` / `TURN_IN_QUEST` / `GIVE_ITEM` / `TAKE_ITEM` / `SET_VARIABLE` / `RUN_SAFE_COMMAND` / `OPEN_DIALOGUE` / `OPEN_MERCHANT` / `CLOSE`), conditions `sealed` (8 + `NegatedCondition`) ; `DialogueLoader` valide en 2ᵉ passe id dupliqué / `OPEN_DIALOGUE` manquant / **cycles `OPEN_DIALOGUE`** ; **ouverture en jeu TOUJOURS par convention `rpgquest:<npcId>`** (identité stable), jamais via `NpcDefinition.dialogue` (diag seulement). **Plugin** : `DialogueCatalog` (pur, sans Bukkit) — par dialogue : `id`, `startNodeId`, `linkedNpcIds`, compteurs, `referencedQuestIds` / `startsQuestIds`, `nodes[]` ordonnés (départ d'abord) avec `reachable` (BFS des `next`), choix avec **actions et conditions typées** `{kind, target, value, raw}` (`NegatedCondition` déplié) ; warnings `NODE_UNREACHABLE` / `QUEST_REF_UNKNOWN` / `DIALOGUE_NO_NPC` / `MULTIPLE_NPCS` / `DEFINITION_DIALOGUE_DIVERGES` / `NEXT_MISSING` ; `loadIssues[]` **à part** (fichiers rejetés) + `declaredButMissing[]`. `DialogueDraft` (modèle éditable minimal) + `DialogueDefinitionYaml.render` (déterministe re-parsable) + `DialogueDefinitionStore.create` (atomique tmp+`ATOMIC_MOVE`, **refus d'écrasement** `EXISTS`, rechargement complet du dossier — fichier supprimé si le nouveau dialogue ne recharge pas). **Protocole** : `dialogue.list` (lecture, permission dédiée `DIALOGUE_READ`) ; `dialogue.definition.create` (mutation, permission dédiée `DIALOGUE_WRITE`, `confirm`, audit — squelette `dialogues/<key>.yml` : 1 nœud `start` + 1 choix `CLOSE` ; jamais de YAML brut ni de chemin ; `reload()` après succès). **Panel** : page `/dialogues` activée (nav + route) — cartes avec graphe lisible (nœud de départ mis en avant, nœuds inaccessibles marqués, MiniMessage rendu via `MiniText`, ids copiables), bannières `loadIssues` + `declaredButMissing`, formulaire de création de squelette ; `dialogueEffectLabel()` rend chaque action/condition en libellé lisible (titre humain de quête via `quest.list` local). **Hors scope V1** (futur éditeur documenté dans le rapport) : `dialogue.node.create/update`, `dialogue.choice.add/update/delete`, édition texte préservant commentaires/structure (couche AST), réordonnancement, builder graphique. Tests : `DialogueCatalogTest` (10), `DialogueDefinitionStoreTest` (3), `AgentActionExecutorTest` +2, `DialoguesCatalogTest` (3), smoke tests étendus → `:test` **1179/0** (29 ignorés), `:control-panel:test` **114/0**, `./gradlew build` vert. **Déployé AWS** (`1fa7f4a`, jar sha256 `3c873e53…`) via `deploy.sh` : `/health` ONLINE, `/dialogues` anon → 303, autres sites 200. **Déployé VeryGames DEV** (jar sha256 `57bda61b…`, `deploy-verygames.sh -y` + `verygames-restart.sh`, backup `…151640Z-predeploy.jar`). **Validation `dialogue.list` réelle** (action injectée) → **SUCCESS** : 7 dialogues (`guide`/`help`/`jeff`/`jo`/`guard`/`junior`/`libraire`), 22 nœuds, actions/conditions typées présentes, `guard`/`junior`/`libraire` liés + `guide`/`help`/`jeff`/`jo` → `DIALOGUE_NO_NPC` (PNJ Citizens sans définition, cohérent #81 p1), **14 `loadIssues`** = 7 fixtures `test_*.yml` préexistantes correctement rejetées (page rendable). `npc.list` toujours SUCCESS. `dialogue.definition.create` **non exécuté en réel** (pas de contenu ajouté à DEV) — couvert par les tests. Vue navigateur authentifiée = `PENDING MANUAL VALIDATION`. Aucun ticket fermé, aucun merge. | DONE | [2026-09-08_1510_dialogues-page-v1.md](2026-09-08_1510_dialogues-page-v1.md) |
| 2026-09-08 | **Nettoyage des fixtures `test_*.yml` égarées dans `plugins/RPGQuest/dialogues/` — VeryGames DEV (issue #83).** La validation `dialogue.list` de la V1 `/dialogues` avait relevé **14 `loadIssues`** (« `start` obligatoire » + « `nodes` obligatoire ») provenant de **7 fichiers `test_*.yml`** posés dans le dossier actif du serveur DEV. **Audit** : ce sont des **fixtures de quête de test manuel** (id `rpgquest:test_<objectif>`, structure `steps`/`objectives`/`rewards`, en-tête « QUÊTE DE TEST MANUEL »), déjà **versionnées** à leur place légitime `docs/manual-tests/quests/test_*.yml` — emplacement **non chargé** par `YamlDialogueEngine`. Téléchargées depuis DEV puis comparées octet à octet aux fixtures du dépôt → **identiques 7/7** : copies exactes déposées dans le **mauvais dossier** (`dialogues/` au lieu de `quests/`). Aucune référence du dépôt (tests, scripts, docs, `story_test.yml`, `StoriesCatalogTest`) ne les attend dans `dialogues/` ; aucun rôle gameplay actif. `RPGQuest/quests/` contient bien 3 fixtures `test_*.yml` mais **légitimement** (scénario `story_test`, dossier lu par le moteur de quêtes) → **laissées en place**. **Décision : aucune modification du dépôt** — les fixtures ont déjà le bon emplacement versionné, pas de nouvelle convention. **DEV** : backup FTPS des 7 fichiers vers `~/.local/share/rpgquest/verygames-backups/issue-83-dialogues-20260908T154228Z/` (tailles + SHA-256 enregistrés), puis **7 × `DELE`** de `RPGQuest/dialogues/test_*.yml` (garde-fou « nom `test_*.yml` »), **aucun autre fichier touché** (7 dialogues gameplay, `quests/`, `data.db`, `config.yml`, mondes, `Citizens/` intacts). Pas de reload à chaud du dossier `dialogues/` (`/rpgquest reload` = `config.yml` seul) → **redémarrage propre** `verygames-restart.sh` (0 joueur, aucune progression touchée). **Aucun redéploiement JAR** (aucun code modifié). **Validation réelle** (actions injectées dans `control-panel.db`, exécutées par l'agent DEV) : `dialogue.list` → **SUCCESS** « 7 dialogue(s) (4 avec avertissement, **0 fichier(s) rejeté(s)**) », `loadIssues` **0** (était 14), 7 dialogues gameplay présents (`guide`/`help`/`jeff`/`jo`/`guard`/`junior`/`libraire`), `nodeTotal=22` inchangé, 4 × `DIALOGUE_NO_NPC` **préexistants** (PNJ Citizens sans `NpcDefinition`, cohérent #81 p1), **aucun nouveau warning** ; `npc.list` → **SUCCESS** (8 PNJ) inchangé ; heartbeat agent `ONLINE` ; **0 `ERROR`** dans `journalctl -u plugadmin`. Loader **inchangé** — aucun filtre `test_*`, aucun `loadIssue` masqué. Vue navigateur `/dialogues` authentifiée = `PENDING MANUAL VALIDATION`. **#83 non fermée, aucun merge.** | DONE | [2026-09-08_1547_nettoyage-fixtures-test-dialogues-dev-83.md](2026-09-08_1547_nettoyage-fixtures-test-dialogues-dev-83.md) |
| 2026-09-08 | **Éditeur guidé `/dialogues` — phase 1 de #82 : édition simple et sûre + refonte UX.** Audit : `DialogueDraft` est volontairement pauvre (pas de conditions ni d'actions riches) → un aller-retour par ce modèle **détruirait** les `START_QUEST` / `QUEST_STATE` / `RUN_SAFE_COMMAND` d'un dialogue gameplay. **Décision** : écriture « charger → muter le modèle métier → re-sérialiser le dialogue **complet** → re-parser → sauver ». **Plugin** : `DialogueDefinitionWriter` (nouveau, pur — sérialiseur fidèle et déterministe des 10 actions + 8 conditions + négation, re-parsable à l'identique) ; `DialogueDefinitionEditor` (nouveau, pur IO+parsing) — localise le fichier par `id`, **refuse un fichier déjà invalide** (`SOURCE_INVALID`), applique la mutation, **garde-fou round-trip** (`reparsed.equals(muté)` sinon `ROUNDTRIP`, rien écrit), **écriture atomique**, **rechargement** puis **restauration du contenu d'origine** si échec (`RELOAD_FAILED`). 5 mutations : `dialogue.node.update` (locuteur/texte, choix conservés), `dialogue.node.create` (nœud simple orphelin), `dialogue.choice.add` / `.update` / `.delete` — **choix simple** seulement (ni condition ni action hors `CLOSE` ; `next` vers un nœud existant **xor** fermeture ; jamais le dernier choix ; `UNSAFE_CHOICE` sur un choix riche). `AgentActionType`↔`AgentActionCatalog` miroir, permission dédiée `DIALOGUE_WRITE`, `confirm` obligatoire, audit, 3 couches de validation (panel/agent/service — `dialogue_id` normalisé, `node_id` borné, `text` ≤ 512 mono-ligne, `choice_index` 0..199, `next` xor `close`). IO + re-parse **sur le thread de poll async**, jamais le thread principal ; `YamlDialogueEngine.reload()` après succès. Renommage/suppression de nœud, réordonnancement, édition des actions/conditions riches = **hors périmètre phase 1** (documenté). Fichier édité au **format canonique** (commentaires/mise en forme d'origine non conservés — choix assumé). **Control Panel** : `/dialogues` refondue en 4 blocs (en-tête identité+**pastille d'état**, résumé départ/PNJ/quêtes, **diagnostics triés erreur→attention→info** message humain puis code discret, graphe de **cartes nœud** départ accentué / inaccessible marqué / actions-conditions en éléments secondaires). Édition **semi-inline** (`<details>` par nœud, **aucun JS**, conforme CSP) : « Modifier ce nœud », « Ajouter un choix » (cible = `<select>` des nœuds du dialogue), « Modifier / supprimer » sous chaque choix simple, « Ajouter un nœud » sous la carte ; un choix riche affiche « édition prévue dans une phase ultérieure ». Bandeau format canonique. Tests : `DialogueDefinitionEditorTest` (14, nouveau), `AgentActionExecutorTest` +5, `AgentActionCatalogTest` +5, `DialoguesCatalogTest` +3 → `:test` **1197/0** (29 ignorés), `:control-panel:test` **121/0**, `./gradlew build` vert. Docs : `AGENT.md` (5 lignes + section « éditeur guidé »), `RPGQUEST_BIBLE.md` §5, `current_state.md`, `control-panel/ROADMAP.md`, `SERVER_CHANGELOG.md` (+ « Exécution réelle » à compléter). **Déploiement AWS + VeryGames DEV : à réaliser** (le code agent change) ; vue navigateur authentifiée + cycle d'édition sur dialogue de test = `PENDING MANUAL VALIDATION`. **#82 non fermée, aucun merge.** | DONE | [2026-09-08_1642_dialogues-editeur-guide-phase1-82.md](2026-09-08_1642_dialogues-editeur-guide-phase1-82.md) |
| 2026-09-08 | **Vérification : l'agent VeryGames DEV reconnaît déjà les 15 nouvelles actions du Control Panel** (`player.list`, `quest.list`, `quest.player.status`, `story.list`, `story.player.status`, `item.list`, `player.resetnew.preview/confirm`, `player.item.give`, `quest.start/complete/reset`, `story.advance/complete`, `player.variable.set`). Demande = déployer le JAR RPGQuest de la branche pour lever des `REJECTED` « type non whitelisté » observés sur `player.list` / `quest.list`. **Constat** : cette observation date d'**avant** le déploiement de l'éditeur guidé `/dialogues` #82 (2026-09-08 ~16:52 UTC, même branche, commit `171849b`), qui a déjà posé le JAR contenant toute l'énumération `AgentActionType`. Vérifs : branche `feat/control-panel-admin-tools` @ `70cbeda` (arbre propre, delta vs `171849b` = `docs/` seul → JAR inchangé) ; les 29 wire-names (dont les 15 demandés) présents dans `AgentActionType` **source HEAD** ET dans `AgentActionType.class` extrait du **JAR distant** ; **SHA-256 identiques** entre `build/libs/rpgquest-0.1.0-SNAPSHOT.jar` local et le JAR VeryGames (`12f66391…`, 1 427 111 o) ; DEV **ONLINE** depuis ~95 min (heartbeat `ONLINE`, `/plugins` RPGQuest+Citizens+WorldEdit+Multiverse verts, `rpgquest version` OK, `journalctl -u plugadmin` **0 ERROR** depuis 17:00). Tests : `./gradlew :test --tests 'com.lodygames.rpgquest.web.agent.*'` **70/0**, `./gradlew build` **SUCCESSFUL**. **Validation distante** (lectures pures via le canal agent) : `player.list` → **SUCCESS** (« 1 joueur »), `quest.list` → **SUCCESS** (« 10 quêtes »), `story.list` → **SUCCESS**, `item.list` → **SUCCESS** — **plus aucun `REJECTED` de type inconnu**. **Aucun déploiement, aucun redémarrage** (JAR déjà en ligne ; un redéploiement identique + restart serait une churn inutile et consommerait un auto-reboot VeryGames). Aucun code, aucune donnée, aucun `SERVER_CHANGELOG` (rien de serveur n'a changé). Scan `ERROR` console VeryGames = `PENDING MANUAL VALIDATION`. Rollback du plugin si besoin : backup `~/.local/share/rpgquest/verygames-backups/rpgquest-20260908T165252Z-predeploy.jar` (SHA-256 `57bda61b…`). Aucun merge. | DONE | [2026-09-08_1846_verygames-dev-agent-actions-verification.md](2026-09-08_1846_verygames-dev-agent-actions-verification.md) |
| 2026-09-08 | **bug(wild) #87 — reconnexion depuis le Wild renvoyée au Hub.** Un joueur non-OP déconnecté dans `wild` (~`-1765,80,2607`) se reconnectait dans `world_hub` **exactement au spawn du village** (`spawn.yml` ~`738,67,-679`) → logout/login = échappatoire gratuit du Wild. **Audit exhaustif** des ~15 `PlayerJoinEvent` / `PlayerSpawnLocationEvent` / `PlayerRespawnEvent` et de tous les `teleport*`/`setSpawnLocation`/`setRespawnLocation` de `src/main` : le **seul** chemin écrivant cette position sur une connexion est `SpawnService.handleFirstJoin` (via `PlayerSpawnLocationEvent`), gardé par `!Player#hasPlayedBefore()`. **Cause racine** : `hasPlayedBefore()` renvoie `false` pour un joueur pourtant déjà venu dès que la métadonnée Bukkit `bukkit.firstPlayed` de son `playerdata` manque ou n'est pas encore peuplée (migration/transfert de serveur — cas VeryGames, UUID hors-ligne↔en-ligne, restauration de sauvegarde, aléa de timing du login) → RPGQuest jette sa position `wild` réelle et le force au village (`world_hub`). **Écarté** : Multiverse-Core DEV (`enforce-access: false`, `first-spawn-override: false`, `enable-join-destination: false`, `wild` pas même un monde MV). **Correction minimale, sans nouvelle persistance** : `spawn.JoinSpawnPolicy` (fonction pure testable) — on ne redirige vers le village **que si** un nouveau joueur (`!hasPlayedBefore()`) est placé par Paper dans le **monde principal** (`getServer().getWorlds().get(0)`) **ou** le **monde Hub** (`config.yml` → `hub.world`) ; si Paper restaure déjà le joueur dans tout autre monde chargé (`wild`, `claims`…) sa position est **conservée**, quel que soit `hasPlayedBefore()`. Repli « monde disparu » préservé (Paper renvoie au monde principal → redirection village) ; **aucun `if (!world.equals("world_hub"))`** codé en dur (allowlist config/API). `SpawnService.handleRespawn` (mort → village), Rune/Pierre de rappel, Claims, `dialogue.*` : **non touchés**. `SpawnService` reçoit un `Supplier<String> hubWorldName` (bootstrap `() -> config.hub().world()`). Nouveau log ciblé, une ligne/connexion, sans coordonnées : `join_restore player=<uuid> world=<w> action=KEEP_LAST_LOCATION|REDIRECT_TO_VILLAGE_SPAWN` (remplace le `[TP-TRACE] reason=first_join_spawn` ad hoc). Point d'intégration futur **#88 anti-combat-logging** documenté dans `JoinSpawnPolicy` (non implémenté). Tests : `JoinSpawnPolicyTest` (nouveau, cas 1-6 + bords), `SpawnServiceTest` étendu (régression #87 via le vrai listener + MockBukkit ; onboarding Hub conservé) ; `:test` / `:control-panel:test` / `build` — à renseigner. **Déploiement DEV** : JAR + **un seul** restart (protection anti-boucle VeryGames) — validation en jeu par l'owner (client réel) = `PENDING MANUAL VALIDATION`. **#87 non fermée, aucun merge.** | DONE | [2026-09-08_1905_bug-wild-reconnexion-hub-87.md](2026-09-08_1905_bug-wild-reconnexion-hub-87.md) |
| 2026-09-08 | **Centre de documentation `/docs` dans le Control Panel — MVP (issue #49).** Wiki d'administration **privé** : entrée « Documentation » dans le menu, accueil (gros champ de recherche + catégories + raccourcis « Comment faire ? »), **recherche plein texte en mémoire** (bonus plats titre 25 > tags 12 > catégorie 7 > commandes 5 > corps plafonné ; ET des termes ; préfixe ; extraits contextualisés), **rendu Markdown sûr** maison (`docs.Markdown` — tout échappé, aucune balise HTML brute, aucun JS, liens limités à `/docs/…` / ancre / `https://` ; titres+ancres, listes 2 niveaux, tableaux, blocs de code avec bouton **Copier** réutilisant `panel.js`, callouts `> [!NOTE]`/`[!WARNING]`). **Source de vérité = Git** : 9 fiches Markdown versionnées sous `control-panel/src/main/resources/docs/`, listées par `_index.txt` qui **est** la liste blanche — **aucun contenu en base**, **aucun chemin du navigateur ouvert** : fiches adressées par un `slug` interne (`[a-z0-9-]{1,64}`) validé puis résolu par un lookup `Map<slug,DocPage>`, **zéro accès disque à la requête** → path traversal / lecture `.env` / clés : impossibles par construction ; slug inconnu/malformé → 404 (jamais 500). Classes : `DocFrontMatter` (title/category/tags/order ; Markdown sans front matter accepté), `Markdown`, `DocPage`, `DocLibrary`, `DocSearchIndex`, `DocsPages` (accueil / résultats / fiche avec fil d'Ariane + sommaire + tags + source). Route `/docs` dans `PanelApp` (`requireSession` + nouvelle permission `DOCS_READ` accordée à tous les rôles) ; entrée nav + ~60 lignes CSS responsive dans `Layout` ; liens contextuels depuis `/npcs` `/quests` `/players` `/dialogues`. **9 fiches** : Créer/configurer un PNJ (prioritaire : nom affiché vs id `Garde`/`guard`, `/npc create|select|skin|move|remove`, `/rpgadmin npc tag|untag|info`, id canonique, risque mauvais tag, lien NpcDefinition/dialogue/giver/`TALK_TO_NPC`/binding, diagnostic `/npcs`), commandes Citizens `2.0.43`, reset joueur (`/rpgadmin player resetnew … confirm`, console vs in-game, OP/deop), quêtes/stories (`start|complete|reset`, `story …`, `player variable set`, ids), Claims (acte via Jo, `/claim admin resettier1|tp`), Wild (portail, Rune/Pierre de rappel, tests, #87), déploiement VeryGames (+ **règle anti-auto-reboot** `10 auto-reboot in less than 30 minutes … We exit now` + quoi faire), déploiement AWS (`plugadmin/deploy.sh` + `rollback.sh app`), référence `/rpgadmin`. Toutes les commandes documentées ont été **auditées dans le code** (`RpgAdminCommand`, `QuestCommand`, `plugin.yml`, scripts) et dans `docs/`. Tests : `DocFrontMatterTest`, `MarkdownTest` (incl. XSS `<script>`/`<img onerror>`/`javascript:`/`../` neutralisés), `DocLibraryTest`, `DocSearchIndexTest` (toutes les requêtes de l'énoncé → bonne fiche en tête), `DocsPagesTest` (routes, auth 303, recherche, fiche, 404, path traversal → jamais de contenu système, jamais de 500). `:control-panel:test` 154/0 ; `:test` inchangé ; `./gradlew build` — à renseigner. **Déploiement AWS uniquement** (aucun code plugin/agent, aucun restart Minecraft) ; validation navigateur = `PENDING MANUAL VALIDATION`. **#49 non fermée, aucun merge.** V2 : édition depuis le navigateur, permissions fines, indexation des `docs/` du dépôt, dernier commit Git par fiche. | DONE | [2026-09-08_2003_control-panel-centre-documentation-49.md](2026-09-08_2003_control-panel-centre-documentation-49.md) |
| 2026-09-08 | **Refonte graphique complète de PlugAdmin (#92) + éditeur guidé de quêtes et de stories (#46).** **#92 (committé `9202aad`+`0e529c5`)** : design system clair en *custom properties* dans `Layout.CSS` (fond gris très clair, surfaces blanches, sidebar `#1d2534` non-noire, primaire bleu `#2f6df6`, couleurs d'état) ; `Icons.java` (35 icônes SVG **locales**, sprite `<symbol>`/`<use>` — ni emoji ni CDN) ; shell refait (topbar identité+chip env DEV+chip état serveur, sidebar en 4 groupes, **drawer mobile sans JS**) ; `Ui.java` étendu (`pageHeader`/`sectionTitle`/`statCard`/`banner`/`searchToolbar`/`filterChip`/`countNote`) ; dashboard en cartes + détails techniques repliés ; `/docs` restylée ; en-têtes standard + recherche/filtres `data-filter-*` sur PNJ/Quêtes/Stories/Dialogues ; `panel.js` `initFilters()`/`initDrawer()`. **Stratégie sans casse** : tous les noms de classes CSS et sous-chaînes HTML asserties conservés → 154/154 tests verts à chaque étape. **#46 (cette fenêtre)** : nouveau paquet `panel.content` — `ContentWorkspace` (accès FS **whitelisté** `quests/*.yml`+`stories/*.yml` uniquement, hash SHA-256 de version, écriture `tmp`+`ATOMIC_MOVE`, refus `EXISTS`/`CONFLICT`/`READONLY`, **dégradation gracieuse en lecture seule** — jamais de `chmod`/`sudo`) ; `MiniYaml` (lecteur minimal pour le round-trip) ; `QuestYaml`/`StoryYaml` (émetteur déterministe = forme exacte de `QuestDefinitionParser`/`StoryDefinitionParser` + `roundTripProblems` : émettre→relire→ré-émettre→égalité, motif `DialogueDefinitionEditor` #82) ; `QuestDraft`/`StoryDraft` (mêmes champs que le moteur, **pas** un 2e modèle) ; `Descriptors` (7 objectifs + 4 récompenses **réels**, champs adaptés) ; `RefData` (ids quêtes/PNJ connus du dernier relevé agent + mondes + listes curées entités/matériaux) ; `Diagnostic` ERROR/WARNING/INFO + `QuestValidator`/`StoryValidator` (contrôles structurels calqués sur le parser + cohérence de référence) ; `TextDiff` ; `ContentEditorPages` (formulaire guidé multi-sections **sans JS**, boutons `_action` add/del/move, selects en `<datalist>`, aperçu YAML+diff, mode lecture seule explicite). Routes `PanelApp` `/quests/new|edit|save` + `/stories/new|edit|save` (session + **`QUEST_CONTENT_WRITE`/`STORY_CONTENT_WRITE`** + CSRF sur `save` + audit `*.content.write`). `PanelConfig.contentRepoDir` (env `PLUGADMIN_CONTENT_DIR` / prop `content.repo-dir` ; absent → lecture seule). **Aucun déploiement, aucun redémarrage Minecraft** (zéro changement plugin/agent). Tests : `ContentYamlRoundTripTest` (5), `ContentEditorPagesTest` (11 bout-en-bout HTTP : rendu guidé+CSRF+datalists, anon→login, ajout d'objectif par round-trip serveur, enregistrement source+ré-ouverture, refus si erreurs, conflit de hash, id traversant refusé, CSRF 403, lecture seule, story ordonnée, RBAC) → `:control-panel:test` **169/0**, `:test` inchangé, `./gradlew build` vert. Docs : `control-panel/ROADMAP.md`, `SECURITY.md`, `CONFIGURATION.md`, `current_state.md`, `RPGQUEST_BIBLE.md`. **Restant** : validation navigateur (rendu réel + chemin principal #46) `PENDING MANUAL VALIDATION` ; déploiement AWS non lancé (décision propriétaire) ; V2 #46 (lignes guidées prérequis/variables, duplication, rechargement des champs au changement de type, action agent `quest.definition.validate`) ; #92 pages Agents/placeholders. **#92 et #46 non fermées, aucun merge.** | PARTIAL | [2026-09-08_2124_refonte-ux-controlpanel-et-editeur-quetes-92-46.md](2026-09-08_2124_refonte-ux-controlpanel-et-editeur-quetes-92-46.md) |
| 2026-09-09 | **Déploiement AWS de #92 (refonte UX) + #46 (éditeur guidé) et activation réelle de l'éditeur.** Le code était committé/poussé mais jamais déployé : `plugadmin.lodylands.com` servait encore l'ancien PlugAdmin sombre (release #49 du 2026-09-08 20:10). **Audit** : branche `feat/control-panel-admin-tools` propre, HEAD `498d46e`, commits `9202aad`/`0e529c5`/`bd1f20f`/`498d46e` présents. **Deux obstacles à l'écriture** identifiés et traités séparément : (1) DAC — `plugadmin` (uid 999) n'avait que `r-x` sur `src/main/resources/{quests,stories}` ; (2) bac à sable systemd — `ProtectSystem=strict` rend `/srv` en lecture seule pour le service quels que soient les droits. **Actions** : `apt-get install acl` (absent) ; **ACL ciblées** `setfacl -m u:plugadmin:rwx` + `-d` sur les 2 dossiers uniquement, `rw` sur les `*.yml` existants, `-d -m g:ubuntu:rwx` pour garder l'accès du propriétaire du dépôt aux fichiers créés par le panel ; `PLUGADMIN_CONTENT_DIR=/srv/rpgquest/repo/src/main/resources` ajouté à `/etc/plugadmin/plugadmin.env` (hors Git, backup `.bak-20260909`) ; **drop-in** `/etc/systemd/system/plugadmin.service.d/10-content-workspace.conf` avec `ReadWritePaths=` limité aux 2 dossiers ; `daemon-reload` ; `scripts/plugadmin/deploy.sh` (installDist + sauvegarde `/opt/plugadmin/releases/20260909-060511` + restart + health). **Vérifs réelles** : jar déployé **byte-identique** à un build frais du working tree (SHA-256 `8ed58f74…`, 99 classes) → contient `Icons`/`ContentEditorPages`/`panel.content.*` et les routes `/quests/new|edit|save`+`/stories/new` ; `Layout.class` porte les tokens du thème clair ; `plugadmin.service` `active`, `NRestarts=0`, drop-in chargé, **0 ERROR** au journal ; `/health` ONLINE local + public ; `/login` public sert la refonte #92 (sprite SVG, `brand-mark`, tokens `--sidebar:#1d2534`/`--primary:#2f6df6`, CSP inchangée) ; routes `/quests/new`, `/quests/edit/<slug>`, `/stories/new`, `/quests/save` → 303 `/login` (présentes, protégées) ; **test d'écriture sous le bac à sable réel** (`systemd-run` répliquant User=plugadmin + ProtectSystem=strict + ReadWritePaths) : écriture/rename/suppression **OK** dans `quests/` et `stories/`, **refusées** (Read-only file system) partout ailleurs — `resources/` parent, repo racine, `config.yml`, `/etc/plugadmin/*.env|*.properties` ; `repo/.env` non lisible. `dig.lodygames.com` 200, `lodylands.com` 200, nginx/dig/lodyland `active`. **VeryGames / Minecraft non touché** (aucun changement plugin/agent, aucun restart). **Rollback** : `scripts/plugadmin/rollback.sh app` (restaure `releases/20260909-060511`, ancien jar sans #92/#46) ; config réversible (rm drop-in + restore `.env.bak` + `setfacl -b`). **PENDING** : validation navigateur **authentifiée** (mot de passe owner non détenu) — rendu #92 + chemin complet création de quête #46. **#92/#46 non fermées, aucun merge.** | DONE | [2026-09-09_0607_deploiement-aws-92-46-activation-editeur.md](2026-09-09_0607_deploiement-aws-92-46-activation-editeur.md) |
| 2026-09-09 | **#92 — Home à tuiles (launcher) + socle Bootstrap 5 local + déploiement AWS.** La page d'arrivée devient un **launcher à grandes tuiles** (esprit DirectAdmin) : le Dashboard n'est plus l'accueil, il devient une tuile et reste sur `/dashboard` ; connexion et `/` redirigent vers **`/home`**. **Bootstrap 5.3.8 + Bootstrap Icons 1.13.1** introduits comme socle UI, **servis 100 % localement** (`/assets/bootstrap/`, `/assets/bootstrap-icons/` — fichiers vendored dans `src/main/resources/assets/`, MIT, cf. `VENDOR_ASSETS.md`), **aucun CDN**, **CSP inchangée** (`default-src 'self'` couvre script/style/police même origine ; bundle sans `eval`/`new Function`). Nouveau handler générique `PanelApp#handleAsset` (contexte `/assets/`, chemin validé anti-traversal, content-type par extension, `ETag`+`304`, `Cache-Control: max-age=3600`) — remplace `handleAssetPanelJs`. **`plugadmin.css`** = design system #92 extrait de `Layout` **verbatim** + **pont `--bs-*`** vers les tokens PlugAdmin (Bootstrap prend l'identité du panel, pas de bleu/blanc) + styles Home ; `Layout` charge `bootstrap.min.css` + `bootstrap-icons.min.css` + `plugadmin.css` et `bootstrap.bundle.min.js` + `panel.js` globalement (fin des `<script>` par page). **`Icons.icon()`** rend désormais `<i class="bi bi-…">` (table 41 noms → Bootstrap Icons) — **signature inchangée**, les ~40 appels existants fonctionnent tels quels ; `sprite()` neutralisé. **`HomePages`** : tuiles groupées (Vue d'ensemble / Gestion du jeu / Ressources / Administration), grille Bootstrap `row-cols-1/md-2/lg-3/xl-4`, chaque tuile activée = vrai `<a>` (toute la carte cliquable, focus clavier), tuiles « à venir » non cliquables (`aria-disabled`), **filtrage par permission** ; badges synthétiques (joueurs en ligne, N PNJ/quêtes/stories/dialogues + alertes, état serveur ONLINE/OFFLINE) tirés du **dernier relevé agent** via `AgentPages#homeSummary` — **aucune requête déclenchée** ; place réservée pour une recherche globale (`input` désactivé). Sidebar : entrée **« Accueil »** en tête. **#46** (`/quests/new`…) et **#49** (`/docs`) non touchés, non-régression vérifiée. Tests : `HomeLauncherTest` (5), `AssetsBootstrapTest` (5, dont CSP identique + traversal + 304), +2 assertions post-login `/dashboard`→`/home` → `:control-panel:test` **179/0** ; `:test` inchangé ; `./gradlew build` vert ; smoke local de la distribution (login→/home, 6 assets 200, toutes pages 200). **Déploiement AWS** `scripts/plugadmin/deploy.sh` : jar déployé **byte-identique** à HEAD (`faa6defc…`, contient HomePages + assets Bootstrap), `plugadmin.service` `active` `NRestarts=0`, **0 ERROR** au journal, `/health` ONLINE local+public, `/`→303 `/home`, 7 assets publics 200 (bons content-types), `/login` ne lie que `/assets/…` (0 CDN), CSP identique, ETag→304, traversal 400/404, `dig.lodygames.com`+`lodylands.com` 200. **VeryGames non touché.** Rollback : `scripts/plugadmin/rollback.sh app` (release `20260909-070146`). **PENDING** : validation navigateur **authentifiée** (mot de passe owner non détenu) — rendu Home + clic des tuiles + mobile. **#92 non fermée, aucun merge.** | DONE | [2026-09-09_0702_home-tuiles-socle-bootstrap-92.md](2026-09-09_0702_home-tuiles-socle-bootstrap-92.md) |
| 2026-09-09 | **#93 — toasts + centre de notifications + page `/actions` filtrable + déploiement AWS.** Remplace les gros blocs « Actions récentes » des pages métier (`/players` `/npcs` `/quests` `/stories` `/dialogues`) par un feedback moderne. **Aucune 2e base** : source de vérité = table `agent_action` (`AgentStore`, #51). **`ActionView`** (pur) : mapping domaine (`player.*`→Joueurs, `npc.*`→PNJ, `quest.*`→Quêtes, `story.*`→Stories, `dialogue.*`→Dialogues, `item.*`→Items, `server.*`/`agent.*`→Serveur/Agents, sinon Autres), regroupement statut (`SUCCESS`→Succès ; `PENDING`/`DELIVERED`→En cours ; `FAILED`/`REJECTED`/`EXPIRED`→Échec), libellé humain (catalogue), cible, résultat court, heure relative, badges Bootstrap (icône + texte), fragments `toastHtml`/`notifItemHtml`/`errorToastHtml` partagés serveur+JSON. **Toasts Bootstrap** : mutation → redirection `…&toast=<id>` (fin de `&ok=1`) ; `handleBusinessPage` rend **un** toast (plus de bandeau). SUCCESS auto-dismiss 6 s ; PENDING/FAILED persistants ; un toast PENDING **mis à jour en place** par `panel.js` quand l'action se résout (pas de spam) ; toast d'erreur immédiat sur `&err=`. `aria-live`, `role`, `btn-close`. **Centre de notifications** (`NotificationCenter`) : cloche `bi-bell` dans la topbar (placeholder `%NOTIF%` dans `Layout`, rendu si `DIAGNOSTICS_READ`), dropdown Bootstrap des 8 dernières actions (icône domaine, libellé, cible, statut, heure, résultat), **badge** = actions en cours + échecs < 24 h (jamais les succès historiques), pied « Voir toutes les actions » → `/actions`. **Page `/actions`** (`ActionsPages`, pur) : recherche GET (type, libellé, domaine, cible, `result_message`/`value`, `created_by`, params) **combinable** avec les filtres ; `nav-pills` statut (Tous/Succès/En cours/Échec) ; puces domaine (seulement ceux présents) ; compteur ; **table Bootstrap desktop** (`d-none d-md-block`) + **cartes mobile** (`d-md-none`) ; **pagination 25/page**. Détail `/actions/<id>` (type technique, params — `value` masqué, cible, timestamps, livraisons, résultat, corps brut `result_json`, acteur) ; id validé, inconnu → 404. Permission **`DIAGNOSTICS_READ`** partout (anonyme→303 `/login`, JSON→401), **pas de bypass OWNER**. `/agents` : la grosse table → « Activité récente » compacte (3 lignes) + lien. `/agents/actions.json` **enrichi** (`label`, `domain`, `group`, `target`, `resultShort`, `age`, `notifHtml`, `badge`). `panel.js` : `initToasts()` + `initNotifications()` — polling léger **20 s** au repos, **3 s** tant qu'une action est en cours, garde-fou de durée, `same-origin` ; suppression de `attach`/`renderRows`/`tableHasPending`. **Pas de WebSocket** (MVP). `Icons` : + `bell`/`history`/`box`. `plugadmin.css` : toasts, dropdown, `/actions` (table + cards), détail. Tests : `ActionsCenterTest` (10 : disparition des blocs, toasts SUCCESS/FAILED/PENDING/erreur, cloche+menu+badge+« voir plus », `/actions` filtres statut/domaine + recherche + combinaison, pagination, détail + 404, auth + permission sans bypass OWNER) + `AgentActionsRefreshTest` **réécrit** (#65→#93) → `:control-panel:test` **189/0** ; `:test` inchangé ; `./gradlew build` vert ; smoke local OK. **Déploiement AWS** `scripts/plugadmin/deploy.sh` : jar déployé **byte-identique** à HEAD (`f4773ef4…`, contient ActionView/ActionsPages/NotificationCenter), `plugadmin.service` `active` `NRestarts=0`, **0 ERROR**, `/health` ONLINE local+public, `/actions`+`/actions/x`+`/actions?status=failed`→303 `/login`, `/agents/actions.json`→401, `panel.js` servi avec `initToasts`/`initNotifications` sans `tableHasPending`, **CSP inchangée**, `dig.lodygames.com`+`lodylands.com` 200. **VeryGames non touché.** Rollback : `scripts/plugadmin/rollback.sh app`. **PENDING** : validation navigateur **authentifiée** (mot de passe owner non détenu) — toasts visuels, cloche, `/actions`, mobile. **#93 non fermée, aucun merge.** | DONE | [2026-09-09_0803_toasts-notifications-actions-93.md](2026-09-09_0803_toasts-notifications-actions-93.md) |
| 2026-09-09 | **fix #93 — panneau de notifications illisible + toast non affiché + redéploiement AWS.** **BUG 1** (dropdown de la cloche trop étroit sur desktop, texte cassé mot par mot) : `NotificationCenter` passe du `dropdown` Bootstrap à un **off-canvas** (`offcanvas-end notif-panel`), largeur `--bs-offcanvas-width: 420px` desktop / `92vw` sous 576px, en-tête + corps scrollable + pied fixe « Voir toutes les actions » ; lignes de notif en wrapping **normal** (`break-word`, plus `anywhere`), résultat borné à 90 caractères (détail complet sur `/actions`). **BUG 2** (`/dialogues` → « Rafraîchir » → `toast=<uuid>` dans l'URL mais aucun toast) : audit complet de la chaîne — le markup serveur du toast **est** rendu (vérifié en repro local `/dialogues`), le point faible est le client. **Cause racine la plus probable** : `panel.js` (seul asset pré-#93) servi avec `Cache-Control: max-age=3600` **sans versionnage** → juste après le déploiement, le navigateur sert l'ancien `panel.js` (sans `initToasts`) depuis son cache pendant ~1h. **Correctifs** : (a) nouveau `web/Assets` — `Layout` construit les URLs d'assets avec `?v=<8 hex du SHA-256 du contenu>` (`bootstrap(.bundle).min.(css|js)`, `bootstrap-icons.min.css`, `plugadmin.css`, `panel.js`) ; un fichier modifié = nouvelle URL, plus de version périmée en cache ; le handler d'asset ignore déjà la query string (ETag/304 conservés). (b) `panel.js` `showToast()` : `bootstrap.Toast.getOrCreateInstance(el).show()` si dispo, **sinon repli manuel** (`classList.add("show")` + auto-dismiss) — le feedback n'est jamais avalé ; `data-toast-ready` anti-double ; `upgradeToasts()` au `load`. (c) CSS `#toast-root` sous la topbar (`top:64px`, `z-index:1200`). Tests : `NotificationsBugfixTest` (6 : off-canvas + largeurs CSS ; assets `?v=` + ordre scripts + query ignorée ; `showToast` robuste ; **toast rendu pour dialogue/npc/player/quest/story/item** ; toast success/failed ; `/actions` + filtres non-régressés) ; `AssetsBootstrapTest` + `ActionsCenterTest` ajustés (`?v=`, `offcanvas`). `:control-panel:test` **195/0** ; `:test` inchangé ; `./gradlew build` vert ; smoke local (repro `/dialogues` → markup toast présent, off-canvas, cache-bust, 0 asset 404, 0 erreur serveur). **Déploiement AWS** `scripts/plugadmin/deploy.sh` : jar déployé **byte-identique** à HEAD (`7ca574d4…`, contient `web/Assets`), `plugadmin.service` `active` `NRestarts=0`, **0 ERROR**, `/health` ONLINE local+public, `plugadmin.css?v=2d6b43dd` servi (cache-busting live), les 6 assets 200 (+ avec `?v=test`), `panel.js` servi avec `showToast`/`upgradeToasts`, `/` → 303 `/home`, `/home`//`/dialogues`//`/npcs`//`/actions` → 303 `/login`, **CSP inchangée**, `dig.lodygames.com`+`lodylands.com` 200. **VeryGames non touché.** Rollback : `scripts/plugadmin/rollback.sh app` (release `20260909-083242`). **PENDING** : validation navigateur **authentifiée** (mot de passe owner non détenu) — les 7 points de la « validation finale obligatoire » (toast visible sur `/dialogues` + une 2e page, panneau lisible desktop + mobile, pas de casse mot par mot, pas d'asset 404, pas d'erreur JS). **#93 non fermée, aucun merge.** | DONE | [2026-09-09_0833_fix-93-toast-invisible-notif-panel.md](2026-09-09_0833_fix-93-toast-invisible-notif-panel.md) |
| 2026-09-09 | **#89 — refonte ciblée de la page `/npcs` + déploiement AWS.** Philosophie : **LISTE = synthèse · CLIC PNJ = détail · CLIC ACTION = formulaire** ; tous les détails/diagnostics/formulaires masqués par défaut. **Toolbar catalogue compacte** : `btn-sm` (« ↻ Catalogue RPGQuest » `btn-outline-primary`, « ↻ Citizens » `btn-outline-secondary`, « + Nouvelle définition PNJ » qui déplie un `collapse`) — fin des grandes cartes vides. **Recherche** = vrai `input-group` Bootstrap (icône dans son `.input-group-text`, plus de chevauchement) alignée aux filtres (Tous/Liés/Non liés/Warnings/Erreurs), wrap/scroll propre sur mobile. **Liste = `accordion` Bootstrap** (`data-bs-parent` → un seul PNJ ouvert à la fois) : en-tête = nom + id logique discret + 2-3 badges d'état (`définition`/`sans définition`/`Citizens #id`/`à lier`/`orphelin`/`désactivé`) + chevron. **Détail structuré** : sections **Identité / Citizens / Contenu / Diagnostics / Actions** ; labels humains (« Donneur de quête » ; `quest_giver` conservé en `<code>` secondaire) ; diagnostics dans des `alert` `danger`/`warning`/`info` différenciées + `ID diagnostic : <code>CODE</code>` discret ; boutons « Ouvrir le dialogue » / « Ouvrir la quête ». **Actions = boutons** qui déplient chacun un `collapse` avec leur formulaire (Créer la définition / Modifier / Attribuer une quête / Lier un PNJ Citizens / Créer le PNJ Citizens) — rien affiché d'emblée. **Formulaire « Créer/Modifier la définition » repensé** : sections Identité/Contenu/État, `form-label`+`form-text`, **select Dialogue** alimenté par `dialogue.list`, **select Rôle** (« Donneur de quête »), `form-switch` « PNJ actif », boutons Annuler/Créer. **BUG DE CONTEXTE corrigé** (capture : fiche « Guide » mais valeurs « Bûcheron Bob » créant `woodcutter_bob`) : audit → restauration d'état de formulaire / autofill navigateur sur des `name` identiques ; correction défense en profondeur — dans une fiche, `npc_id` **et** `npc_ctx` sont des champs **cachés** = l'id de la fiche (plus d'input texte d'id), `autocomplete="off"`, pré-remplissage avec les valeurs **du PNJ courant** ; **garde-fou serveur** `PanelApp#createAgentAction` : si `npc_ctx` ≠ `npc_id` validé → refus (audit `DENIED`, `&err=Contexte de PNJ incohérent`), **aucune action créée**. Toasts #93 conservés (plus de gros message local). Non-régression : refresh, recherche, filtres, create/update, `npc.citizens.link`/`.create`, `quest.giver.set`, diagnostics, permissions, CSRF, audit — tous conservés ; registre canonique en repli. Icône `refresh`. Tests `NpcsCatalogTest` (13) : accordion+synthèse+sections+diagnostics+labels humains ; toolbar compacte + input-group ; liaison Citizens ; **formulaire de création contextualisé** (`npc_id`/`npc_ctx` cachés, aucune valeur d'un autre PNJ, `autocomplete=off`) ; **serveur refuse `npc_ctx=guide` + `npc_id=woodcutter_bob`** (0 action). `:control-panel:test` **198/0** ; `:test` inchangé ; `./gradlew build` vert ; smoke local (accordion, sections, pas de fuite de form entre fiches, 0 asset 404, 0 erreur). **Déploiement AWS** `scripts/plugadmin/deploy.sh` : jar déployé **byte-identique** à HEAD (`4f5393ee…`), `plugadmin.service` `active` `NRestarts=0`, **0 ERROR**, `/health` ONLINE local+public, `/npcs` → 303 `/login`, `plugadmin.css` servi avec les classes #89, **CSP inchangée**, `dig.lodylands.com`+`lodylands.com` 200. **VeryGames non touché.** Rollback : `scripts/plugadmin/rollback.sh app` (release `20260909-090615`). **PENDING** : validation navigateur **authentifiée** (mot de passe owner non détenu) — vue compacte, recherche lisible, accordion, détail structuré, aucun form d'un autre PNJ, formulaire repensé, mobile. **#89 non fermée, aucun merge.** | DONE | [2026-09-09_0906_refonte-npcs-89.md](2026-09-09_0906_refonte-npcs-89.md) |
| 2026-09-09 | **#89 (suite) / #49 — refonte UX généralisée `/quests` `/stories` `/dialogues` (+ audit `/players`) + registre `DiagnosticHelp` + 4 fiches de dépannage `/docs`.** La philosophie `/npcs` (liste = synthèse, clic = détail, clic action = formulaire) est portée aux 3 autres pages métier : chaque liste devient un **accordéon Bootstrap** (`npc-accordion`/`npc-item` réutilisés, `data-bs-parent` → un seul ouvert), détail en sections repliées — Quêtes : Général/Donneur/Prérequis/Objectifs/Récompenses/Diagnostics/Actions ; Stories : Identité/Chaîne de quêtes/Diagnostics/Actions ; Dialogues : Résumé/PNJ/Quêtes/Diagnostics/**Graphe (`<details>` NON ouvert)**/Actions. Toolbars de rafraîchissement **compactes** partout (`listCatbar`+`compactRefresh(returnPath)`, `btn-sm` : `[↻ Quêtes][↻ PNJ]`, `[↻ Stories][↻ Quêtes]`, `[↻ Dialogues][↻ Quêtes]`) — fin des grandes cartes vides. Recherche `input-group` Bootstrap + puces de filtre alignées (`listControls`). `/players` (peu dense) : toolbar compacte + recherche au-delà de 6 joueurs (`tr[hidden]`). **`DiagnosticHelp`** (`panel.web`, nouveau) = **registre unique** de 21 entrées : chaque code moteur (PNJ `NpcCatalog`, dialogues `DialogueCatalog`) + les vérifications de référence **calculées côté panel** (`QUEST_PREREQ_UNKNOWN` = prérequis absent du catalogue, `QUEST_GIVER_UNKNOWN` = `giverId` absent de `npc.list`, `STORY_QUEST_UNKNOWN` = quête de chaîne absente, `DIALOGUE_LOAD_ISSUE`/`DIALOGUE_DECLARED_MISSING`) → **1. problème FR clair · 2. conséquence · 3. action · 4. lien vers une ANCRE PRÉCISE de `/docs` · 5. code technique en secondaire**. `render()` = `alert` Bootstrap colorée selon la sévérité (`danger`/`warning`/`info`), bouton « Comment corriger ? » (`bi-question-circle`) + « Corriger maintenant » (`npcFixButton`, déplie le bon formulaire) côté PNJ. Ancre **dérivée du titre** (`Markdown.slug(title)`) ; `Markdown.slug` **replie désormais les accents** (NFD) → `#prerequis-inconnu`. Terminologie §18 : `tagué`/`binding`/`giver`/`orphan`/`raw`/`namespaced` bannis des messages principaux (n'existent que dans le `code technique`). **#49** : 4 fiches `pnj-depannage`/`dialogues-depannage`/`quetes-depannage`/`stories-depannage` (whitelist `_index.txt`, embarquées dans le jar), format *Ce que cela signifie / Pourquoi corriger / Comment corriger / Vérification / Référence technique*, titres de section **alignés** sur `DiagnosticHelp`. Anciennes méthodes supprimées : `renderQuestCard`/`renderStoryCard`/`renderDialogueCard`/`dialogueDiagnostics`/`diagAlert`. Tests : `DiagnosticHelpTest` (7 : registre ↔ fiches ↔ `_index.txt` ↔ ancres ; message humain ; code secondaire ; bouton d'aide ; terminologie interdite absente ; code inconnu → repli propre ; `Level.of`) + assertions accordéon / toolbar compacte / `input-group` / diagnostic humanisé + ancre doc dans `Quests`/`Stories`/`DialoguesCatalogTest` ; `NpcsCatalogTest` migré. `:control-panel:test` **205/0** ; `:test` inchangé ; `./gradlew build` vert. **Déploiement AWS** `scripts/plugadmin/deploy.sh` : release `20260909-095349`, `plugadmin.service` `active` `NRestarts=0`, **0 ERROR**, `/health` ONLINE local+public, `/npcs`//`/dialogues`//`/quests`//`/stories`//`/docs`//`/players` → 303 `/login`, **CSP inchangée**, `plugadmin.css?v=bedb0b2d` 200, `dig.lodygames.com`+`lodylands.com` 200 ; jar contient `DiagnosticHelp` + 4 `*-depannage.md`. **VeryGames non touché.** Rollback : `scripts/plugadmin/rollback.sh app`. **PENDING** : validation navigateur **authentifiée** (mot de passe owner non détenu) — listes compactes, recherche, accordéons, diagnostics + lien ancre, graphe replié, mobile. **#89 et #49 non fermées, aucun merge.** | DONE | [2026-09-09_0930_control-panel-ux-metier-diagnostics-89-49.md](2026-09-09_0930_control-panel-ux-metier-diagnostics-89-49.md) |
| 2026-09-09 | **Polish / bugfix #49/#89 après validation visuelle de la nouvelle UX.** Trois défauts précis. **(1) Titre en double sur les fiches `/docs`** : `DocsPages` rend déjà le titre de fiche dans son propre `<h1>` ; `Markdown.render` ré-affichait le `# …` d'ouverture du corps (parfois une variante plus courte → deux titres différents à la suite). Nouveau `Markdown.stripLeadingH1()` (pur, testé, null-safe, conservateur) : retire **uniquement** le premier titre s'il est de niveau 1 et en tête ; `##`/`###` et un éventuel second `#` plus bas conservés ; `render()` l'appelle avant le parsing. Vérifié sur les **13 fiches**. **(2) Bloc de code / bouton « Copier »** (« grand rectangle sombre vide à droite, Copier perdu dedans ») : cause = `<svg class="ic"><use href="#i-copy">` — sprite retiré à la migration Bootstrap #92 → `<svg>` sans contenu ni dimensions = taille intrinsèque **300×150** dans le bouton `inline-flex`. `renderCodeBlock` : bouton « Copier » = **texte seul**, compact, en tête du wrapper `.doc-cmd`, juste avant `<pre><code>`. CSS : `.doc-copy{position:absolute;top:7px;right:7px;z-index:2;white-space:nowrap;padding:4px 9px}` ; `.doc-cmd pre{padding:14px 72px 14px 16px;overflow-x:auto}` (gouttière droite → le texte ne passe jamais sous le bouton, scroll horizontal seulement si commande longue, OK mobile) ; ancienne règle en double `.codeblock .doc-copy,.doc-cmd .doc-copy` **supprimée**. **(3) Action « Créer la définition » affichée deux fois** dans le détail PNJ (diagnostic `[Créer la définition] [Comment corriger ?]` puis section **Actions** qui réaffiche `[Créer la définition]`) : `npcFixButton` → `npcFixTarget` renvoyant `{cible, libellé}` (libellé aligné sur le toggle : « Créer la définition » / « Modifier » / « Lier un PNJ Citizens ») ; `renderNpcAccordionItem` mémorise les cibles déjà exposées par un « Corriger maintenant » et **les retire** des toggles de la section Actions ; la section **Actions disparaît si vide** ; les `actionCollapse` (formulaires masqués) restent **toujours** rendus pour que le bouton du diagnostic les ouvre. Généralisation : `/quests` `/stories` `/dialogues` n'émettent aucun CTA de diagnostic → aucun doublon possible (vérifié). Structure `DiagnosticHelp` (titre humain / explication / conséquence / à faire / boutons / code technique) inchangée, pas de jargon réintroduit. Tests : `MarkdownTest` (+2 : `stripLeadingH1` unitaire + conservateur ; bouton Copier compact sans `<svg>`), `DocsPagesTest` (+2 : les 13 fiches ont **un seul** `<h1>` de header et **plus** de `<h1 id=` issu du Markdown ; bloc code — markup dans le même wrapper + CSS `.doc-copy` absolute / `.doc-cmd pre` overflow / ancienne règle absente), `NpcsCatalogTest` (+1 : `BINDING_NO_DEFINITION` = **un seul** bouton d'ouverture du formulaire de création — `aria-controls` unique —, section « Actions » masquée, formulaire caché conservé, terminologie interdite absente du message principal). `:control-panel:test` **210/0** ; `:test` inchangé ; `./gradlew build` vert. **Déploiement AWS** `scripts/plugadmin/deploy.sh` : release `20260909-101733`, jar **byte-identique** au build (`955d5055…`), `plugadmin.service` `active` `NRestarts=0`, `/health` ONLINE local+public, `/docs`//`/docs/pnj-citizens`//`/npcs`//`/quests`//`/stories`//`/dialogues` → 303 `/login`, **CSP inchangée**, CSS `.doc-copy`/`.doc-cmd pre` corrigés servis + ancienne règle absente, `dig.lodygames.com`+`lodylands.com` 200. Un `WARNING handler_error path=/login stream closed` = `curl -I` (HEAD) client qui ferme, bénin. **VeryGames non touché.** Rollback : `scripts/plugadmin/rollback.sh app`. **PENDING** : validation navigateur **authentifiée** (mot de passe owner non détenu) — titre unique + blocs code propres sur `/docs`, un seul « Créer la définition » sur `/npcs` Guide, une fiche Dialogues/Quêtes/Stories. **#49 et #89 non fermées, aucun merge.** | DONE | [2026-09-09_1005_polish-docs-titres-codeblock-cta-49-89.md](2026-09-09_1005_polish-docs-titres-codeblock-cta-49-89.md) |
| 2026-09-09 | **#49 — réécriture de la fiche `/docs` de référence des commandes.** L'ancienne fiche affichait de gros blocs listant plusieurs familles de commandes (`/rpgadmin npc … | /rpgadmin spawn … | …`) derrière **un seul** bouton « Copier » — pas une documentation utile. **Audit du code réel avant rédaction** (§13) : `RpgAdminCommand` (2405 l.), `RPGQuestCommand`, `QuestCommand`, `DialogueCommand`, `CustomItemCommand` — sous-commandes, arg parsing exact, messages d'usage, permissions (`rpgquest.admin.world`, `rpgquest.admin`, `rpgquest.admin.debug`, `rpgquest.item`), branches console (`quest`/`story`/`player`/`guide`). **Nouvelle fiche** : table « En un coup d'œil » (*Je veux… → commande*, répond à « quelle commande ? » §16), puis **8 catégories `##`** (Joueurs / PNJ / Quêtes / Stories / Mondes / Portails & Waystones / Administration serveur / Outils builder & avancés) et **37 sections `###`**, une par commande / sous-commande : **nom humain d'abord** (§12) + `` `id` `` technique, description, **Syntaxe**, **Paramètres**, **Exemple(s)**, **Résultat attendu**, **Permission** / **Attention**. **78 blocs `` ``` ``, 78 boutons « Copier », chacun UNE seule commande exécutable** (§9) — plus aucun bloc n'énumère d'alternatives avec `|`. Exemples avec des IDs de test **valides** (`LoDyMcFly`, `guard`, `first_steps`, `main_story`, `world_hub`, `wild`, `claims`), jamais inventés (§8). `player resetnew` : preview / confirm / console / conséquence détaillés (§6). CSS : sections `###` en cartes légères (bord gauche accent + `<code>` chip), mini-libellés « Syntaxe / Paramètres / … » discrets — **aucune modification du renderer Markdown** (zéro risque XSS / régression). Tags front matter recentrés « référence » (pas de `npc`/`tag`/`quest`/`story`/`complete`/`reset`/`joueur` isolés) pour ne pas déclasser `pnj-citizens` / `quetes` / `joueurs-reset` dans la recherche. Tests : `CommandReferenceSheetTest` (6) — un seul ordre exécutable par bloc ; `data-copy` jamais multi-commande ; 21 commandes majeures ont section + description + syntaxe + exemple ; ancien bloc monolithique absent ; la recherche trouve les commandes **et** `tag npc`→`pnj-citizens` / `quest complete`→`quetes` / `reset joueur`→`joueurs-reset` inchangés ; rendu sûr + placeholders échappés + pas de H1 en double. `:control-panel:test` **221/0** ; `:test` inchangé ; `./gradlew build` vert. **Déploiement AWS** `scripts/plugadmin/deploy.sh` : release `20260909-111244`, jar **byte-identique** au build (`f25c5633…`), fiche embarquée (78 blocs), `plugadmin.service` `active` `NRestarts=0`, `/health` ONLINE local+public, `/docs`//`/docs/rpgquest-commandes`//`/docs?q=npc+tag` → 303 `/login`, **CSP inchangée**, `dig.lodygames.com`+`lodylands.com` 200. Un `WARNING handler_error path=/login stream closed` = `curl -I` (HEAD) client qui ferme, bénin. **VeryGames non touché.** Rollback : `scripts/plugadmin/rollback.sh app`. **PENDING** : validation navigateur **authentifiée** (mot de passe owner non détenu) — table + catégories + un Copier par exemple, sommaire, mobile, recherche. **#49 non fermée, aucun merge.** | DONE | [2026-09-09_1104_fiche-commandes-reference-49.md](2026-09-09_1104_fiche-commandes-reference-49.md) |
| 2026-09-09 | **#38 — dashboard d'observabilité / page `/diagnostics`.** Rend enfin utile la tuile Diagnostics : « qu'est-ce qui ne va pas actuellement sur RPGQuest ? » sans ouvrir PNJ / Dialogues / Quêtes / Stories un par un. **Audit** des diagnostics réels : PNJ (`NpcCatalog` — `BINDING_NO_DEFINITION`/`NO_DEFINITION`/`DIALOGUE_MISSING`/`DUPLICATE_*`/`DISABLED`/`NOT_LINKED`/`GIVER_NO_DIALOGUE`), Dialogues (`DialogueCatalog` — `NEXT_MISSING`/`QUEST_REF_UNKNOWN`/`NODE_UNREACHABLE`/`DIALOGUE_NO_NPC`/`MULTIPLE_NPCS`/`DEFINITION_DIALOGUE_DIVERGES` + `loadIssues`/`declaredButMissing`), Quêtes/Stories (aucun code moteur → vérifs de référence côté panel : `QUEST_PREREQ_UNKNOWN`/`QUEST_GIVER_UNKNOWN`/`STORY_QUEST_UNKNOWN`), Serveur/Agent (heartbeat + `AgentLiveness`). Items/Portails/Claims : **aucune source de données** → domaines non affichés (respect de « seulement ce qui est réellement supporté »). **Nouveau paquet `panel.diag`** : modèle unique `DiagnosticEntry` (code stable, `Severity` ERROR/WARNING/INFO, `Domain` humain, ressource + label, titre/message/conséquence/action humains, ancre `/docs` précise, lien « Ouvrir », `QuickAction` optionnelle, `source`, `observedAt`) — wording repris de `DiagnosticHelp`, **jamais dupliqué** ; interface `DiagnosticProvider` + `DiagnosticContext` (**lecture seule du dernier snapshot agent, aucune requête déclenchée**) ; providers Npc/Dialogue/Quest/Story/Server (les 2 premiers **mappent** les `warnings[]` du moteur, ne re-détectent rien) ; `RefKeys` extrait d'`AgentPages` (partagé) ; `DiagnosticsService` agrège / **dédoublonne** (code + ressource) / **trie** (ERROR → WARNING → INFO puis domaine/ressource/code) + compteurs + domaines présents + fraîcheur. **Page `DiagnosticsPages`** (`DIAGNOSTICS_READ`) : en-tête + cartes synthétiques (Erreurs/Avertissements/Infos/Total), **filtres combinables** gravité + domaine + recherche plein texte (`panel.js#initFilters` étendu pour **plusieurs groupes de puces par scope**, rétro-compatible), cartes compactes humain-d'abord (icône + titre + domaine · ressource + conséquence + **Ouvrir** / **Corriger maintenant** / **Comment corriger ?** + code technique en **dernier**), regroupement au-delà de 4 diagnostics identiques (`<details>` « Voir les N »), **état vide positif** (« Aucun problème détecté »), fraîcheur « Dernière vérification : il y a … ». **« Ouvrir »** = lien profond `/<page>?agent=…&focus=<id>` ; `panel.js#initFocus()` déplie l'élément d'accordéon ciblé (`data-res-id` ajouté aux 4 pages) + scroll + surbrillance ; **« Corriger maintenant »** = même lien + `&fix=create|edit|link` qui déplie en plus le bon formulaire — **aucune correction automatique générique**. **Refresh coordonné** : `[ ↻ Actualiser les diagnostics ]` → `POST /diagnostics/refresh` (CSRF, `DIAGNOSTICS_READ`) enqueue **les 4** relevés `*.list` nécessaires (jamais dix) en une action ; feedback **toast** (#93). **Home** : tuile Diagnostics active (« N erreurs · N avertissements » ou « Tout est en ordre ») ; **Dashboard** : section « État du contenu » (compteurs + « Voir les diagnostics ») ; sidebar active. **#49** : fiche `serveur-depannage.md` (whitelist `_index.txt`, 7 sections format §15) + section « Citizens inactif » dans `pnj-depannage.md`. Tests : `DiagnosticsServiceTest` (7 — agrégation multi-domaines, un de chaque gravité, tri, dédoublonnage, heartbeat manquant → ERROR, monde non chargé, ancres doc ↔ fiches) + `DiagnosticsPageTest` (9 — auth, cartes humaines, code secondaire, filtres combinables + panel.js, regroupement, refresh coordonné (**exactement 4** actions) + toast, CSRF, Home/Dashboard, sidebar) ; `HomeLauncherTest` ajusté. `:control-panel:test` **235/0** ; `:test` inchangé ; `./gradlew build` vert. **Déploiement AWS** `scripts/plugadmin/deploy.sh` : release `20260909-120245`, jar **byte-identique** au build (`c5897313…`), 17 classes `panel/diag/` + `serveur-depannage.md` embarqués, `plugadmin.service` `active` `NRestarts=0`, **0 ERROR**, `/health` ONLINE local+public, `/diagnostics`//`/diagnostics/refresh`//`/home`//`/dashboard`//`/docs/serveur-depannage` → 303 `/login`, **CSP inchangée**, `dig.lodygames.com`+`lodylands.com` 200. **VeryGames non touché.** Rollback : `scripts/plugadmin/rollback.sh app`. **PENDING** : validation navigateur **authentifiée** (mot de passe owner non détenu) — les 11 points de l'énoncé (tuile active, compteur, page, filtre Erreurs, filtre PNJ, recherche « guide », ouvrir un diagnostic, bouton ressource, Comment corriger, refresh → toast, mobile). **#38 non fermée, aucun merge.** | DONE | [2026-09-09_1139_dashboard-observabilite-diagnostics-38.md](2026-09-09_1139_dashboard-observabilite-diagnostics-38.md) |
| 2026-09-09 | **#101 — bug(control-panel) : un PNJ Citizens réel (« Stan ») n'apparaît pas dans `/npcs`.** **Investigation avant fix** (chaîne complète tracée) : Stan **est** dans le registre Citizens runtime (`#7`), **est** dans le payload agent `npc.citizens.list` (action DEV réelle `7d394d2d…` : `#7 Stan`, `linkedNpcId:null`, `availableForBinding:true`, `spawned:false`, `uuid:5e081a06…` — sérialisation liste 1:1, aucune clé par nom, aucun dédoublonnage), **est** dans `control-panel.db`. Il **disparaît au rendu** : `AgentPages.npcs()` construit la liste **uniquement** depuis `npc.list` (`NpcCatalog` = définitions `npcs/*.yml` + liaisons `npc_citizens_bindings` + références quêtes/dialogues), qui **ne lit jamais** le registre Citizens ; `npc.citizens.list` ne servait qu'à la ligne de synthèse et aux noms des PNJ déjà liés. Un PNJ créé en jeu (`/npc create Stan`) sans fiche **ni** binding **ni** référence ne produit aucune ligne → invisible, même si « Citizens : 8 » le compte. Le « 8 = 8 » du ticket est une coïncidence (les 8 de `npc.list` = 6 orphelins + `guard` + `woodcutter_bob` ≠ les 8 Citizens `#0…#7`). **Cause racine : le rendu du Control Panel, pas le registre / l'agent / le stockage / un merge destructif.** **Fix (control-panel uniquement, aucun code plugin/agent, aucun redémarrage VeryGames)** : `AgentPages.npcs()` raccroche chaque PNJ de `npc.citizens.list` sans `linkedNpcId` comme **ligne d'identité physique** (`renderFreeCitizensAccordionItem`) — libellé = **nom en jeu**, sous-titre `Citizens #N` (jamais `undefined`/id technique), badges « sans fiche RPGQuest » + « non lié », `data-filter-cat="unlinked"` (visible sous **Tous** / **Non liés**), `data-filter-text` = nom + `#N` + id numérique + **UUID** (recherche §14), `data-res-id="citizens-<N>"`. Détail : **Identité Citizens** (numéro, UUID, spawné), **Fiche RPGQuest** = « aucune » + phrase humaine, **Diagnostics** = INFO `CITIZENS_ONLY`. Actions facultatives : *Créer une fiche RPGQuest* (`npcDefForm` create, id pré-rempli = nom normalisé `[a-z0-9._-]`, repli `citizens_<N>`) ; *Lier à une fiche existante* = **liaison inverse** (`citizens_id` fixé, `<select>` des fiches prêtes non liées, réutilise `npc.citizens.link`). Compteur `/npcs` = lignes `npc.list` + PNJ Citizens libres. Nouvel état d'affichage `CITIZENS_ONLY` (`npcStateBadge`), entrée `DiagnosticHelp` **INFO** « PNJ du jeu sans fiche RPGQuest » + section `pnj-depannage.md` (ancre `slug(titre)`, tableau des 3 états) ; `NpcDiagnosticProvider` émet un INFO `CITIZENS_ONLY` par PNJ libre lu dans `npc.citizens.list` (sans effet si le relevé est absent — un PNJ libre **n'est jamais** une erreur). **Pas fait (délibéré)** : extension de `NpcCatalog` au registre Citizens (hors périmètre du modèle canonique + impliquerait un redéploiement plugin) ; position/monde du PNJ libre (non exposés par `npc.citizens.list`). Tests : `NpcsCatalogTest` (+2 — PNJ libre listé/cherchable/rattachable ; deux homonymes « Stan » conservés ; borne de la fiche `guard` d'un test existant resserrée), `NpcCitizensPayloadTest` (+1 — la sérialisation agent conserve chaque entrée : PNJ libre + homonymes, aucune clé par nom). `:control-panel:test` vert ; `./gradlew test` + `./gradlew build` verts. **Déploiement AWS** `scripts/plugadmin/deploy.sh` (release + `systemctl restart plugadmin` + `/health`) — VeryGames **non touché**. Rollback : `scripts/plugadmin/rollback.sh app`. **PENDING MANUAL VALIDATION** : `/npcs` live (Stan visible, `Citizens #7`, non lié, filtres, recherche, aucun autre PNJ perdu) + `/diagnostics` (INFO) + navigateur authentifié (mot de passe owner non détenu). Docs : `NPC_FORMAT.md`, `RPGQUEST_BIBLE.md`, `current_state.md`, `docs/control-panel/ROADMAP.md` (Étape 3h), `docs-site/npc.html`, `pnj-depannage.md`, `SERVER_CHANGELOG.md`. **#101 non fermée, aucun merge.** | DONE | [2026-09-09_1245_fix-101-npc-citizens-libre-masque-npcs.md](2026-09-09_1245_fix-101-npc-citizens-libre-masque-npcs.md) |
| 2026-09-09 | **#103 — 502 Bad Gateway sur PlugAdmin après connexion, suite au déploiement #101.** BLOQUANT. `/login` OK mais `/home` (et toute page authentifiée) → 502. **Captures avant correction** : `systemctl status` = `active (running)` depuis 12:50:35, `NRestarts=0`, `ExecMainStatus=0` ; `ss -ltnp` = écoute bien sur `127.0.0.1:8090` ; **process pas mort, pas de crash-loop, pas d'OOM** (`Memory: 108.9M`). Le 502 était **par requête**. **Stacktrace (journalctl)** : `java.time.format.DateTimeParseException: Text '2026-09-09T12:51:33' could not be parsed at index 19` → `Instant.parse` → `AgentStore.readAction:335` → `AgentStore.recentActions:285` → `NotificationCenter.recent:41` → `NotificationCenter.bellHtml:71` → `PanelApp.notifBell:1223` → `finishPage:1214` → `renderPage` → `handleHome`/`handleBusinessPage` → `status=500 ms=28` (répété à chaque `POST /login`+`GET /home` de l'owner entre 12:51 et 13:09). **CAUSE RACINE — PAS une régression du code #101** (`freeCitizens`/`CITIZENS_ONLY`/`NpcDiagnosticProvider`/`DiagSummary` absents de la trace ; panel sain à 12:50:37, cassé à 12:51:33) : pendant la « validation live » de #101, une ligne `agent_action` (sonde `npc.citizens.list`, `created_by='claude-101-validation'`) a été **insérée directement en base** avec `created_at = strftime('%Y-%m-%dT%H:%M:%S','now')` = `'2026-09-09T12:51:33'` — **sans « Z »**, alors que `AgentStore.createAction` écrit toujours `isoSeconds(Instant.now())`. `recentActions` trie `created_at DESC` → cette ligne (la plus récente) est toujours scannée → `Instant.parse` (qui exige un décalage horaire) lève → la cloche de la topbar casse → toute page authentifiée → 500 → 502 nginx. `/login` n'affiche pas la cloche → **le smoke anonyme « `/home` → 303 » ne rend jamais le corps de `/home`, donc n'exécute jamais `notifBell`** → insuffisant par construction pour une page authentifiée. **FIX (1) données** (immédiat, panel re-servi à ~13:09) : `UPDATE agent_action SET created_at='2026-09-09T12:51:33Z' WHERE id='0cd8dd38…' AND created_at='2026-09-09T12:51:33'` — UPDATE ciblé, aucune suppression, valeur d'origine consignée, ligne inspectée avant (`EXPIRED`, sonde jetable) ; vérifié 0 ligne au `created_at` sans « Z ». **FIX (2) durcissement `AgentStore`** (déployé, protection permanente) : `parseTimestamp(raw,context)` tolérant — ISO-8601 canonique **plus** formes héritées sans décalage (`T` ou espace, date seule) interprétées **UTC** ; `null`/blanc/illisible → `null` + `WARNING event=timestamp_unparseable`. `created_at`/`received_at` (colonnes `NOT NULL`, tri/comparaison) → `instantOrEpoch` = `Instant.EPOCH` en dernier recours, **jamais d'exception**. `instantOrNull` (`delivered_at`/`completed_at`) délègue à `parseTimestamp`. **Frontière de sécurité PAR LIGNE** dans `recentActions` : `readAction(rs)` par ligne dans `try/catch` → ligne illisible **ignorée** + `WARNING event=agent_action_row_skipped`, jamais propagée. Règle : donnée agent invalide/incomplète ⇒ diagnostic/WARNING, **jamais crash du serveur web**. **Régression** `PanelHardeningMalformedAgentDataTest` : (1) ligne exacte de #103 (Stan libre, `created_at` sans « Z ») → connexion **owner réelle** + `GET /home /dashboard /npcs /diagnostics /docs /actions` = **200** ; (2) formes héritées variées ; (3) `npc.citizens.list` SUCCESS au `result_json` hérité (Stan sans `uuid`/`availableForBinding`/`spawned`) → `/npcs` 200, `Citizens #7` visible ; (4) `-DpanelProdDbCopy` (ignoré sinon) → `PanelApp` sur une **copie de la vraie `control-panel.db`** (79 lignes), connexion owner, `/home /dashboard /npcs /diagnostics /docs /actions /agents /players /quests /stories /dialogues` = **200**, **Stan (#101) toujours visible**, ligne mal formée réinjectée → `/home` reste 200. **Vérification négative** : `git stash` de `AgentStore.java` → test (1) échoue (`EOFException` = réponse tronquée = 502) ; `stash pop` → vert. `build.gradle.kts` : passe `-DpanelProdDbCopy` au worker (Gradle ne le propage pas), inerte en run normal. `:control-panel:test` **241/0** (1 ignoré = smoke prod-copy) ; `:test` plugin inchangé ; `./gradlew build` vert. **Déploiement AWS** `scripts/plugadmin/deploy.sh --no-build` : release `20260909-131643` (PID 480917), jar **byte-identique** au build (SHA-256 `9a21828f…`), `parseTimestamp`+`instantOrEpoch` dans le bytecode ; `/health` ONLINE local+public ; `/login` → 200 ; `/home`//`/npcs`//`/diagnostics`//`/dashboard` anon → 303 ; `plugadmin.service` `active` `NRestarts=0` `ExecMainStatus=0` ; **0 `ERROR`** depuis le redéploiement ; `control-panel.db` = 79 lignes, **0** au `created_at` sans « Z ». **VeryGames / Minecraft / Citizens / `data.db` / mondes non touchés.** Rollback : `scripts/plugadmin/rollback.sh app` (→ `20260909-125035` = #101 sans durcissement ; ⚠️ réparer alors toute future ligne `created_at` sans « Z »). **PENDING** : confirmation navigateur authentifiée sur le service de prod par l'owner (mot de passe non détenu ; couverture équivalente par le smoke authentifié sur copie de prod). **#101 état** : préservé — Stan (`Citizens #7`, sans fiche / non lié) toujours visible dans `/npcs` (vérifié par le smoke sur données de prod réelles). **Leçon** : ne plus écrire directement en base pour « valider » ; passer par le vrai chemin applicatif ou n'utiliser que des horodatages ISO canoniques. **#103 et #101 non fermées, aucun merge.** | DONE | [2026-09-09_1322_hotfix-103-502-bad-gateway-plugadmin.md](2026-09-09_1322_hotfix-103-502-bad-gateway-plugadmin.md) |
| 2026-09-09 | **#96 — `/players` devient un annuaire d'administration (joueurs hors ligne inclus).** **Audit** : `player.list` = **joueurs connectés** (`getOnlinePlayers()`) — non surchargé. `PlayerDirectory`/`BukkitPlayerDirectory` résolvent déjà nom↔UUID *offline-aware*. Permissions CP = `Permission`/`Role` (OWNER=allOf). Ban : aucun système RPGQuest — API Paper `BanList`. Build : **#27 (`BuildPermissionService`, `rpgquest.build.*`) N'EST PAS fusionnée sur cette branche** (vit sur `feature/27-granular-permissions`) et délègue de toute façon la persistance à LuckPerms → RPGQuest n'a **aucun store de grant persistant**. **SOURCE DE VÉRITÉ** = serveur Paper (`OfflinePlayer` = tous ceux ayant un `playerdata` + connectés) ; **aucune base joueurs propre à PlugAdmin** (n'entrave pas MariaDB #42). **AGENT** (3 types, aucune commande console / SQL / migration) : `player.catalog` — instantané des connectés sur le **thread principal** puis `getOfflinePlayers()` sur un **thread async** ; par joueur `uuid`/`name`/`online`/`hasPlayedBefore`/`firstPlayed`/`lastSeen`/`banned`(+`banReason`)/monde+pos si en ligne ; agrégats `total`/`online`/`offline`/`banned`/`truncated` ; **cap 5000** si aucune limite. `player.ban`/`player.unban` — `Bukkit.getBanList(BanListType.PROFILE)` (`ProfileBanList`), **hors ligne OK**, `kick` si connecté, **raison obligatoire** (validée panel + agent), idempotents (`ALREADY_BANNED`/`NOT_BANNED`) ; cible **résolue en UUID canonique** avant écriture. **CONTROL PANEL** : modèle pur `PlayerCatalog` (parse tolérant + recherche pseudo(ci)/UUID + filtres Tous/En ligne/Hors ligne/Bannis + tri « récent »=connectés puis `lastSeen` desc / « nom » A-Z + **pagination 50**, **tout côté serveur** pour le volume §28). Page réécrite LISTE=synthèse / CLIC=détail(accordion) / ACTION=formulaire : badges **● En ligne** (`text-bg-success`) / **○ Hors ligne** (`text-bg-secondary`) / **Banni** (`text-bg-danger`) — texte, pas que la couleur ; sections **Identité** (pseudo principal, UUID [Copier] en détail seulement, 1re/dernière connexion en relatif) / **Activité** (monde+pos si en ligne) / **RPGQuest** (liens `/quests?player=` `/stories?player=` `/actions?domain=players` — jamais un dump) / **Droits** (droit de construction : **non géré** + explication, **sans faux interrupteur**) / **Modération** (statut ban + raison) / **Actions**. Toolbar « Actualiser » (toast #93), recherche `input-group` GET, puces de filtre = liens serveur (puce active marquée), pager Précédent/Suivant. **Capacité par action §15** : ban/unban/variables/reset = badge « hors ligne OK » ; **« donner un objet » = « en ligne uniquement »** → si hors ligne : « Indisponible : le joueur est hors ligne » **sans bouton**, jamais de faux succès §23. Reset = workflow existant (aperçu→confirmation, zone danger). Nouvelles permissions **`PLAYER_MODERATE`** (ban/unban — OWNER seul ; `READ_ONLY`/`TESTER`/`CONTENT_EDITOR` ne voient/appellent pas) + **`PLAYER_BUILD_WRITE`** (posée, non fonctionnelle). Ban : CSRF + `confirm` + `reason` obligatoires, cible = UUID caché. Audit `agent.action.create` (acteur, UUID+`reason` dans `safeParams`, `rid`) + résultat dans `/actions` domaine Joueurs. **`DIAGNOSTICS #38` : rien ajouté** (offline = normal). **BUILD RIGHT : BLOQUÉ (livré = architecture + permission + fiche « non géré », PAS de fonctionnalité)** — motif : #27 non fusionnée + pas de store de grant persistant ; décision §19 : ne pas bricoler, documenter le blocage. Bans temporaires : hors périmètre. **Tests** : `PlayerCatalogTest` (8 — parse/tri/recherche/filtres/pagination), `PlayersPageTest` (8 — HTTP **authentifié** : badges, nom≠UUID en en-tête, sections, pas de champ `command`, recherche+filtres serveur, ban validé CSRF+confirm+raison+UUID+toast, unban si banni, give en-ligne-uniquement, `?player=` pré-ouvre, pagination>50), `AgentActionExecutorTest` (+5). Run précédent : plugin **1214/0** (29 ignorés MockBukkit inchangés), control-panel **257/0**, web-api **30/0** ; `./gradlew build` vert (re-run final en cours au moment du commit). **Déploiement** : plugin RPGQuest → VeryGames (JAR only, backup daté, **un seul** restart RCON, règle anti-auto-reboot) + Control Panel AWS `deploy.sh`. **Validation live** : `player.catalog` → SUCCESS attendu ; **ban/unban en réel uniquement sur cible de test sûre** (jamais l'owner) sinon `PENDING MANUAL VALIDATION`. Navigateur authentifié `/players` = `PENDING OWNER`. Docs : `/docs/joueurs-admin` (+ `_index.txt`), bible §5, `current_state`, `docs/control-panel/ROADMAP.md` (Étape 3j), `SERVER_CHANGELOG`. **#96 non fermée, aucun merge.** | DONE | [2026-09-09_1400_players-annuaire-offline-96.md](2026-09-09_1400_players-annuaire-offline-96.md) |
| 2026-09-09 | **#46 — passe UX ciblée sur l'éditeur guidé de quêtes du Control Panel.** Squelette de `/quests/new` déjà en place mais une validation navigateur réelle avait révélé des défauts. **Audit** : le système de descripteurs demandé (`content/Descriptors.java` : 7 types d'objectif + 4 de récompense **réels** du moteur, noms de champ = ceux lus par `QuestDefinitionParser`) **existait déjà** — les incohérences venaient du **rendu**, pas du modèle. **Corrections (Control Panel uniquement, aucun code plugin/agent/parseur, validation finale/round-trip/diff/hash/CSRF inchangés)** : **(1) Un type = une source de vérité, champs pilotés par le type** — `renderRow` émet pour *chaque* type du catalogue un `<div class="type-fields" data-kind>` complet issu du même descripteur ; un seul visible+actif, les autres `hidden`+`disabled` (ni soumis, ni validés). `panel.js` `initEditorForms`/`applyType` bascule au `change` du `<select>` **sans recharger**, réactive les champs du nouveau type et **vide** ceux des autres (jamais de valeur d'un type précédent conservée). Sans JS : le serveur re-rend le bon jeu au 1er aller-retour ; nettoyage serveur de secours (`normaliseRow` : liste blanche `Descriptors.fieldNames` + champ caché `_was` → efface si le type a changé). Libellé récompense `EXPERIENCE` → « Points d'expérience » (fin de « Montant » ambigu). **(2) Construire le brouillon jamais bloqué** — tous les boutons `_action` (`add/del/mv` étape·objectif·récompense, `refresh`, `validate`, `save`) portent `formnovalidate` : la validation HTML `required` ne bloque plus la structure ni une suppression ; la validation métier (`QuestValidator`, ERREUR/ATTENTION/INFO) n'a lieu qu'à « Vérifier »/« Enregistrer ». Bouton `submit` par défaut caché (`refresh`) → « Entrée » ne déclenche plus la 1re action destructive. **(3) Scroll / contexte** — ids stables (`step-<i>`, `obj-<i>-<j>`, `rew-<i>`, `q-<i>`, `sec-*`) + `formaction=".../save#ancre"` sur chaque bouton → recentrage par le navigateur après le POST, **sans JS** ; filet JS `sessionStorage` pour les suppressions. **(4) Listes recherchables** — `panel.js` `initCombo()` transforme tout `<input list>` de l'éditeur en liste filtrée (valeur **ou** libellé humain), largeur alignée, hauteur bornée + scroll, repli au-dessus, clavier ↑/↓/Entrée/Échap, clic, fermeture au clic extérieur ; **aucune dépendance, aucun CDN** (CSP `default-src 'self'`) ; sans JS l'`<input list>` natif reste. Sources corrigées : **catégorie** = liste curée `RefData.CATEGORIES` + saisie libre (plus jamais alimentée par les PNJ), **icône** = matériaux, **entité/matériau** avec libellé FR (`MinecraftNames`). Tests : `EditorDescriptorsTest` (6, nouveau — kinds = moteur, chaque champ label+aide+nom moteur, aucun débordement entre types, libellé XP, `fieldNames` liste blanche) + `ContentEditorPagesTest` (+9 — `formnovalidate` sur toutes les actions, actions structurelles OK avec formulaire vide, ancre de scroll, type objectif → champs+aide cohérents, récompense ITEM = objet+quantité jamais XP, changement de type efface les valeurs, catégorie ≠ liste PNJ, combos recherchables). `:control-panel:test` **272/0** ; `:control-panel:build` vert ; `./gradlew test` + `./gradlew build` __(voir rapport)__. **Déploiement** : Control Panel AWS uniquement (`scripts/plugadmin/deploy.sh`, `systemctl restart plugadmin`) — **VeryGames / Minecraft non touchés, aucun redémarrage MC, aucune migration**. Rollback : `scripts/plugadmin/rollback.sh app`. **PENDING MANUAL VALIDATION** : scénario navigateur §28 authentifié (mot de passe owner non détenu). Docs : `RPGQUEST_BIBLE.md` §46, `current_state.md` (V3), `docs/quetes.md` (`/docs`). **Limites** : PNJ donneur affiché par id (pas de libellé dans `npc.list`), items custom RPGQuest non exposés (le moteur ne lit qu'un `Material` vanilla), action agent `quest.definition.validate` toujours à faire. **#46 non fermée, aucun merge.** | DONE | [2026-09-09_1607_editeur-quetes-passe-ux-46.md](2026-09-09_1607_editeur-quetes-passe-ux-46.md) |
| 2026-09-09 | **#111 → #120 — resynchronisation automatique après mutation + passe UX des formulaires PNJ/Dialogues du Control Panel.** **Priorité donnée à la synchronisation.** **Audit** : les pages métier lisent le **dernier relevé `*.list` réussi stocké** (`AgentPages#latestDetails` → `AgentStore#latestSuccessfulActionOfType`) ; après une mutation (`npc.definition.update`, `npc.citizens.link`, `dialogue.definition.create`…) **rien** ne ré-exécute le relevé correspondant → l'instantané reste l'ancien → « Rafraîchir catalogue **puis** F5 » obligatoire. `panel.js` suivait déjà le cycle des actions (toasts/cloche) mais ne rechargeait jamais la page. **Correctif mutualisé (aucun JS dispersé, aucun refresh ad hoc par bouton)** : `AgentActionCatalog.Spec` gagne `sensitive` + `refreshTypes` (relevés périmés par l'action : `npc.definition.*`→`npc.list` ; `npc.citizens.link/create`→`npc.list`+`npc.citizens.list` ; `dialogue.*`→`dialogue.list` ; `quest.giver.set`→`npc.list`+`quest.list` ; `player.ban/unban`→`player.catalog`). `AgentStore#hasOpenActionOfType`. `AgentEndpoints#handleActionResult` ré-enfile ces relevés **à la première transition SUCCESS** (`created_by="auto"`, dédup, jamais sur échec ni renvoi idempotent). `NotificationCenter#recent()` masque les lignes `auto` ; `/agents/actions.json` les expose (`auto:true`) et les compte dans `pending`. `panel.js` : un **unique** `reloadPlan` s'arme quand une action **vue s'exécuter** (pending→succès) se termine, et se déclenche dès `pending==0` (garde `sawPending` + marqueur `sessionStorage`, repli 30 s). Résultat : après « Enregistrer », fiche + liste + diagnostics se réconcilient **sans F5 ni clic manuel**. **UX** : `AgentPages#mutationConsent` supprime la case à cocher cachée des éditions réversibles (création/modif de définition, nœud, choix, liaison logique, `quest.giver.set`) — une phrase d'info suffit ; confirmation explicite conservée pour `player.ban/unban`, `player.resetnew.confirm`, `npc.citizens.create`, `dialogue.choice.delete`. Formulaire PNJ (#111) : exemples en `placeholder` seulement, aide métier par champ, lien doc `target=_blank`. Page Dialogues (#117/#118) : bouton primaire **`+ Nouveau dialogue`** (fin de l'accordéon discret), bandeau « phase 1 / format canonique » → une phrase + `<details>`, **palette de couleurs MiniMessage** (`AgentActionCatalog.PALETTE_COLORS`, 14 teintes, pastilles + aperçu `panel.js#initColorPalette`, `MiniText.colorHex` rendu public) qui génère `<couleur>…</couleur>` sans ré-enrober un MiniMessage saisi à la main ; ID technique distinct du « Locuteur affiché » ; `autocomplete="off"`. **Non traité (délibéré, roadmap 3l)** : #114 (sélecteur Citizens recherchable/paginé), #118 (sélecteur de locuteur alimenté par les PNJ), #113 (modale de liaison dédiée). Tests : `CatalogResyncTest` (5 — auto-refresh après succès, double catalogue Citizens, dédup, renvoi idempotent, échec sans refresh, mécanisme `panel.js`), `AgentActionCatalogTest` (+3 — `sensitive`/`refreshTypes`/palette), `NpcsCatalogTest`/`DialoguesCatalogTest` mis à jour (création réversible sans `confirm`). `:control-panel:test` **279/0** (1 ignoré pré-existant) ; `:control-panel:build` vert ; `./gradlew build` __(voir rapport)__. **Déploiement** : Control Panel AWS uniquement (`scripts/plugadmin/deploy.sh`, `systemctl restart plugadmin`) — **VeryGames / Minecraft non touchés, aucune migration, aucun format YAML modifié**. Rollback : `scripts/plugadmin/rollback.sh app`. **PENDING MANUAL VALIDATION** : 6 scénarios navigateur authentifiés (PNJ→dialogue sans F5, liaison Citizens sans F5, création de dialogue + palette, absence de checkbox, échec backend, mobile) — mot de passe owner non détenu. Docs : `RPGQUEST_BIBLE.md`, `current_state.md`, `docs/control-panel/ROADMAP.md` (Étape 3l), `SERVER_CHANGELOG.md`, `dialogues-depannage.md`. **#111 → #120 non fermées, aucun merge.** | DONE | [2026-09-09_2016_resync-mutations-ux-formulaires-controlpanel-111-120.md](2026-09-09_2016_resync-mutations-ux-formulaires-controlpanel-111-120.md) |
| 2026-09-09 | **#124 — MVP waypoints par instance de biome.** Nouveau package `com.lodygames.rpgquest.waypoint`, **additif**, bâti sur l'infra partagée (`DatabaseManager`, `SchemaMigrator`, `PluginService`, `RandomSafeLocationFinder`, patron repository + table de découverte) — **le système `waystone` existant (réseau de voyage sur grille, déployé) n'est PAS modifié** (muter l'identité d'un système à données de prod violerait « intégrité des données » ; décision structurante documentée, réversible). **Instance de biome** = `(monde, biomeKey, regionX, regionZ)` avec `region = floor(coord / region-size)` (défaut 256) — deux forêts éloignées → deux waypoints, jamais `biomeType -> waypoint` ; compromis (pas un flood-fill de blob contigu) documentés dans `docs/WAYPOINTS.md` ; colonne `biome_instance` stockée explicitement pour migrer l'identité plus tard sans changement de schéma. **Moteur** (`WaypointService`) : génération paresseuse **single-flight** (verrou mémoire + `INSERT OR IGNORE` + index unique `(world, biome_instance)` → deux entrées simultanées = un seul waypoint), `PlayerMoveEvent` filtré (changement de bloc) + throttle temporel par joueur + cache de la dernière instance (jamais de scan terrain par déplacement), recherche de surface `RandomSafeLocationFinder`, empreinte « zone naturelle libre + hors claim » (jamais de destruction de construction), `minimum-spacing`, échec → retry **borné** 30 s→1 h (pas de boucle). **Rendu versionné** (`waypoint.render.WaypointModelRegistry`) : la logique ne stocke qu'un `model_version` ; V1 = `COBBLESTONE_WALL` + `GOLD_BLOCK` + `STONE_BUTTON` latéral ; l'identité (`id`, `biome_instance`) est **indépendante du rendu** (test dédié). **Découverte** : la proximité ne découvre rien, **seul un clic droit sur le bouton** valide (persistée par UUID, `waypoint_discoveries`), message + son. **Protection MVP** (`WaypointProtectionListener`, patron `ClaimProtectionListener`) : casse joueur, explosion, piston, feu (burn/ignite), fluide entrant, entités (sable/gravier/enderman) ; bypass `rpgquest.admin.world`. Protection fonctionnelle anti-enfermement = **#122**, hors périmètre (architecture compatible). **Migration V18** (`waypoints`, `waypoint_discoveries` + index unique) — additive, idempotente, portable MariaDB. **Config** `travel.waypoint.*` (8 clés, défauts sûrs, auto-complétée). Tests : **33 tests waypoint** (`WaypointIdentityResolverTest` 7, `WaypointGenerationPlannerTest` 4, `WaypointModelRegistryTest` 5, `WaypointServiceTest` 11 MockBukkit, `WaypointProtectionListenerTest` 6) + `SchemaMigratorTest`/`MySqlDialectTranslationTest`/`ConfigValidatorTest` étendus. `./gradlew build` **vert** (root `:test` 1254/0, `:control-panel:test` 279/0). **Control Panel** `/waypoints` : **non livré** (priorité au moteur, temps ; `WaypointService` expose `all()`/`byId()`/`discoveryCount()`). **Déploiement DEV VeryGames : NON effectué** (`scripts/verygames.env` absent de la box + règle « aucun déploiement automatique » + 1re pose de blocs autonome = validation humaine souhaitable) — commande exacte fournie (`./gradlew build` ; `scripts/deploy-verygames.sh` ; `scripts/verygames-restart.sh`). **PENDING MANUAL VALIDATION** : `docs/MANUAL_TEST_PLAN.md` TC-210 (rendu réel, biomes réels, physique fluides/pistons/gravité, suppression redstone du bouton). Docs : `docs/WAYPOINTS.md` (nouveau), `RPGQUEST_BIBLE.md` (§7 + migrations), `current_state.md`, `TRAVEL.md`, `README.md`, `TODO.md`, `.ai/ROADMAP.md`, `SERVER_CHANGELOG.md`. Branche `feat/control-panel-admin-tools` (consigne : y rester), **#124 non fermée, aucun merge.** | DONE | [2026-09-09_2118_waypoints-mvp-instance-biome-124.md](2026-09-09_2118_waypoints-mvp-instance-biome-124.md) |
| 2026-09-09 | **#108 — export versionné des contenus déclaratifs (phase 1 du pipeline de contenus LodyQuests).** Nouveau **format public** `lodyquests-content-pack` (`schemaVersion: 1`, YAML déterministe) pour exporter **quêtes / stories / dialogues / PNJ logiques** — sauvegarde, archive, entrée pour l'import #109 puis la génération IA #110. **Décision** : couche d'export **dans le plugin** (`com.lodygames.rpgquest.content.pack`) — le Control Panel n'a aucun accès disque au contenu et ne reçoit que des résumés d'affichage lossy. **Vocabulaire du pack = le schéma YAML RPGQuest existant** (contrat stable versionné, pas une 2ᵉ représentation métier → re-parsable, round-trip visé pour #109). DTO sans Bukkit (`ContentPack`, `*PackEntry`, `PackText`), `ContentPackMapper` (modèle runtime → DTO ; jamais de sérialisation runtime directe ; couvre 7 objectifs / 4 récompenses / 10 actions / 7 conditions de dialogue ; déplie `NegatedCondition` → `negate: true`), `ContentPackSerializer` (YAML canonique écrit à la main — ordre de clés fixe, familles triées par id, textes entre guillemets, `content` garde ses 4 clés — **octet-pour-octet reproductible**), `ContentFamily` + `ContentPackAssembler` (`exportAll`/`exportFamily`/`exportElement`/`exportSelection`, manifest + `counts`, `exportedAt` injecté). **Action agent lecture seule** `content.export` (`AgentActionType.CONTENT_EXPORT`, params `family`+`ids`) → pack sous `details.pack`, borné à **~56 Kio** (plafond transport 64 Kio ; au-delà = échec lisible, exporter par famille/élément). **Control Panel** : `Permission.CONTENT_EXPORT` (rôles en lecture), page `/content/export` (Exporter tout / par famille / sélection d'ids), `GET /content/export/download?action=<id>` (`Content-Disposition: attachment`, `lodyquests-<famille>-<AAAA-MM-JJ>.yaml`), audit enfilement + téléchargement, entrée de menu, CSS `.export-*` mobile, CSP inchangée. **Sécurité** : par construction aucun secret / donnée joueur / chemin système / état runtime dans le pack (les modèles source ne portent que du contenu éditorial) — test dédié. `secret: true` exporté (backup fidèle) avec son flag. **Tests** : plugin `content.pack.*` (22 : sérialiseur 11, assembleur 7, mapper 4) + `AgentActionExecutorTest` (+6) ; control-panel `AgentActionCatalogTest` (+2), `ContentExportNameTest` (3), `AuthenticatedSmokeTest` (`/content/export`). `./gradlew build` vert. **Doc** : `docs/CONTENT_PACK.md` (nouveau — format + exemple complet + stratégie `schemaVersion` + lien #109/#110), BIBLE §5, `current_state`, `SERVER_CHANGELOG`. **Hors périmètre (assumé)** : import #109, génération IA #110, familles items/recettes, découpage transport, boutons par ligne sur les 4 pages métier. **Aucune migration, aucun déploiement, aucun merge.** Branche `feat/control-panel-admin-tools`. **PENDING MANUAL VALIDATION** : navigateur owner (`/content/export`, téléchargement, mobile). | DONE | [2026-09-09_2215_export-versionne-contenu-108.md](2026-09-09_2215_export-versionne-contenu-108.md) |
| 2026-09-10 | **#46 — passe UX ciblée éditeur de quêtes (bugs navigateur confirmés).** L'audit montre que la passe #46 précédente (`dbb1420`, déployée) rendait déjà le bon HTML côté serveur (descripteurs, jeux de champs par type, `_was`/`normaliseRow`, `formnovalidate`, ancres, combos) — tests HTTP verts. Les bugs restants étaient **runtime navigateur** : (1) l'attribut HTML `required` toujours émis pouvait bloquer une action de brouillon (« Veuillez renseigner ce champ ») selon le chemin de soumission ; (2) le scroll repartait en haut car un POST HTML 200 n'applique pas de façon fiable le fragment d'un `formaction`, et le repli `panel.js` était court-circuité dès qu'un hash apparaissait ; (3) un `init*` de `panel.js` qui échouait tuait la suite (bascule de type, combos) ; (4) la datalist PNJ n'avait aucun libellé humain ; (5) l'aide de la quantité d'une récompense reprenait un texte d'objectif. **Correctifs** : `<form class="editor" novalidate>` + arrêt de l'émission de l'attribut `required` (marqueur `*` conservé) → construire/supprimer/réordonner ne peut plus être bloqué, validation métier 100 % serveur aux seuls Vérifier/Aperçu/Enregistrer ; `panel.js` restaure explicitement la position au chargement (cible du `#hash` via `scrollIntoView({block:'center'})` + focus, sinon `scrollY` mémorisé) ; chaque module `panel.js` isolé en `try/catch` + `applyType()` appliqué une fois à l'init par `<select>` ; `RefData` transporte `id -> displayName` (depuis `npc.list`), datalist `dl-npc` = `<option value="guard" label="Garde">`, le combo filtre nom **et** id ; `Descriptors` : récompense `ITEM` = « objet vanilla, pas un objet personnalisé RPGQuest » explicite, `GIVE_AMOUNT` distinct de l'`AMOUNT` d'objectif. **Tests** : `ContentEditorPagesTest` +10 (`novalidate` + aucun `required`, ancre de scroll sur chaque bouton structurel, suppression d'une ligne à champs vides, isolation des 7 objectifs / 4 récompenses, changement de type qui efface l'incompatible, libellés PNJ, validation finale bloquante + aperçu conservé), `EditorDescriptorsTest` +2. `:control-panel:test` **294/0** ; `./gradlew build` vert ; `panel.js` `node --check` OK. **Déploiement AWS Control Panel** (`scripts/plugadmin/deploy.sh`, release `20260910-095149`, restart, `/health` ONLINE local+public, `panel.js` public à jour, 0 ERROR au journal) — **VeryGames / Minecraft non touchés, aucun redémarrage MC, aucune migration, aucun changement plugin**. Rollback : `scripts/plugadmin/rollback.sh app`. **PENDING MANUAL VALIDATION** : session owner navigateur (8 scénarios) — couverte par les tests HTTP authentifiés. Docs : `current_state.md` (V4), `RPGQUEST_BIBLE.md` §46, `SERVER_CHANGELOG.md`. **#46 non fermée** (action agent `quest.definition.validate` toujours à faire), aucun merge. | DONE | [2026-09-10_0949_editeur-quetes-passe-ux-46.md](2026-09-10_0949_editeur-quetes-passe-ux-46.md) |
| 2026-09-10 | **#46 — passe UX ciblée éditeur de *stories* (`/stories/new`, `/stories/edit/...`).** Suite de la passe éditeur de quêtes : l'éditeur de story reçoit la même logique validée (construire le brouillon sans blocage → « Vérifier » → « Aperçu » YAML + diff → « Enregistrer » si valide ; `<form novalidate>`, aucun `required`, conservation du scroll — déjà en place V4 ; bouton **« Actualiser le formulaire »** ajouté pour la parité). **Audit** : `StoryDraft` / `StoryYaml` / `StoryValidator` / `ContentWorkspace` reflètent déjà fidèlement le modèle moteur (`StoryDefinition` : `id`, `name`, `secret`, liste ordonnée de `questIds` — le parser ne résout jamais les références, **n'interdit pas les doublons**) → rien réécrit, rien inventé ; catalogue `/stories` déjà cohérent → inchangé ; `panel.js` inchangé (le combo affiche déjà valeur + label et filtre sur les deux). **Deux manques comblés** : (1) **§3/§7 — la sélection des quêtes ne montrait que l'id** : la datalist `dl-quest` porte maintenant le **titre humain** en `label` (`<option value="first_steps" label="Premiers pas">`), `RefData` transporte `id -> titre` depuis le dernier `quest.list` (`RefData.questNames` + `questLabel()`, namespace `rpgquest:` toléré, titre nettoyé via `MiniText.plain`) ; (2) **§5 — ordre peu lisible** : chaque ligne de la chaîne affiche `renderStoryQuestRow` = rang `N.` + **titre humain au-dessus** de l'id technique éditable, badge « quête inconnue » si le catalogue est chargé et l'id absent (reste un **WARNING**, jamais bloquant — le moteur ne résout pas les références). Doublon = avertissement (autorisé par `StoryDefinition`). Fichiers : `panel/content/RefData.java`, `panel/web/AgentPages.java`, `panel/web/ContentEditorPages.java`, `assets/plugadmin.css` (`.row-n` / `.row-title`). CSP inchangée, aucun CDN, `control-panel.db` non touché, aucune migration, aucune permission. **Tests** : `StoryEditorPassTest` (nouveau, **17** : rendu new/edit, datalist titre+id + repli id seul, `questLabel` nu/`rpgquest:`, titre au-dessus de l'id, badge quête inconnue, add/del/reorder sur brouillon incomplet sans validation, ancres de scroll, aucun `required`, validation id/nom/chaîne vide, doublon + référence inconnue, aperçu = YAML écrit + round-trip, diff, conflit de hash). `:control-panel:test` **311 / 0** (+17). `./gradlew build` : voir commit final. **Déploiement AWS Control Panel** (`scripts/plugadmin/deploy.sh`) — VeryGames / Minecraft / JAR non touchés, aucun redémarrage MC, aucune migration. Rollback : `scripts/plugadmin/rollback.sh app`. **PENDING MANUAL VALIDATION** : session owner navigateur (10 scénarios) — couverte par les tests de rendu. Docs : `current_state.md` (V5), `RPGQUEST_BIBLE.md` §46, `SERVER_CHANGELOG.md`. **#46 non fermée** (action agent `quest.definition.validate` toujours à faire), aucun merge. Branche `feat/control-panel-admin-tools`. | DONE | [2026-09-10_1032_editeur-stories-passe-ux-46.md](2026-09-10_1032_editeur-stories-passe-ux-46.md) |
| 2026-09-10 | **#50 — socle RBAC PlugAdmin : rôles, permissions et gestion des comptes (`/users`).** Consolidation (pas de réécriture) du contrôle d'accès du Control Panel autour du modèle **utilisateur → rôle → `Set<Permission>` → contrôle backend → UI filtrée**. **Audit** : `PermissionService.can` était déjà centralisé et utilisé partout (aucun `if role == OWNER` dispersé) ; `PasswordHasher` PBKDF2-HMAC-SHA256 210k = fort, **conservé** ; manquaient les rôles `ADMIN`/`BUILDER`, toute notion de compte/stockage/actif-inactif, une page de gestion, le filtrage de `Layout.nav()`, des 403 cohérents. **Rôles** (`authz.Role`, libellé FR) : `OWNER` (= `EnumSet.allOf(Permission.class)`, aucune liste à maintenir), `ADMIN` (exploitation serveur — **pas** de gestion des comptes ni du module dev), `TESTER` (lectures + `ACTION_QUEST/STORY/VARIABLE_GET` — pas d'écriture de contenu, pas de reset), `BUILDER` (docs + infos PNJ — **pas** `PLAYERS_READ`, pas d'action serveur), `CONTENT_EDITOR` (édition guidée quêtes/stories/dialogues/PNJ logiques — pas de modération/spawn), `READ_ONLY` (aucune permission qui pilote une mutation d'`AgentActionCatalog`). Nouvelle permission `USER_MANAGE` (seul ajout — usage réel : `/users`). Aucune permission fonctionnelle renommée. **Comptes** : nouveau paquet `panel.users` — `PanelUser`, `UserRepository` + `SqliteUserRepository` (**nouvelle table `panel_user` dans `control-panel.db`**, `CREATE TABLE IF NOT EXISTS` idempotent et additif — **aucun lien avec `data.db` ni MariaDB #42**) + `InMemoryUserRepository`, `UserDirectory` (validation, hachage, **garde-fous anti-verrouillage** : dernier OWNER actif ni rétrogradable ni désactivable, pas d'auto-désactivation ; `ensureBootstrapOwner` réamorce le compte `owner` d'environnement actif+OWNER à chaque démarrage → chemin de récupération). **Auth** : `AuthService` adossé au dépôt (casse ignorée, coût PBKDF2 payé même compte absent/inactif, `Outcome.DISABLED` pour un compte désactivé, `last_login_at` posé). **Session** : `Session.refreshRole` + re-contrôle par requête dans `currentSession` (compte supprimé/désactivé → session invalidée `303 /login` ; rôle réaligné sans reconnexion). **Page `/users`** (`USER_MANAGE`, motif liste compacte → clic → détail) : `GET /users` (+ création : identifiant, mot de passe ≥ 12 jamais réaffiché, rôle), `GET /users/<id>`, `POST /users/create` / `/users/<id>/role` / `/users/<id>/active` — **CSRF synchroniseur** sur chaque POST, permission backend (une requête directe sans droit → **403 unique et humain** « Vous n'avez pas l'autorisation d'accéder à cette fonction. » ; les 11 blocs 403 inline y sont ramenés), aucune élévation via paramètre client, **audit** `user.create` / `user.role.change` (`from=…/to=…`) / `user.active.change`, `login.failure` motif `compte désactivé`. **Navigation** : `Layout.nav()` + tuiles `HomePages` filtrées par permission (groupe vide masqué), topbar affiche l'utilisateur **et** son rôle. **Tests (+43)** : `RolePermissionMatrixTest` (10), `AuthServiceTest` (6), `UserDirectoryTest` (11), `SqliteUserRepositoryTest` (5, dont migration idempotente), `UserManagementTest` (11, bout-en-bout : 403 backend en appel direct, CSRF absent/faux, audit, mot de passe jamais réaffiché, dernier OWNER protégé, 2ᵉ OWNER débloque, session d'un compte désactivé invalidée, changement de rôle effectif sans reconnexion, navigation filtrée). `:control-panel:test` **354 / 0** (1 ignoré pré-existant) ; `./gradlew test` + `./gradlew build` **BUILD SUCCESSFUL** (`RPGQUEST_TEST_MAX_HEAP=768m`). **Déploiement AWS Control Panel effectué** (`scripts/plugadmin/deploy.sh`, release `20260910-155250`, `systemctl restart plugadmin` → `active (running)`, `/health` ONLINE local+public, `/login` 200, `/users` anonyme → 303 `/login`, table `panel_user` créée + `owner` amorcé, 0 ERROR/WARN au journal) ; **sauvegarde préalable** `control-panel.db.20260910-155247`. **VeryGames / Minecraft non touchés, aucun redémarrage MC.** Rollback : `scripts/plugadmin/rollback.sh app` + restauration de la copie datée. **PENDING MANUAL VALIDATION** : checklist navigateur owner (créer un TESTER, se connecter, menus visibles, `/users` interdit → 403, changer le rôle, désactiver). Docs : `current_state.md`, `RPGQUEST_BIBLE.md` (§3), `docs/control-panel/SECURITY.md` (section « Rôles et permissions #50 »), `SERVER_CHANGELOG.md`, `.ai/ROADMAP.md`. **Hors périmètre** (assumé, architecture non bloquée) : multi-rôles par compte, permissions par environnement/serveur, droits temporaires, approbation à deux niveaux, groupes, 2FA, SSO, invitations, reset de mot de passe self-service, suppression de compte (désactivation seulement). **#50 non fermée, aucun merge.** Branche `feat/control-panel-admin-tools`. | DONE | [2026-09-10_1553_rbac-plugadmin-50.md](2026-09-10_1553_rbac-plugadmin-50.md) |
